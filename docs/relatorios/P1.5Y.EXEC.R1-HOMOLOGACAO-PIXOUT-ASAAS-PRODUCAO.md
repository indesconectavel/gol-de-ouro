# P1.5Y.EXEC.R1 — Homologação PIX OUT Asaas Produção (Reexecução com Espera de Runtime)

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Base:** P1.5Y.EXEC FAIL (`no started VMs` + quoting SSH Windows)

---

## Veredito

# FAIL

A **espera de runtime funcionou** (90s, app started, checks passing, `/health` 200). **Preflight remoto PASS** foi obtido via SSH, porém o orquestrador **abortou** por exit code não-zero do `fly ssh` no Windows (`Identificador inválido`). **Nenhuma transferência real executada.** Gates **fechados**.

---

## 1. Resumo Executivo

| Critério | Status |
|----------|--------|
| Pré-check (schema, saque pendente, gates OFF) | ✅ |
| Abertura gates | ✅ |
| Espera runtime ≥ 90s | ✅ **Correção R1 validada** |
| Preflight SSH (remoto) | ✅ **PASS** (output) |
| Orquestrador interpretou preflight | ❌ exit code SSH Windows |
| Execução HTTP `/v3/transfers` | ❌ Não executada |
| Webhook / ledger / wallet live | ❌ N/A |
| Gates fechados | ✅ |
| `/health` pós-janela | ✅ 200 |

---

## 2. Timeline

| UTC | Fase | Evento |
|-----|------|--------|
| 05:08:13 | 1 | Pré-check OK |
| 05:08:18 | 2 | Gates abertos (v501) |
| 05:09:50 | 3 | Runtime estável após 90s — app started, 1/1 passing, health OK |
| 05:09:54 | 4 | Preflight SSH → **###P15Y_VERDICT PASS** + erro Windows pós-output |
| 05:09:54 | — | Orquestrador FAIL (exit code 1) → gates fechados |
| 05:10:07 | 8 | Gates OFF confirmado |

**Tentativa 1 (mesma sessão):** SSH sem `sh -c` → `executable file not found` — corrigido antes da tentativa 2.

---

## 3. Correção R1 — Espera de Runtime

**Problema P1.5Y.EXEC:** SSH antes do app estabilizar.

**Solução R1:** poll 90s mínimo + `fly status` + `/health`.

**Resultado:**

```text
poll elapsed=90s app.started=true checks=true health=true
```

Máquina `080e207b071048` — `started`, `1 total, 1 passing` (v501).

---

## 4. Preflight (evidência remota PASS)

```json
{
  "saqueId": "a705bb8c-5f90-4532-9f55-d167dbdff672",
  "status": "pendente",
  "amount": 10,
  "net_amount": 8,
  "correlation_id": "486f6bef-4d6f-42da-be56-62a28e55dd3d",
  "pixKeyMasked": "0478***8664",
  "walletSaldo": 0,
  "ledgerCount": 2,
  "ledgerTipos": ["saque", "taxa"]
}
```

Veredito script: `"phase":"preflight","result":"PASS"`

**Erro pós-output (Windows):** `Error: Identificador inválido.` — exit code 1 apesar do PASS.

---

## 5. Transferência

**Não executada.** Saque permanece:

| Campo | Valor |
|-------|-------|
| `status` | `pendente` |
| `asaas_transfer_id` | `null` |

---

## 6. Gates

### Abertura

```bash
fly secrets set ASAAS_PIX_OUT_PRODUCTION_ENABLED=true PAYMENT_ENGINE_PIXOUT_ENABLED=true ASAAS_PIX_OUT_ENABLED=true ASAAS_PRODUCTION_ENABLED=true PAYOUT_PROVIDER=asaas -a goldeouro-backend-v2
```

### Fechamento (automático pós-FAIL)

```bash
fly secrets set ASAAS_PIX_OUT_PRODUCTION_ENABLED=false PAYMENT_ENGINE_PIXOUT_ENABLED=false ASAAS_PRODUCTION_ENABLED=false -a goldeouro-backend-v2
fly secrets unset PAYOUT_PROVIDER ASAAS_PIX_OUT_ENABLED -a goldeouro-backend-v2
```

---

## 7. Bloqueador Remanescente

| # | Bloqueador | Mitigação sugerida |
|---|------------|-------------------|
| 1 | Exit code `fly ssh` no Windows ≠ sucesso remoto | Validar `###P15Y_VERDICT### PASS` no stdout, não exit code |
| 2 | Orquestrador abortou antes do execute | Executar fases 5–7 manualmente em **uma janela** (bash/WSL ou script corrigido) |
| 3 | Saque legado vs conta técnica | Criar saque R$10 em `85872488-…` |

---

## 8. Plano para Próxima Janela (P1.5Y.EXEC.R2 — não executado)

1. Abrir gates  
2. Aguardar 90s + status + health (**procedimento R1 validado**)  
3. Preflight SSH — aceitar PASS por output  
4. **Execute único** (sem abortar por exit code Windows)  
5. Wait → replay → fechar gates → verify-p15y  

Comando execute (referência):

```bash
fly ssh console -a goldeouro-backend-v2 -C "sh -c 'P15Y_PHASE=execute P15Y_ALLOW_REAL_PAYOUT=1 P15Y_SAQUE_ID=a705bb8c-5f90-4532-9f55-d167dbdff672 node /app/scripts/p15y-pix-out-production-live.cjs'"
```

---

## 9. P1.5Z

**BLOQUEADA** — homologação live sem transferência real.

---

## 10. Veredito Final

**FAIL** — infraestrutura e preflight remotos OK; execução financeira live não realizada por abort operacional Windows pós-preflight.

---

*Artefatos: `tmp/p15y-exec-r1-state.json`, `tmp/p15y-exec-r1-console-2.log`, `scripts/run-p15y-exec-r1.cjs`*

*Pipeline: P1.5Y.EXEC (FAIL) → **P1.5Y.EXEC.R1 (FAIL operacional)** → P1.5Z (bloqueado)*
