# P1.7B — Auditoria Asaas Imediata Pré-Homologação Final

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-30  
**Modo:** READ-ONLY ABSOLUTO  
**Base:** P1.7A (PASS)  
**App Fly:** `goldeouro-backend-v2`  
**Saque preparado:** `f3723ce8-a24f-4a4d-ac03-6f8d34bcc0ef`

---

# Veredito

# GO

Arquitetura íntegra, saque técnico pendente com chave correta, webhooks e homologador alinhados. **Nenhum novo bloqueio interno** detectado desde P1.7A. O risco externo conhecido (`invalid_action` / autorização crítica Asaas) permanece registrado e **não impede GO** para P1.7.

---

# Etapa 1 — Estado operacional

| Item | Evidência | Resultado |
|------|-----------|-----------|
| `/health` | 200, `database: connected`, v1.2.1 | ✅ |
| `/meta` | `gitCommit: 6e24318c8aa950173aa54379a1940fa5c4bde0e4` | ✅ |
| Release Fly | **v532**, máquina `080e207b071048` started, **1/1 checks** | ✅ |
| Runtime | App started; `payout_worker` stopped (esperado) | ✅ |
| Payment Engine | Gates PIX OUT OFF; `PAYOUT_PROVIDER` unset | ✅ |
| Homologador P1.6ZA | Precheck `READY_TO_EXECUTE`; verify P1.6W PASS | ✅ |
| Webhook P1.6W | Probe financeiro **401** (não 403) | ✅ |

**Veredito etapa 1:** **PASS**

---

# Etapa 2 — Saque preparado

**ID:** `f3723ce8-a24f-4a4d-ac03-6f8d34bcc0ef`  
**Auditoria:** `node scripts/p16c-audit-only.cjs` + `p16b-r3-audit-saques.mjs`  
**Timestamp:** `2026-06-30T23:14–23:22Z`

| Campo | Valor | OK? |
|-------|-------|-----|
| Status | **`pendente`** | ✅ |
| Valor bruto | R$ 10,00 | ✅ |
| Taxa | R$ 2,00 | ✅ |
| Valor líquido | R$ 8,00 | ✅ |
| Wallet (`usuarios.saldo`) | R$ 166.545,00 | ✅ |
| `correlation_id` | `p17a-7690fece-bb6f-42fd-8289-fb7cb1f8721b` | ✅ |
| `asaas_transfer_id` | **null** | ✅ |
| Ledger | `saque` + `taxa` (sem rollback) | ✅ |
| Elegibilidade R3 | `###P16B_R3_VERDICT### READY` | ✅ |

**Nenhuma alteração** desde P1.7A.

---

# Etapa 3 — Chave PIX

| Campo | Valor persistido |
|-------|------------------|
| `pixAddressKeyType` | **EMAIL** (`tipo_chave: email`) |
| `pixAddressKey` | **`indesconectavel@gmail.com`** |

Confirmado em `pix_key` e `chave_pix` (DB + precheck).

**Não utilizada:** `validacao.goldeouro.e2e@gmail.com` — presente apenas no saque legado `d0313dfe-…` (**falhou**).

---

# Etapa 4 — Auditoria conta Asaas (read-only)

## API

| Verificação | Resultado |
|-------------|-----------|
| Chave ativa | ✅ `GET /v3/myAccount` → HTTP **200** |
| Tipo chave | `$aact_prod_…` (produção) |
| Conta | `personType: JURIDICA`, `accountStatus: APPROVED` |
| Autenticação | ✅ Válida |
| Saldo disponível | ✅ **R$ 10,19** (`GET /v3/finance/balance` → 200) — cobre líquido R$ 8,00 |

*Consulta via API Key de produção (Fly runtime); valores de key omitidos.*

## Segurança

| Item | Status |
|------|--------|
| Validação de saque via Webhook (backend) | ✅ Rota `/webhooks/asaas/transfer-validation` ativa |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` | ✅ Deployed (digest Fly) |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | **false** em repouso (esperado — habilitado na janela P1.7) |
| Configuração painel Asaas (auth webhook URL + token) | ⚠️ **Não revalidável via API** nesta sessão — assumida conforme P1.6C/P1.6H |

## Webhook

| Webhook | Estado backend | Probe |
|---------|----------------|-------|
| Financeiro (`/webhooks/asaas`) | `ASAAS_WEBHOOK_ENABLED=true` | HTTP **401** `INVALID_PAYLOAD` — ingress OK (P1.6W) |
| Auth (`/webhooks/asaas/transfer-validation`) | Secrets + rota deployados | HTTP **404** `asaas_transfer_auth_webhook_disabled` — repouso OK |
| Token financeiro | `ASAAS_WEBHOOK_TOKEN` deployed | ✅ |
| Token auth | `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` deployed | ✅ |

**Verify P1.6W:** `Verification PASSED (P1.6W webhook gate decoupling)`

## Fila (webhooks Asaas)

| Status | Evidência |
|--------|-----------|
| **Incerto** (painel Asaas) | API `GET /webhooks` não consultável de forma confiável nesta sessão |
| Ingress financeiro pós-P1.6W | ✅ Respostas **401/200** (não **403**) — não penaliza fila conforme doc Asaas |
| Histórico pré-P1.6W | Respostas **403** repetidas documentadas em P1.6X — risco residual de fila pausada **não confirmado nem descartado** |

**Recomendação operacional (fora do escopo P1.7B):** verificar fila no painel Asaas antes ou logo após P1.7. **Não constitui bloqueio interno para GO.**

---

# Etapa 5 — Logs Asaas

## Último erro conhecido (paridade P1.5Y / P1.6F / P1.6G)

| Campo | Valor |
|-------|-------|
| **Timestamp** | `2026-06-30T15:34:24Z` (P1.6F) / `2026-06-29T05:28:26Z` (P1.5Y.R2) |
| **Endpoint** | `POST https://api.asaas.com/v3/transfers` |
| **HTTP** | **400** |
| **Resposta Asaas** | `{ "code": "invalid_action" }` (via `errorCode` sanitizado) |
| **Saque** | `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |
| **`pixAddressKey`** | **`validacao.goldeouro.e2e@gmail.com`** |
| **`pixAddressKeyType`** | `EMAIL` |
| **`value`** | `8.00` |
| **`externalReference`** | `wd_abbd5e0512f808f2c4fc12ab` |
| **`transfer_id` criado** | **null** |
| **Efeito** | `falha_payout` + `rollback` em ~200 ms |

## Confirmação chave do erro

| Chave | Usada no erro `invalid_action`? |
|-------|--------------------------------|
| `validacao.goldeouro.e2e@gmail.com` | ✅ **Sim** (P1.6F, P1.5Y.R2) |
| `indesconectavel@gmail.com` | ❌ **Não** — saque P1.7A ainda **não** passou por `approve-and-send` |

**Logs Fly instante P1.6F:** buffer rotacionado — sem linhas `asaas_api_request_failed` retidas. Evidência primária: ledger + paridade P1.5Y com log estruturado `invalid_action`.

**Painel Asaas Logs de Requisições:** não acessado nesta sessão — expectativa documentada em P1.6G.

---

# Etapa 6 — Auditoria documental

**Fontes:** P1.6H (PASS READ-ONLY), documentação oficial Asaas (transfer, webhook auth, segurança, penalização de filas).

**Pergunta:** Existe alguma exigência documental ainda não atendida?

## NÃO

*(no lado integração Gol de Ouro)*

| Requisito documental | Status |
|---------------------|--------|
| Payload `POST /v3/transfers` compatível | ✅ |
| `pixAddressKeyType=EMAIL` | ✅ |
| Webhook auth mechanism implementado | ✅ (backend + homologador P1.6ZA) |
| Webhook financeiro desacoplado (P1.6W) | ✅ |
| Fluxo PIX OUT documentado | ✅ |

**Ressalva:** a documentação Asaas exige **liberação de conta** (autorização crítica desabilitada **ou** webhook de validação configurado no **painel Asaas**) — pendência **operacional/conta**, não de código. Escalation P1.6G/H permanece válida.

---

# Etapa 7 — Classificação de risco

## Arquitetura Gol de Ouro — **BAIXO**

- Release v532 estável, health OK
- Precheck `READY_TO_EXECUTE`, zero blockers internos
- Saque pendente elegível, chave correta
- Homologador P1.6ZA + webhook P1.6W validados
- Payment Engine íntegra; gates fechados

## Conta Asaas — **ALTO**

- Histórico consistente de `invalid_action` em `POST /v3/transfers`
- Autorização crítica / configuração de conta não confirmada como resolvida
- Webhook auth no painel Asaas não revalidado nesta sessão
- Status da fila de webhooks financeiros **incerto**
- Saldo R$ 10,19 é suficiente para R$ 8,00, mas **não** elimina `invalid_action`

**Objetivo:** P1.7 é a tentativa única que testará se a conta Asaas aceitará transferência com chave **`indesconectavel@gmail.com`** e webhook auth habilitado na janela.

---

# Etapa 8 — GO / NO-GO

# GO

| Critério GO | Atendido? |
|-------------|-----------|
| Arquitetura íntegra | ✅ |
| Saque pronto (`pendente`, net R$ 8) | ✅ |
| Chave `indesconectavel@gmail.com` | ✅ |
| Webhook correto (ingress + secrets) | ✅ |
| Homologador correto (P1.6ZA) | ✅ |
| Novo bloqueio interno | ❌ Nenhum |

**Risco Asaas conhecido registrado — não bloqueia GO estrutural.**

---

# Checklist Final

| Item | Status |
|------|--------|
| Backend | ✅ PASS |
| Runtime | ✅ PASS (v532, 1/1 checks) |
| Payment Engine | ✅ PASS (gates OFF) |
| Homologador | ✅ PASS (P1.6ZA) |
| Saque | ✅ PASS (`pendente`, elegível) |
| PIX Key | ✅ PASS (`indesconectavel@gmail.com`) |
| Webhook Financeiro | ✅ PASS (ingress 401, P1.6W) |
| Webhook Auth | ✅ PASS (secrets + rota; OFF repouso) |
| API Key | ✅ PASS (prod, myAccount 200) |
| Conta Asaas | ⚠️ APPROVED + saldo OK; risco `invalid_action` **ALTO** |
| Logs | ⚠️ Último erro `invalid_action` com chave **legada** |
| GO / NO-GO | **GO** |

---

# Operações NÃO executadas

approve-and-send · abertura gates · POST /v3/transfers · novo saque · alteração código/backend/banco/secrets/Fly/deploy · webhook POST real de payout

---

# Variáveis para P1.7

```bash
export P16C_SAQUE_ID=f3723ce8-a24f-4a4d-ac03-6f8d34bcc0ef
export P16C_PIX_KEY=indesconectavel@gmail.com
# Uma única tentativa:
# P16C_ALLOW_REAL=1 P16C_PHASE=homolog node scripts/p16c-homolog-final.cjs
```

---

# Artefatos

| Arquivo | Conteúdo |
|---------|----------|
| `docs/relatorios/P1.7B-AUDITORIA-ASAAS-PRE-HOMOLOGACAO.md` | Este relatório |
| `tmp/p16c-audit.json` | Bloco `p17bAudit` |
| `tmp/p16c-final-precheck.json` | `READY_TO_EXECUTE` @ 23:14Z |
| Consulta Asaas prod | `myAccount` 200, `balance` 10.19 |

---

# Pipeline

```text
P1.7A (PASS)
    ↓
P1.7B (GO)
    ↓
P1.7 — Homologação Final PIX OUT (UMA ÚNICA TENTATIVA)
```

---

*Gerado em 2026-06-30T23:24Z · Modo READ-ONLY absoluto · Nenhuma transferência executada*
