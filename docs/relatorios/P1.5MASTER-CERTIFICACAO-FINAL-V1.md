# P1.5MASTER — Certificação Final V1 (Webhooks + PIX OUT Asaas Produção)

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** Produção controlada — pipeline oficial P1.5MASTER  
**App Fly:** `goldeouro-backend-v2` · release **v511**

---

## Veredito

# PASS COM RESSALVAS

Webhook Asaas em produção **homologado** com evento real pós-gate (`PAYMENT_RECEIVED`, HTTP 200, crédito R$ 5,00). PIX OUT Asaas **executado uma única vez** com `POST /v3/transfers` **HTTP 200**, `transfer_id` criado, saque atualizado. Gates principais **fechados**. **Ressalvas:** webhook de transferência ainda pendente (`aguardando_confirmacao`); saque legado (não conta técnica); `ASAAS_PIX_OUT_ENABLED` permanece deployado `true`.

---

## 1. Resumo executivo

| Fase | Resultado |
|------|-----------|
| 1 — Preflight webhook | ✅ PASS |
| 2 — Captura logs | ✅ Ativa |
| 3 — Cobrança PIX R$ 5 | ✅ PASS |
| 4 — Webhook capturado | ✅ PASS |
| 5 — Auditoria wallet/ledger | ✅ PASS |
| 6 — Fechar gate webhook | ✅ PASS |
| 7 — Preflight PIX OUT | ✅ PASS (1 candidato) |
| 8 — Abrir janela PIX OUT | ✅ PASS |
| 9 — PIX OUT único | ✅ PASS |
| 10 — Asaas `/transfers` | ✅ HTTP 200 |
| 11 — Auditoria final | ✅ PASS |
| 12 — Fechamento gates | ✅ PASS |
| Relatório | ✅ |

---

## 2. Cronologia

| UTC | Evento |
|-----|--------|
| `18:10:37` | Preflight Fase 1 — health 200, gate webhook ON |
| `18:20:18` | Cobrança PIX criada `pay_nguleet890a6t6h6` (R$ 5) |
| `18:20:19` | Webhook `PAYMENT_CREATED` — token válido |
| `18:27:27` | Webhook `PAYMENT_RECEIVED` — crédito fallback Asaas |
| `18:32:57` | Validação P14 — `asaas_paid`, wallet +R$ 5 |
| `18:37:16` | Gate webhook fechado (`ASAAS_PRODUCTION_ENABLED=false`) |
| `18:39:17` | Janela PIX OUT aberta |
| `18:43:00` | Preflight saque `94480975-…` PASS |
| `18:43:01` | `POST /v3/transfers` HTTP **200** — transfer `44a2a04e-…` |
| `18:46:15` | Snapshot auditoria — saque `aguardando_confirmacao` |
| `18:47:48` | Fechamento total gates — health 200 |

---

## 3. Webhook Asaas (Fases 1–6)

### Preflight

- `/health` **200**, `/health/workers` **200**
- `ASAAS_PRODUCTION_ENABLED` **ON**; PIX OUT **OFF**
- `payout_worker` **stopped**

### Cobrança

| Campo | Valor |
|-------|-------|
| Script | `p14-go-live-pix-in-real.cjs` |
| `payment.id` | `pay_nguleet890a6t6h6` |
| Valor | R$ 5,00 |
| Conta | `85872488-9e4c-42df-8978-7f9ef9f5cb00` |
| `pagamentos_pix_id` | `2ce2a85b-16c0-4eb1-95ea-bc838d04abd5` |

### Webhooks capturados (logs Fly)

**`PAYMENT_CREATED`** — `2026-06-29T18:20:19.674Z`

```json
{
  "eventType": "PAYMENT_CREATED",
  "resourceId": "pay_nguleet890a6t6h6",
  "paymentStatus": "PENDING",
  "decision": "VALID_SIMULATED_EVENT"
}
```

**`PAYMENT_RECEIVED`** — `2026-06-29T18:27:27.185Z`

```json
{
  "eventType": "PAYMENT_RECEIVED",
  "resourceId": "pay_nguleet890a6t6h6",
  "paymentStatus": "RECEIVED"
}
```

Processamento:

- `deposit_claim_rpc_asaas_fallback` → `deposit_claim_credited_fallback`
- `user_id`: conta técnica
- `valor`: 5, `ledger_id`: `ce067996-01cb-40bf-a589-337a0a452d34`
- `deposit_webhook_claim` — `status: approved`, `provider: asaas`

### Validação read-only (P14 validate)

| Campo | Valor |
|-------|-------|
| `asaas_status` | `RECEIVED` |
| `pagamentos_pix_status` | `approved` |
| `saldo_antes` | R$ 166.550 |
| `saldo_atual` | R$ 166.555 |
| `saldo_delta` | **+R$ 5** |
| `ledger_count` | 1 (`deposito`) |
| **Veredito** | **PASS** |

### Fechamento gate webhook

- `ASAAS_PRODUCTION_ENABLED=false` — `18:37:16Z`
- `/health` **200**

---

## 4. PIX OUT Asaas (Fases 7–11)

### Preflight

| Item | Valor |
|------|-------|
| Candidato | `94480975-0051-4ede-968c-275abff07a19` |
| Status | `pendente` |
| Bruto / líquido | R$ 10 / **R$ 8** |
| `correlation_id` | `10d0f3af-7f00-4a93-83a8-bc491d8f2fc8` |
| Wallet pré-exec | R$ 8 |
| Ledger pré | `saque` + `taxa` |

**Nota:** único saque pendente disponível — legado usuário (`1116***3606`), não conta técnica.

### Janela aberta (Fase 8)

```
ASAAS_PRODUCTION_ENABLED=true
ASAAS_PIX_OUT_PRODUCTION_ENABLED=true
PAYMENT_ENGINE_PIXOUT_ENABLED=true
ASAAS_PIX_OUT_ENABLED=true
PAYOUT_PROVIDER=asaas
```

### Execução única (Fase 9)

| Campo | Valor |
|-------|-------|
| Fluxo | `approveAndSendWithdrawAdmin` → Asaas |
| Asaas API | `POST https://api.asaas.com/v3/transfers` |
| **HTTP Asaas** | **200** ✅ |
| `transfer_id` | `44a2a04e-f150-4fe9-966b-fbb4025ec17f` |
| Status Asaas | `PENDING` |
| Status saque | `aguardando_confirmacao` |
| `payout_external_reference` | `wd_ac7a8b47492b509ef0fcf088` |
| `invalid_action` | **Ausente** ✅ |
| **Veredito execute** | **PASS** |

### Snapshot pós-execução

| Campo | Valor |
|-------|-------|
| `asaas_transfer_id` | `44a2a04e-f150-4fe9-966b-fbb4025ec17f` |
| `asaas_transfer_status` | `PENDING` |
| `asaas_payout_raw.value` | 8 |
| `asaas_payout_raw.operationType` | `PIX` |
| Wallet | R$ 8 (débito já refletido na criação do saque) |
| Ledger | 2 entradas (`saque`, `taxa`) — sem duplicidade |
| Rollback | **Não acionado** |

---

## 5. Fechamento total (Fase 12)

| Secret | Digest final | Status |
|--------|--------------|--------|
| `ASAAS_PRODUCTION_ENABLED` | `fa61a138…` | **false** ✅ |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | `fa61a138…` | **false** ✅ |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | `fa61a138…` | **false** ✅ |
| `PAYOUT_PIX_ENABLED` | `fa61a138…` | **false** ✅ |
| `ENABLE_PIX_PAYOUT_WORKER` | `fa61a138…` | **false** ✅ |
| `PAYOUT_PROVIDER` | *(unset)* | ✅ |
| `ASAAS_PIX_OUT_ENABLED` | `d8c5ac2e…` | **true** ⚠️ |

Pós-fechamento:

- `/health` **200**
- `/health/workers` — `payoutWorker.enabledByEnv: **false**`

---

## 6. Ressalvas

| # | Ressalva | Severidade |
|---|----------|------------|
| 1 | Webhook `TRANSFER_*` não capturado nos logs — saque em `aguardando_confirmacao` | Média |
| 2 | Saque executado é legado de usuário, não conta técnica | Baixa |
| 3 | `ASAAS_PIX_OUT_ENABLED` não revertido no fechamento (fora da lista Fase 12) | Baixa |
| 4 | Validação painel Asaas (Transferências / saldo) — confirmar visualmente | Operacional |

---

## 7. Evidências (artefatos)

| Arquivo | Conteúdo |
|---------|----------|
| `tmp/p15master-fase1.txt` | Preflight inicial |
| `tmp/p15master-charge.out` | Criação cobrança |
| `tmp/p15master-validate.out` | Validação pós-pagamento |
| `tmp/p15master-logs-live.txt` | Stream logs webhook |
| `tmp/p15master-fase9-preflight.out` | Preflight PIX OUT |
| `tmp/p15master-fase9-execute.out` | Execução PIX OUT |
| `tmp/p15master-fase11-snapshot.out` | Auditoria final |
| `tmp/p15master-pipeline-state.json` | Estado pipeline |

---

## 8. Conclusão

A Indesconectável Payment Engine™ completou em produção controlada:

1. **PIX IN Asaas** — webhook real com gate ON, crédito idempotente via fallback RPC, wallet + ledger consistentes.
2. **PIX OUT Asaas** — transferência criada com sucesso (`HTTP 200`), erro histórico `invalid_action` / saldo insuficiente **superado**, rastreabilidade completa (`asaas_transfer_id`, payload raw, correlation_id).

A V1 do Gol de Ouro™ está **operacionalmente homologada** para o ciclo webhook + PIX OUT Asaas, ressalvando confirmação terminal via webhook de transferência.

---

*Pipeline: P1.5MASTER · Operador confirmou pagamento PIX `2026-06-29`*
