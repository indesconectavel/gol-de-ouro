# P1.6B — Homologação do Webhook de Autorização Asaas

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** HOMOLOGAÇÃO CONTROLADA — interrompida no preflight  
**App Fly:** `goldeouro-backend-v2`

---

## Veredito

# FAIL TÉCNICO

A homologação **não pôde ser executada**. O código P1.6A **não está deployado** em produção — a rota `/webhooks/asaas/transfer-validation` retorna **404 Rota não encontrada**. Sem deploy + configuração do painel Asaas, o fluxo server-to-server **não pode ser comprovado**.

**Bloqueador adicional (Fase 0):** painel Asaas não verificado nesta sessão — requer acesso manual a Integrações → Mecanismos de Segurança.

---

## 1. Resumo executivo

| Critério | Status |
|----------|--------|
| Fase 0 — Painel Asaas | ⏸️ Não verificado (sem acesso nesta sessão) |
| Fase 1 — URL no Asaas | ⏸️ Pendente |
| Fase 2 — Secrets Fly auth | ⏸️ Pendente |
| Fase 3 — Preflight | ❌ **BLOCKED_DEPLOY** |
| Dry-run local P1.6A | ✅ PASS |
| Health produção | ✅ HTTP 200, DB connected |
| Workers health | ✅ payout worker OFF |
| Rota auth em produção | ❌ **Não existe** |
| PIX OUT executado | ❌ Não (bloqueado) |
| Gates PIX OUT abertos | ❌ Não |
| Gates fechados pós-janela | ✅ N/A — janela não aberta |

---

## 2. Configuração Asaas (Fase 0–1)

### Esperado no painel

**Caminho:** Menu do usuário → Integrações → Mecanismos de Segurança → Validação de saque via Webhook

| Campo | Valor planejado |
|-------|-----------------|
| URL | `https://goldeouro-backend-v2.fly.dev/webhooks/asaas/transfer-validation` |
| E-mail erros | E-mail operacional da conta |
| Token | Gerar token forte (≠ webhook financeiro se possível) |
| Habilitar validação | Sim |

### Evidência Fase 0

**Não coletada.** Sem login no painel Asaas nesta execução.

**Ação obrigatória antes de retomar P1.6B:**

1. Confirmar que a opção existe e está habilitada.
2. Registrar print da tela de configuração.
3. Solicitar ao account manager desativação da **autorização crítica** (SMS), se ainda ativa.

Se a opção não existir → **FAIL OPERACIONAL** definitivo.

---

## 3. Configuração Fly (Fase 2)

### Não executado nesta sessão

Comandos planejados (após deploy):

```bash
fly secrets set ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN="<TOKEN_ASAAS>" -a goldeouro-backend-v2
fly secrets set ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true -a goldeouro-backend-v2
```

**Não configurado** — homologação bloqueada antes desta fase.

---

## 4. Preflight (Fase 3)

**Script:** `node scripts/p16b-preflight-homolog.mjs`  
**Execução:** 2026-06-29T21:09:32Z  
**Veredito:** `BLOCKED_DEPLOY`

### Health

```json
{
  "status": 200,
  "body": {
    "status": "ok",
    "database": "connected",
    "version": "1.2.1"
  }
}
```

### Workers

```json
{
  "payoutWorker": {
    "enabledByEnv": false,
    "payoutPixProcessingEnabled": false
  }
}
```

### Probe auth endpoint

```http
POST /webhooks/asaas/transfer-validation
→ HTTP 404

{
  "success": false,
  "message": "Rota não encontrada",
  "path": "/webhooks/asaas/transfer-validation"
}
```

**Interpretação:** release Fly atual **não contém** o código P1.6A. Esperado após deploy com gate OFF:

```json
{ "error": "asaas_transfer_auth_webhook_disabled" }
```

### Dry-run local

```bash
node scripts/test-asaas-transfer-auth-webhook.mjs
# PASS — exit_code 0 (9 cenários)
```

---

## 5. Saque controlado (Fase 4)

**Não preparado** — execução bloqueada no preflight.

Referência P1.5CERT: transferência `44a2a04e-f150-4fe9-966b-fbb4025ec17f` exigiu Token SMS manual.

---

## 6. Gates (Fases 5 e 10)

### Estado observado (remoto)

| Gate | Estado |
|------|--------|
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | Não deployado / desconhecido |
| `ASAAS_PRODUCTION_ENABLED` | OFF (workers) |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | OFF |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | OFF |
| `PAYOUT_PIX_ENABLED` | OFF |
| `ENABLE_PIX_PAYOUT_WORKER` | OFF |

### Janela PIX OUT

**Não aberta.** Nenhum gate de produção PIX OUT foi alterado nesta sessão.

### Fechamento (Fase 10)

**N/A** — gates permanecem no estado pré-homologação.

---

## 7. Execução única (Fase 7)

**Não executada.**

Motivo: pré-requisitos não atendidos (deploy + painel Asaas + secrets).

---

## 8. Chamada ao webhook de autorização

| Evento | Resultado |
|--------|-----------|
| Asaas → `/transfer-validation` | ❌ Não observado |
| Backend → `APPROVED` | ❌ Não observado |
| Token SMS | ❌ Não testado |

---

## 9. Resposta do backend

Probe externo sem token: **404 rota inexistente** (não é recusa de auth — é ausência de código deployado).

---

## 10. Resultado no Asaas

**N/A** — nenhuma transferência disparada nesta P1.6B.

---

## 11. Wallet / Ledger

**Não alterados.** Nenhuma operação financeira executada.

---

## 12. Webhook Transfer (`TRANSFER_*`)

**N/A**

---

## 13. Fechamento

Gates não foram abertos; nenhuma ação de fechamento necessária.

---

## 14. Ressalvas

1. **Código pronto localmente, produção desatualizada** — `fly deploy` necessário antes de qualquer homologação real.
2. **Painel Asaas não auditado** — Fase 0 pendente de evidência humana.
3. **Token SMS** — P1.5CERT provou que a conta ainda exige autorização crítica; mesmo com deploy, pode resultar em **PASS COM RESSALVAS** até o Asaas desabilitar SMS.
4. **P1.6A read-only** — módulo auth não grava DB; adequado para infra, homologação real depende de saque + transferência real.

---

## 15. Classificação dos cenários P1.6B

| Cenário | Aplicável? |
|---------|------------|
| A — PASS | ❌ |
| B — PASS COM RESSALVAS | ❌ |
| C — FAIL OPERACIONAL | ⚠️ Parcial (painel não verificado) |
| D — FAIL TÉCNICO | ✅ **Principal** — rota não deployada |

---

## 16. Próximos passos (ordem obrigatória)

### P1.6B.R1 — Deploy infra (sem abrir PIX OUT)

```bash
fly deploy -a goldeouro-backend-v2
```

Validar probe:

```bash
node scripts/p16b-preflight-homolog.mjs
# Esperado: auth_route_deployed=true (404 com asaas_transfer_auth_webhook_disabled)
```

### P1.6B.R2 — Configurar Asaas + Fly auth

1. Painel Asaas → URL + token
2. `fly secrets set ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN=...`
3. `fly secrets set ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true`
4. Re-probe com token válido → `REFUSED` (saque inexistente) confirma rota ativa

### P1.6B.R3 — Homologação real (1 PIX OUT)

1. Preflight PASS
2. Abrir gates PIX OUT (janela controlada)
3. `POST /api/admin/withdraw/approve-and-send` — 1 saque técnico valor mínimo
4. Capturar logs `[ASAAS][TRANSFER_AUTH]` + `TRANSFER_DONE`
5. Fechar todos os gates (Fase 10)

### P1.6B.R4 — Relatório final

Atualizar este documento com evidências de execução real.

---

## 17. Artefatos

| Artefato | Caminho |
|----------|---------|
| Preflight script | `scripts/p16b-preflight-homolog.mjs` |
| Dry-run P1.6A | `scripts/test-asaas-transfer-auth-webhook.mjs` |
| Módulo auth | `src/finance/webhooks/asaasTransferAuthorization.js` |
| Infra P1.6A | `docs/relatorios/P1.6A-INFRA-WEBHOOK-AUTHORIZATION-ASAAS.md` |
| Auditoria P1.6 | `docs/relatorios/P1.6-AUDITORIA-WEBHOOK-AUTHORIZATION.md` |

---

## 18. Conclusão

O **código está pronto** (P1.6A PASS local), mas a **conta/infra produção ainda não está preparada** para homologar o webhook de autorização:

1. Deploy Fly pendente.
2. Configuração painel Asaas pendente.
3. Possível liberação comercial/suporte Asaas para eliminar SMS.

**Veredito P1.6B: FAIL TÉCNICO** — homologação interrompida de forma segura, sem PIX OUT, sem gates abertos, sem alteração financeira.
