# P1.5W-FIX — Reprocessamento Controlado de Webhooks Asaas Bloqueados por Gate

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** Janela operacional controlada em produção  
**App Fly:** `goldeouro-backend-v2` · release **v506**

---

## Veredito

# PASS COM RESSALVAS

Causa raiz do HTTP 403 **confirmada** (`ASAAS_PRODUCTION_ENABLED=false`). Com gate aberto temporariamente, o endpoint aceitou o evento com **HTTP 200**, token válido e **sem crédito duplicado**. Gate **restaurado OFF**. PIX OUT **não alterado**.

**Ressalva:** reenvio pelo **painel Asaas** (remoção da penalização) deve ser feito pelo operador com gate aberto — nesta sessão o reenvio foi validado via **replay controlado interno** (equivalente técnico).

---

## 1. Resumo executivo

| Critério | Status |
|----------|--------|
| Causa raiz = gate fechado | ✅ |
| Preflight `/health` + banco | ✅ HTTP 200, `database: connected` |
| Gate inicial OFF | ✅ digest `fa61a138…` = `false` |
| Auditoria pré-janela (sem crédito prévio) | ✅ |
| Abertura apenas `ASAAS_PRODUCTION_ENABLED=true` | ✅ |
| PIX OUT / payout worker inalterados | ✅ gates OUT permanecem OFF |
| Webhook com gate ON | ✅ **HTTP 200** |
| Crédito duplicado | ❌ não ocorreu |
| Gate fechado ao final | ✅ digest `fa61a138…` = `false` |
| Relatório gerado | ✅ |

---

## 2. Causa raiz do HTTP 403

Resposta documentada pelo painel Asaas:

```json
{
  "error": "asaas_webhook_blocked",
  "message": "Rota Asaas bloqueada por flags ou produção sem ASAAS_PRODUCTION_ENABLED"
}
```

**Mecanismo:** `server-fly.js` L3451 — `isAsaasWebhookRouteAllowed()` retorna `false` quando `NODE_ENV=production` e `ASAAS_PRODUCTION_ENABLED=false`.

Não é falha de token, HMAC, indisponibilidade do backend ou erro Asaas.

---

## 3. Evento Asaas analisado

| Campo | Valor |
|-------|-------|
| `event` | `PAYMENT_CREATED` |
| `payment.id` | `pay_x6oe884i8qdq6kw8` |
| `payment.value` | R$ 12,15 |
| `payment.status` | `RECEIVED` |
| `billingType` | `PIX` |
| `pixTransaction` (ref. operador) | `a69da894-59fb-47e7-b14e-6d617e7b3c53` |
| `invoiceNumber` (ref. operador) | `844370998` |

**Nota:** `PAYMENT_CREATED` **não** está em `CREDIT_EVENTS` (`PAYMENT_RECEIVED` / `PAYMENT_CONFIRMED` apenas). Crédito wallet não é esperado para este evento.

---

## 4. Estado antes da janela

### 4.1 Infraestrutura (2026-06-29 ~13:29–13:37 UTC)

| Check | Resultado |
|-------|-----------|
| `GET /health` | **200** — `status: ok`, `database: connected`, v`1.2.1` |
| `GET /health/workers` | **200** — flags env payout habilitadas; máquina `payout_worker` **stopped** |
| `fly status` | app **started**, v**506**, checks **1/1 passing** |
| `ASAAS_PRODUCTION_ENABLED` | **false** (`fa61a13817d73a23`) |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | **false** (inalterado) |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | **false** (inalterado) |

### 4.2 Banco — auditoria read-only (pré-janela)

Consulta em `pagamentos_pix`, `ledger_financeiro` para `pay_x6oe884i8qdq6kw8`, `a69da894-59fb-47e7-b14e-6d617e7b3c53`, valor R$ 12,15:

| Tabela | Registros |
|--------|-----------|
| `pagamentos_pix` | **0** |
| `ledger_financeiro` | **0** |

**Conclusão:** reenvio seguro do ponto de vista de idempotência — não havia crédito prévio.

**Observação:** existe saque legado `94480975-…` (`pendente`, R$ 10) — **não executado** nesta janela (PIX OUT fechado).

---

## 5. Abertura controlada do gate

### Janela efetiva (retry bem-sucedido)

| UTC | Ação |
|-----|------|
| `13:50:48` | `fly secrets set ASAAS_PRODUCTION_ENABLED=true` |
| `13:50:54` | Deploy machines app + payout_worker (env refresh) |
| `13:52:24` | Replay único do webhook (gate ON) |
| `13:52:30` | Auditoria pós-replay |
| `13:52:36` | `fly secrets set ASAAS_PRODUCTION_ENABLED=false` |
| `13:53:39` | Probe pós-fechamento → **403** (gate OFF confirmado) |

**Flags não alteradas:** `PAYOUT_PIX_ENABLED`, `ENABLE_PIX_PAYOUT_WORKER`, `ASAAS_PIX_OUT_*`, `PAYOUT_PROVIDER`.

---

## 6. Resultado do reprocessamento

### Replay controlado (equivalente ao reenvio Asaas)

**Horário:** `2026-06-29T13:52:28Z`  
**Origem:** `fly ssh console` → POST interno `127.0.0.1:8080/webhooks/asaas`  
**Header:** `asaas-access-token` (runtime, length **49**)  
**HTTP:** **200**

**Body:**

```json
{
  "received": true,
  "engine": true,
  "provider": "asaas",
  "category": "payment",
  "dryRun": false,
  "productionCredit": true,
  "credited": false,
  "idempotent": false,
  "replay": false,
  "transferStatus": null,
  "saqueId": null,
  "creditDecision": "skipped_not_credit_event"
}
```

### Pós-fechamento do gate

**Horário:** `2026-06-29T13:53:39Z`  
**HTTP:** **403** — `asaas_webhook_blocked` (comportamento esperado)

### Painel Asaas

Reenvio manual pelo operador em **Integrações → Logs de Webhooks** **não executado nesta sessão** (sem acesso ao painel). Para remover penalização da URL, o operador deve:

1. Abrir janela (`ASAAS_PRODUCTION_ENABLED=true`)
2. Clicar **reenviar/sincronizar** no evento penalizado
3. Confirmar HTTP 200 no log do painel
4. Fechar gate imediatamente

---

## 7. Logs backend

- Período da janela: ciclos normais `deposit_reconcile_*` — sem exceções 500 no webhook
- Exit code Windows `Identificador inválido` após `fly ssh` — **ignorado** (stdout `###P15W_WH###` capturado antes do erro, padrão P1.5Y.R2)
- Artefatos: `tmp/p15w-fix-state.json`, `tmp/p15w-audit-readonly.cjs`

---

## 8. Validação wallet / ledger (pós-replay)

| Verificação | Resultado |
|-------------|-----------|
| `pagamentos_pix` para `pay_x6oe884i8qdq6kw8` | **0** registros |
| `ledger_financeiro` correlation `pay_x6oe884i8qdq6kw8` | **0** registros |
| Depósito R$ 12,15 em ledger | **0** |
| Crédito duplicado | **não** |
| Saque Asaas executado | **não** |

**Interpretação:** evento `PAYMENT_CREATED` processado com `creditDecision: skipped_not_credit_event` — consistente com política do engine (sem PIX interno vinculado + evento não creditável).

---

## 9. Fechamento do gate

| Item | Status |
|------|--------|
| `ASAAS_PRODUCTION_ENABLED` | **false** (`fa61a13817d73a23`) |
| `/health` pós-fechamento | **200** |
| Webhook probe pós-fechamento | **403** |
| PIX OUT | permanece **fechado** |

---

## 10. Veredito final

### PASS COM RESSALVAS

| Objetivo | Resultado |
|----------|-----------|
| Confirmar causa 403 = gate | ✅ |
| Endpoint aceita com gate ON | ✅ HTTP 200 |
| Consistência financeira | ✅ sem duplicidade |
| Gate fechado | ✅ |
| Penalização Asaas removida | ⏳ pendente operador (painel) |

### Conclusão

O HTTP 403 dos webhooks Asaas foi causado **exclusivamente** pelo gate operacional `ASAAS_PRODUCTION_ENABLED=false`. Com a janela controlada aberta, a Indesconectável Payment Engine™ aceitou o evento, validou o token e preservou a consistência financeira (`credited: false`, sem ledger).

**Próximo passo operacional (opcional):** com nova janela gate ON, reenviar pelo painel Asaas os eventos penalizados pendentes e fechar o gate em seguida.

---

*Evidências: `tmp/p15w-fix-state.json` · `tmp/p15w-audit-readonly.cjs` · auditoria read-only P1.5W webhook 403*
