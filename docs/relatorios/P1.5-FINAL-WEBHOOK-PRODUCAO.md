# P1.5 FINAL — Validação Operacional Definitiva do Webhook Asaas em Produção

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** Produção controlada — evidence first, read-only, zero regressão  
**App Fly:** `goldeouro-backend-v2` · release **v511**

---

## Veredito

# FAIL

Objetivo não atingido: **nenhum webhook Asaas novo** foi recebido após abertura do gate (`ASAAS_PRODUCTION_ENABLED=true` desde `2026-06-29T16:10:55Z`). Infraestrutura pronta; aguardando operador gerar cobrança PIX e pagar.

**Gate mantido ON** conforme instrução — não fechado nesta sessão.

---

## 1. Cronologia

| UTC | Etapa | Evento |
|-----|-------|--------|
| `16:10:55` | Pré-sessão | `ASAAS_PRODUCTION_ENABLED=true` aplicado (sessão P1.5FINAL-MAESTRIA) |
| `16:11:01` | Deploy | Máquina `app` v511 started |
| `16:35:47` | Etapa 1 | Preflight `/health` → **200** |
| `16:35:48` | Etapa 1 | Preflight `/health/workers` → **200** |
| `16:35:54` | Etapa 1 | `fly status` — app started, payout_worker **stopped** |
| `16:35:58` | Etapa 1 | Secrets confirmados — gate webhook ON, PIX OUT OFF |
| `16:36:08` | Etapa 2 | Stream `fly logs` iniciado → `tmp/p15final-webhook-logs-live.txt` |
| `16:36–16:40` | Etapa 2–3 | Logs: apenas ciclos `deposit_reconcile_*` (MP legado) — **zero** `/webhooks/asaas` |
| `16:40` | Etapa 3 | **Nenhum webhook novo** capturado |

---

## 2. Etapa 1 — Estado atual (evidência)

### `GET /health` — HTTP 200

```json
{
  "status": "ok",
  "timestamp": "2026-06-29T16:35:47.742Z",
  "version": "1.2.1",
  "database": "connected",
  "mercadoPago": "connected"
}
```

### `GET /health/workers` — HTTP 200

```json
{
  "success": true,
  "data": {
    "payoutWorker": {
      "enabledByEnv": true,
      "payoutPixProcessingEnabled": true
    }
  }
}
```

### `fly status`

| PROCESS | STATE | VERSION | CHECKS |
|---------|-------|---------|--------|
| `app` | **started** | 511 | 1/1 passing |
| `payout_worker` | **stopped** | 511 | — |

### Secrets (gates)

| Secret | Digest | Interpretação |
|--------|--------|---------------|
| `ASAAS_PRODUCTION_ENABLED` | `d8c5ac2e…` | **true** ✅ |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | `fa61a138…` | **false** ✅ |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | `fa61a138…` | **false** ✅ |
| `PAYOUT_PROVIDER` | *(ausente)* | **unset** ✅ |

**Etapa 1:** PASS — pré-requisitos atendidos; PIX OUT não aberto.

---

## 3. Etapa 2 — Logs em tempo real

**Comando:** `fly logs -a goldeouro-backend-v2` (stream ativo)

**Filtros aplicados na varredura:** `webhooks/asaas`, `PAYMENT_CREATED`, `PAYMENT_RECEIVED`, `PAYMENT_CONFIRMED`, `credited`, `idempotent`, `creditDecision`, `asaas_webhook`

**Resultado (janela pós-gate, ~16:11–16:40 UTC):**

- ❌ Nenhuma linha com `/webhooks/asaas`
- ❌ Nenhum `PAYMENT_CREATED` / `PAYMENT_RECEIVED` / `PAYMENT_CONFIRMED` Asaas
- ✅ Apenas `deposit_reconcile_*` (Mercado Pago legado) — sem interferência

**Artefato:** `tmp/p15final-webhook-logs-live.txt`, `tmp/p15final-webhook-etapa1.txt`

---

## 4. Etapa 3 — Novo webhook

| Campo | Valor |
|-------|-------|
| Webhook novo recebido | **NÃO** |
| `payment.id` | — |
| HTTP Asaas → backend | — |
| Body resposta | — |

**Causa:** operador ainda não gerou/pagou cobrança PIX após abertura do gate. Eventos penalizados históricos (403 pré-gate) **não reenviáveis** no painel — abordagem correta é webhook **novo**.

---

## 5. Etapa 4 — Auditoria read-only

**Não executada** — depende de webhook com HTTP 200 e `payment.id` conhecido.

Checklist pendente (pós-pagamento):

- [ ] `pagamentos_pix` — registro único
- [ ] `ledger_financeiro` — sem duplicidade
- [ ] `usuarios.saldo` — crédito correto (se `PAYMENT_RECEIVED`/`CONFIRMED`)
- [ ] Idempotência — replay não duplica
- [ ] Nenhum saque executado
- [ ] Nenhum rollback

**Conta técnica recomendada:** `85872488-9e4c-42df-8978-7f9ef9f5cb00`

---

## 6. Restrições respeitadas

| Restrição | Status |
|-----------|--------|
| Não abrir PIX OUT | ✅ |
| Não alterar `PAYMENT_ENGINE_PIXOUT_ENABLED` | ✅ |
| Não alterar `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | ✅ |
| Não modificar payout worker | ✅ (stopped) |
| Não alterar providers | ✅ |
| Não fechar `ASAAS_PRODUCTION_ENABLED` | ✅ (mantido ON) |

---

## 7. Próximo passo operacional

1. Operador: criar cobrança PIX Asaas (valor baixo, ex. R$ 5,00) na conta técnica.
2. Pagar o PIX.
3. Confirmar no painel Asaas: **Integrações → Logs de Webhooks** → HTTP **200**.
4. Informar `payment.id` e horário UTC.
5. Reexecutar Etapas 4–5 para veredito **PASS**.

**Stream de logs permanece recomendado** até confirmação.

---

## 8. Critérios de veredito

| Critério | Resultado |
|----------|-----------|
| Gate webhook ON, PIX OUT OFF | ✅ |
| Backend saudável | ✅ |
| Novo webhook HTTP 200 pós-gate | ❌ **não observado** |
| Processamento + idempotência | ⏳ pendente |
| Relatório gerado | ✅ |

---

*Evidências: `tmp/p15final-webhook-etapa1.txt` · stream `tmp/p15final-webhook-logs-live.txt` · preflight P1.5FINAL Etapa 1*
