# P1.6A.R1 — Deploy da Infraestrutura de Autorização Asaas

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** DEPLOY CONTROLADO — SEM EXECUÇÃO FINANCEIRA REAL  
**App Fly:** `goldeouro-backend-v2`

---

## Veredito

# PASS

Deploy concluído com sucesso. A rota `POST /webhooks/asaas/transfer-validation` **existe em produção** e responde com gate OFF de forma segura. Nenhum gate financeiro foi aberto. Nenhum PIX OUT executado.

---

## 1. Resumo executivo

| Critério | Status |
|----------|--------|
| Dry-run local P1.6A | ✅ PASS |
| Deploy Fly | ✅ Sucesso |
| `/health` pós-deploy | ✅ HTTP 200 |
| `/health/workers` | ✅ payout worker OFF |
| Rota auth deployada | ✅ `auth_route_deployed=true` |
| Gate auth OFF | ✅ `asaas_transfer_auth_webhook_disabled` |
| Gates PIX OUT OFF | ✅ Inalterados |
| PIX real | ❌ Não executado |
| Wallet/Ledger | ✅ Não alterados |

---

## 2. Estado antes do deploy

| Item | Valor |
|------|-------|
| Release / Image | `deployment-01KW8M5Y9Y2KFEXTD9S7TT70D6` |
| Machine version | **516** |
| Health | HTTP 200, DB connected |
| Workers | payout worker OFF |
| Probe auth | HTTP 404 — `Rota não encontrada` |
| Preflight | `BLOCKED_DEPLOY` |

---

## 3. Arquivos P1.6A presentes

| Arquivo | Status |
|---------|--------|
| `src/finance/webhooks/asaasTransferAuthorization.js` | ✅ |
| `src/finance/config/asaas-transfer-auth-config.js` | ✅ |
| `scripts/test-asaas-transfer-auth-webhook.mjs` | ✅ |
| `server-fly.js` (rota registrada L3481) | ✅ Modificado |

**Nota:** `git status` mostra alterações adicionais no working tree (fora do escopo P1.6A). O deploy Fly copia o diretório completo (`COPY . .` no Dockerfile). Nenhuma alteração de secrets/gates foi feita nesta etapa.

---

## 4. Testes dry-run (Etapa 1)

```bash
node scripts/test-asaas-transfer-auth-webhook.mjs
# PASS — exit_code 0 (9 cenários)
```

Execução: 2026-06-29T21:37:20Z

---

## 5. Deploy Fly (Etapa 3)

```bash
fly deploy -a goldeouro-backend-v2 --yes
# exit_code: 0
```

| Campo | Valor |
|-------|-------|
| Início | ~2026-06-29T21:41Z |
| Image anterior | `deployment-01KW8M5Y9Y2KFEXTD9S7TT70D6` |
| **Image nova** | `deployment-01KWANBA3GXS8A8923F8CSNQJY` |
| Machine version | **516 → 517** |
| Região | `gru` |
| App machine | `started`, 1/1 checks passing |
| Payout worker | `stopped` |

**Aviso deploy:** mensagem transitória *"app is not listening on the expected address"* durante rolling update — machine atingiu `good state` e health checks passaram.

Monitor: https://fly.io/apps/goldeouro-backend-v2/monitoring

---

## 6. Health pós-deploy (Etapa 4)

### `/health`

```json
{
  "status": "ok",
  "database": "connected",
  "mercadoPago": "connected",
  "version": "1.2.1"
}
```

HTTP **200**

### `/health/workers`

```json
{
  "payoutWorker": {
    "enabledByEnv": false,
    "payoutPixProcessingEnabled": false
  }
}
```

HTTP **200**

---

## 7. Probe da rota (Etapa 5)

```http
POST https://goldeouro-backend-v2.fly.dev/webhooks/asaas/transfer-validation
Content-Type: application/json

{"type":"TRANSFER","transfer":{"id":"p16b_preflight_probe","status":"PENDING","value":1}}
```

### Antes

```json
HTTP 404
{ "message": "Rota não encontrada" }
```

### Depois

```json
HTTP 404
{ "error": "asaas_transfer_auth_webhook_disabled" }
```

| Check | Resultado |
|-------|-----------|
| `auth_route_deployed` | ✅ **true** |
| `auth_route_missing` | ✅ **false** |
| Preflight veredito | ✅ `PREFLIGHT_PASS` |

A rota **existe** e está **bloqueada pelo gate** — comportamento esperado P1.6A.

---

## 8. Estado dos gates (Etapa 6)

| Gate | Estado pós-deploy |
|------|-------------------|
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | **ausente** (OFF) |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` | **ausente** |
| `ASAAS_PRODUCTION_ENABLED` | OFF (`fa61a138…`) |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | OFF |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | OFF |
| `PAYOUT_PIX_ENABLED` | OFF |
| `ENABLE_PIX_PAYOUT_WORKER` | OFF |
| `PAYOUT_PROVIDER` | **unset** |

Nenhum gate financeiro foi aberto nesta etapa.

---

## 9. Ressalvas

1. **Working tree amplo** — deploy incluiu código local completo, não apenas P1.6A; sem alteração de secrets.
2. **Warning rolling deploy** — resolvido; health final OK.
3. **Token auth ausente** — correto para esta fase; será configurado em P1.6B.R2.

---

## 10. Próximo passo — P1.6B.R2

1. **Painel Asaas** → Integrações → Mecanismos de Segurança:
   - URL: `https://goldeouro-backend-v2.fly.dev/webhooks/asaas/transfer-validation`
   - Token forte (dedicado)
   - Habilitar validação
2. **Fly secrets:**
   ```bash
   fly secrets set ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN="<TOKEN>" -a goldeouro-backend-v2
   fly secrets set ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true -a goldeouro-backend-v2
   ```
3. **Probe com token** → esperado `REFUSED` (saque inexistente), não 404 gate
4. **Homologação real** — 1 PIX OUT controlado (P1.6B janela)

---

## 11. Conclusão

| Veredito | **PASS** |
|----------|----------|
| Release nova | `517` / `deployment-01KWANBA3GXS8A8923F8CSNQJY` |
| Rota | Existe, gate OFF |
| Gates financeiros | OFF |
| Próximo | P1.6B.R2 — Asaas + Token Fly |
