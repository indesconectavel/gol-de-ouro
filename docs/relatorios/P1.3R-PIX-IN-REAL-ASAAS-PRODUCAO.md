# P1.3R — Reexecução PIX IN Real Asaas Produção

**Projeto:** Gol de Ouro™ Backend  
**Data:** 2026-06-28  
**Modo:** READ-WRITE controlado — cobrança real **autorizada** mas **não executada**  
**Gate Fly:** `ASAAS_PRODUCTION_ENABLED=false` — **mantido**  
**App Fly:** `goldeouro-backend-v2` · máquina `080e207b071048`

---

## Resumo executivo

| Etapa | Resultado |
|-------|-----------|
| 1 — Flags runtime Fly | **FAIL** parcial |
| 2 — Cobrança PIX real Asaas | **NÃO EXECUTADA** |
| 3 — Retorno payment/QR | **N/A** |
| 4 — Pagamento manual | **NÃO REALIZADO** |
| 5 — Webhook simulado | **403** (crédito bloqueado — esperado) |
| 6 — Logs / HTTP | **Registrados** |
| **Veredito P1.3R** | **FAIL** |

---

## Veredito

# FAIL

A reexecução **não pôde validar PIX IN real** porque `ASAAS_API_KEY` está **vazia no runtime Fly** e não há chave produção (`$aact_prod_…`) disponível no ambiente de execução. O código P1.3F permanece válido (readiness PASS em regressão local). Webhook em produção continua **protegido** (403 sem crédito).

---

## 1. Etapa 1 — Flags (runtime Fly)

**Fonte:** `fly ssh console` + `_p13r_fly_diag.cjs`  
**Timestamp:** 2026-06-28T19:17Z

| Flag | Esperado P1.3R | Observado Fly | OK |
|------|----------------|---------------|:--:|
| `ASAAS_ENABLED` | `true` | **ausente** (`null`) | ❌ |
| `ASAAS_ENV` | `production` | **ausente** (`null`) | ❌ |
| `ASAAS_PIX_IN_ENABLED` | `true` | **ausente** (`null`) | ❌ |
| `ASAAS_API_KEY` | não vazia | **length=0** | ❌ |
| `ASAAS_WEBHOOK_ENABLED` | `true` | `true` | ✅ |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | `true` | `true` | ✅ |
| `ASAAS_PRODUCTION_ENABLED` | `false` | `false` | ✅ |
| `ASAAS_WEBHOOK_TOKEN` | presente | length=**49** | ✅ |

### 1.1 Secrets Fly (vault)

**Timestamp:** 2026-06-28T19:16Z

| Secret | Na lista | Status |
|--------|:--------:|:------:|
| `ASAAS_API_KEY` | ✅ | Deployed (digest `49bb8a67…`) |
| `ASAAS_PRODUCTION_ENABLED` | ✅ | Deployed |
| `ASAAS_WEBHOOK_TOKEN` | ✅ | Deployed |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | ✅ | Deployed |

**Divergência vault × runtime:** secret existe no Fly vault, valor **vazio** no processo Node (reincidência P1.2 / P1.3).

### 1.2 Ambiente local

| Item | Observado |
|------|-----------|
| `.env` `ASAAS_API_KEY` | Sandbox (`$aact_hmlg_…`) — **não aplicável** a produção |
| Chave `$aact_prod_` | **Não disponível** nesta sessão |

---

## 2. Etapa 2 — Cobrança PIX real

### 2.1 Script de auditoria

Criado: `scripts/p13r-pix-in-real-asaas-production.mjs`

| Env | Função |
|-----|--------|
| `P13R_ALLOW_REAL_CHARGE=1` | Autoriza `POST` cobrança real API Asaas |
| `P13R_AMOUNT=5` | Valor (default R$ 5,00) |
| `P13R_PROBE_WEBHOOK=1` | Probe `POST /webhooks/asaas` no Fly |
| Gate no script | `ASAAS_PRODUCTION_ENABLED=true` **apenas no processo** — Fly **não alterado** |

### 2.2 Execução

**Timestamp:** 2026-06-28T19:21Z

```
###P13R_VERDICT###FAIL
###P13R_BLOCKERS###["ASAAS_API_KEY vazia","ASAAS_API_KEY não é produção ($aact_prod_)"]
```

**Cobrança real:** **não criada** — bloqueada antes de qualquer `POST /v3/payments`.

---

## 3. Etapa 3 — Retorno esperado (não obtido)

| Campo | Status |
|-------|--------|
| `payment_id` | **N/A** |
| `status` | **N/A** |
| QR Code | **N/A** |
| Pix Copia e Cola | **N/A** |
| `provider=asaas` | **N/A** |
| `ambiente=production` | **N/A** |

---

## 4. Etapa 4 — Pagamento manual

**Não realizado** — sem cobrança criada, não há QR para pagamento.

---

## 5. Etapa 5 — Webhook simulado (gate fechado)

**Probe:** `POST https://goldeouro-backend-v2.fly.dev/webhooks/asaas`  
**Timestamp:** 2026-06-28T19:21Z

| Campo | Valor |
|-------|-------|
| **HTTP Status** | `403` |
| **Body** | `{"error":"asaas_webhook_blocked","message":"Rota Asaas bloqueada por flags ou produção sem ASAAS_PRODUCTION_ENABLED"}` |
| **fly-request-id** | `01KW7TV36XBQ51F2HSHRDS81HE-gru` |

**Interpretação:** webhook **atinge** o backend; com `ASAAS_PRODUCTION_ENABLED=false` a rota é barrada **antes** do validator/crédito. Comportamento **correto** para pré-go-live.

Se uma cobrança fosse paga manualmente hoje, o Asaas enviaria webhook → **403** → **sem crédito wallet/ledger** até abertura do gate.

---

## 6. Etapa 6 — Regressão P1.3F

| Script | Resultado |
|--------|-----------|
| `verify-asaas-pix-in-production-readiness.mjs` | **PASS** (regressão local) |

Código HTTP PIX IN produção (P1.3F) **não regrediu**.

---

## 7. Matriz de conformidade

| Regra | Status |
|-------|:------:|
| `ASAAS_PRODUCTION_ENABLED` alterado no Fly | **Não** |
| Crédito wallet/ledger real | **Não** |
| PIX OUT | **Não** |
| Mercado Pago legado alterado | **Não** |
| Arquitetura alterada | **Não** |
| Cobrança real criada | **Não** (bloqueada) |

---

## 8. Bloqueadores para PASS

| # | Bloqueador | Responsável |
|---|------------|-------------|
| B1 | `ASAAS_API_KEY` vazia no runtime Fly | Operador Fly |
| B2 | `ASAAS_ENABLED`, `ASAAS_ENV`, `ASAAS_PIX_IN_ENABLED` ausentes no Fly | Operador Fly |
| B3 | Chave produção `$aact_prod_…` não disponível para auditoria | Operador |

---

## 9. Próximos passos (operador)

1. Re-setar `ASAAS_API_KEY` com chave produção válida:
   ```bash
   fly secrets set ASAAS_API_KEY='$aact_prod_…' -a goldeouro-backend-v2
   ```
2. Configurar flags pré-go-live (sem abrir gate):
   ```bash
   fly secrets set ASAAS_ENABLED=true ASAAS_ENV=production ASAAS_PIX_IN_ENABLED=true -a goldeouro-backend-v2
   ```
3. `fly apps restart goldeouro-backend-v2`
4. Reexecutar P1.3R:
   ```bash
   P13R_ALLOW_REAL_CHARGE=1 P13R_PROBE_WEBHOOK=1 P13R_AMOUNT=5 \
     ASAAS_API_KEY='…' ASAAS_ENABLED=true ASAAS_ENV=production \
     ASAAS_PIX_IN_ENABLED=true ASAAS_WEBHOOK_ENABLED=true \
     PAYMENT_WEBHOOK_ENGINE_ENABLED=true ASAAS_PRODUCTION_ENABLED=false \
     ASAAS_WEBHOOK_TOKEN='…' \
     node scripts/p13r-pix-in-real-asaas-production.mjs
   ```
5. Pagar PIX manualmente (opcional) → confirmar webhook 403 até go-live.
6. Após checklist: abrir gate (`ASAAS_PRODUCTION_ENABLED=true`) em janela controlada.

---

## 10. Conclusão

| Componente | Veredito |
|------------|----------|
| Proteção webhook (403, sem crédito) | **PASS** |
| Código P1.3F (readiness) | **PASS** |
| Flags Fly completas | **FAIL** |
| Cobrança PIX real produção | **FAIL** |
| **P1.3R global** | **FAIL** |

---

**Fim do relatório P1.3R — Reexecução PIX IN Real Asaas Produção**
