# P1.6W.DEPLOY — Relatório de Deploy Controlado

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-30  
**Modo:** DEPLOY CONTROLADO  
**Base:** P1.6W (PASS) · P1.6X (FAIL)

---

# Veredito

# PASS

Deploy **v532** publicado com sucesso. Webhook financeiro Asaas **desacoplado** dos gates PIX OUT em produção. Backend saudável. Gates permanecem fechados. Nenhuma operação financeira executada.

---

# Resumo Executivo

| Item | Resultado |
|------|-----------|
| Pré-deploy | **BLOCKED** (working tree suja) → mitigado com commit cirúrgico P1.6W |
| Commit | `6e24318c8aa950173aa54379a1940fa5c4bde0e4` |
| Release Fly | **v532** `complete` |
| Health | ✅ HTTP 200, `database: connected` |
| Webhook desacoplado | ✅ Ingresso sem HTTP 403 (probe → **401**, não `asaas_webhook_blocked`) |
| P1.6W em produção | ✅ Marker confirmado no container |
| Gates | ✅ Todos fechados (somente leitura) |
| Operação financeira | ❌ Nenhuma executada |

---

# Pré-deploy

## Auditoria

| Critério | Resultado |
|----------|-----------|
| Working tree limpa | ❌ Centenas de arquivos modificados/untracked |
| Branch | `chore/f2-4e-2-mp-log` |
| Arquivos P1.6W presentes | ✅ |
| Arquivos estranhos no commit | ❌ Não incluídos — commit cirúrgico |

## Resposta Etapa 1

**BLOCKED** (working tree) → **mitigado** com commit escopado:

| Arquivo | Ação |
|---------|------|
| `src/finance/webhooks/processPaymentWebhook.js` | commit |
| `server-fly.js` | commit |
| `src/finance/webhooks/processAsaasTransferWebhook.js` | commit (dependência) |
| `src/finance/compat/processPaymentWebhookCompat.js` | commit (dependência) |
| `scripts/verify-asaas-webhook-gate-decoupling-p16w.mjs` | commit (novo) |
| `scripts/verify-asaas-production-webhook-wire.mjs` | commit |
| `docs/relatorios/P1.6W-DESACOPLAMENTO-WEBHOOK-ASAAS.md` | commit |

## Testes locais pré-deploy

```bash
node scripts/verify-asaas-webhook-gate-decoupling-p16w.mjs   # PASS
node scripts/verify-asaas-production-webhook-wire.mjs        # PASS
```

---

# Deploy

| Campo | Valor |
|-------|-------|
| Comando | `fly deploy --build-arg GIT_COMMIT=6e24318… -a goldeouro-backend-v2` |
| Push | `origin/chore/f2-4e-2-mp-log` → `682088b..6e24318` |
| Image | `deployment-01KWCWYXS2574YZGYCANXH60G8` |
| Release | **v532** |
| Commit | `6e24318c8aa950173aa54379a1940fa5c4bde0e4` |
| Horário UTC | `2026-06-30T18:35:04Z` (machine updated) |
| Estratégia | Rolling update |
| Máquina app | `080e207b071048` → started, checks passing |
| payout_worker | `d894655b5114e8` → stopped (esperado) |

**Observação:** Fly emitiu warning transitório de listen address durante rollout; health checks `/health` passaram após conclusão.

---

# Runtime

| Item | Evidência |
|------|-----------|
| Estado app | **started** |
| Versão Fly | **532** |
| Checks | **1/1 passing** |
| Região | `gru` |
| P1.6W no container | `grep P1.6W` → `* P1.6W — Ingresso HTTP do webhook financeiro Asaas.` |

---

# Health

**URL:** `https://goldeouro-backend-v2.fly.dev/health`  
**Timestamp:** `2026-06-30T18:36:54Z`

```json
{
  "status": "ok",
  "timestamp": "2026-06-30T18:36:54.448Z",
  "version": "1.2.1",
  "database": "connected",
  "mercadoPago": "connected"
}
```

✅ HTTP 200 · database connected · runtime saudável

---

# Meta

**URL:** `https://goldeouro-backend-v2.fly.dev/meta`

```json
{
  "success": true,
  "data": {
    "version": "1.2.1",
    "build": "2025-10-21",
    "gitCommit": "6e24318c8aa950173aa54379a1940fa5c4bde0e4",
    "environment": "production"
  }
}
```

---

# Secrets (somente leitura)

Digest: `d8c5ac2e11c8e492` = **true** · `fa61a13817d73a23` = **false**

| Secret | Digest | Esperado | OK? |
|--------|--------|----------|-----|
| `ASAAS_WEBHOOK_ENABLED` | `d8c5ac2e11c8e492` | **true** | ✅ |
| `ASAAS_PRODUCTION_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `PAYOUT_PIX_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `PAYOUT_PROVIDER` | *(ausente)* | unset | ✅ |

Nenhum secret alterado nesta etapa.

---

# Webhook

## Objetivo estrutural

Confirmar que com `ASAAS_WEBHOOK_ENABLED=true` + `ASAAS_PRODUCTION_ENABLED=false` o webhook **não retorna HTTP 403** `asaas_webhook_blocked`.

## Probe (sem evento financeiro real)

**Request:**

```http
POST /webhooks/asaas
Content-Type: application/json

{"event":"PAYMENT_RECEIVED","payment":{"id":"p16wd_probe","status":"RECEIVED","value":1}}
```

**Response:**

| Campo | Valor |
|-------|-------|
| HTTP status | **401** |
| Body | `{ "error": "INVALID_PAYLOAD", "provider": "asaas" }` |

## Interpretação

| Antes P1.6W | Depois P1.6W |
|-------------|--------------|
| HTTP **403** `asaas_webhook_blocked` na rota | HTTP **401** na validação de payload/token |
| Bloqueio no gate de ingresso | Ingresso **permitido** — falha na camada de autenticação/validação (esperado sem token Asaas) |

✅ Webhook desacoplado ativo — **não há mais 403 por `ASAAS_PRODUCTION_ENABLED=false`**.

---

# Logs

**Comando:** `fly logs --no-tail` (últimas linhas)

| Verificação | Resultado |
|-------------|-----------|
| Exception / syntax error | ❌ Não observado |
| Module error / route error | ❌ Não observado |
| Startup | ✅ Reconcile cycles normais pós-deploy |
| Webhook probe | Processado na camada de validação (401) |

---

# Compatibilidade

| Área | Status |
|------|--------|
| Payment Engine | ✅ Intacta — sem alteração de payout/ledger |
| Provider Asaas | ✅ Intacto |
| Compat Layer | ✅ Atualizada apenas para propagação webhook |
| Auth administrativa | ✅ Não alterada |
| PIX OUT | ✅ Protegido — todos gates OFF |
| Webhook auth (`/transfer-validation`) | ✅ Gate OFF (404 esperado em repouso) |

---

# Evidências

| # | Evidência | Timestamp UTC |
|---|-----------|---------------|
| 1 | Commit `6e24318` pushed | `2026-06-30T18:33Z` |
| 2 | Release v532 complete | `2026-06-30T18:35:04Z` |
| 3 | `/health` 200 | `2026-06-30T18:36:54Z` |
| 4 | `/meta` gitCommit match | idem |
| 5 | Webhook probe 401 (não 403) | `2026-06-30T18:40:58Z` |
| 6 | `grep P1.6W` no container | confirmado |
| 7 | Secrets audit | gates corretos |

---

# Checklist

| Item | OK? |
|------|-----|
| Deploy publicado | ✅ v532 |
| Runtime saudável | ✅ |
| Webhook desacoplado | ✅ |
| Nenhuma regressão detectada | ✅ |
| Gates permanecem fechados | ✅ |
| Nenhuma operação financeira executada | ✅ |

## Bloqueios restantes (fora deste deploy)

Estes itens **não impedem PASS** de P1.6W.DEPLOY, mas bloqueiam P1.7 (homologação PIX OUT):

1. Conta Asaas — escalação `invalid_action` (P1.6G) sem resolução confirmada
2. Novo saque técnico pendente inexistente
3. Chave PIX ainda `validacao.goldeouro.e2e@gmail.com` (esperado `indesconectavel@gmail.com`)
4. `p16c-final-precheck.mjs` desatualizado (espera 403 no webhook)
5. `openGates()` não habilita `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED`

---

# Próximos Passos

1. **P1.6Z** — Certificação Final READ-ONLY (confirmar desacoplamento em prod + ambiente completo)
2. Resolução chamado Asaas (PIX OUT API)
3. Novo saque técnico + chave PIX atualizada
4. Atualizar precheck/homologador para P1.6W
5. **P1.7** — Homologação Final PIX OUT (somente após P1.6Z PASS + pré-requisitos)

---

*Pipeline: P1.6X (FAIL) → **P1.6W.DEPLOY (PASS)** → P1.6Z → P1.7*
