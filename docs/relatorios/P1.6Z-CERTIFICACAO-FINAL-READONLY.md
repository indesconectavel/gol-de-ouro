# P1.6Z — Certificação Final READ-ONLY Pós-Deploy

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-30  
**Modo:** READ-ONLY ABSOLUTO  
**Base:** P1.6W (PASS) · P1.6W.DEPLOY (PASS)

---

# Veredito

# FAIL

O deploy P1.6W foi **propagado corretamente** em produção (webhook desacoplado confirmado). O ambiente **não está apto** para executar P1.7 (homologação PIX OUT) sem resolver bloqueios remanescentes.

---

# Resumo Executivo

| Área | Resultado |
|------|-----------|
| Runtime Fly v532 | ✅ PASS |
| Meta / gitCommit P1.6W | ✅ PASS (`6e24318…`) |
| Secrets (gates fechados) | ✅ PASS |
| Webhook financeiro desacoplado | ✅ PASS (401, não 403) |
| Auth preflight | ✅ PASS |
| Payment Engine (sem regressão deploy) | ✅ PASS |
| Homologador / precheck P1.6W | ❌ **Desatualizado** |
| Conta Asaas PIX OUT | ❌ **`invalid_action` não resolvido** |
| Chave PIX | ❌ **BLOCKER** — ainda `validacao.goldeouro.e2e@gmail.com` |
| Saque técnico pendente | ❌ **BLOCKER** — `technicalPending: []` |
| P1.7 autorizada | ❌ **Não** |

---

# 1. Runtime Fly

| Item | Evidência |
|------|-----------|
| Release | **v532** `complete` |
| Image | `deployment-01KWCWYXS2574YZGYCANXH60G8` |
| Máquina app | `080e207b071048` — **started** |
| Versão Fly | **532** |
| Checks | **1/1 passing** |
| Região | `gru` |
| payout_worker | stopped (esperado, gates OFF) |
| `/health` | HTTP **200**, `database: connected`, v1.2.1 |

**Timestamp health:** `2026-06-30T18:59:41Z`

**Certificação:** ✅ **PASS**

---

# 2. Meta

```json
{
  "version": "1.2.1",
  "build": "2025-10-21",
  "gitCommit": "6e24318c8aa950173aa54379a1940fa5c4bde0e4",
  "environment": "production"
}
```

Commit corresponde ao deploy P1.6W (`fix(finance): desacoplar webhook Asaas dos gates PIX OUT`).

**Certificação:** ✅ **PASS**

---

# 3. Secrets (somente leitura)

Digest: `d8c5ac2e11c8e492` = **true** · `fa61a13817d73a23` = **false**

| Secret | Digest | Valor | OK? |
|--------|--------|-------|-----|
| `ASAAS_WEBHOOK_ENABLED` | `d8c5ac2e11c8e492` | **true** | ✅ |
| `ASAAS_PRODUCTION_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `PAYOUT_PIX_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | `fa61a13817d73a23` | **false** | ✅ |
| `PAYOUT_PROVIDER` | *(ausente)* | unset | ✅ |

Nenhum secret alterado nesta certificação.

**Certificação:** ✅ **PASS**

---

# 4. Webhook financeiro

## Probe controlado (sem evento financeiro real)

```http
POST /webhooks/asaas
{"event":"PAYMENT_RECEIVED","payment":{"id":"p16z_probe","status":"RECEIVED","value":1}}
```

| Campo | Valor |
|-------|-------|
| HTTP status | **401** |
| Body | `{ "error": "INVALID_PAYLOAD", "provider": "asaas" }` |
| `asaas_webhook_blocked` | ❌ **Ausente** |

Log Fly correlato:

```text
asaas_webhook_invalid_payload — decision: INVALID_PAYLOAD — ASAAS_WEBHOOK_MISSING_EVENT_ID
```

## Comparação P1.6X → P1.6Z

| Estado | Webhook com prod OFF |
|--------|----------------------|
| P1.6X (pré-deploy) | HTTP **403** `asaas_webhook_blocked` |
| P1.6Z (pós-deploy v532) | HTTP **401** (validação) — ingresso permitido |

Marker P1.6W no container: `* P1.6W — Ingresso HTTP do webhook financeiro Asaas.`

**Certificação:** ✅ **PASS** — webhook desacoplado ativo; nunca 403 por gate.

---

# 5. Webhook transferência (auth)

| Item | Estado |
|------|--------|
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | **false** (digest `fa61a138…`) |
| Token deployado | ✅ digest `4d518522…` |
| Rota `/webhooks/asaas/transfer-validation` | Configurada no código |
| Chamadas HTTP | **Não executadas** (READ-ONLY) |

Gate OFF em repouso — **correto e esperado**.

**Certificação:** ✅ **PASS** (configuração; inativo em repouso)

---

# 6. Payment Engine

| Componente | Status |
|------------|--------|
| Provider Asaas | ✅ Configurado (`ASAAS_ENABLED`, `ASAAS_API_KEY` deployed) |
| Compat layer | ✅ Intacta pós-deploy |
| PIX OUT gates | ✅ Fechados — payout bloqueado |
| Ledger / wallet | ✅ Não alterados nesta etapa |
| Regressão deploy | ❌ Não detectada (`/health` OK, logs normais) |

**Certificação:** ✅ **PASS**

---

# 7. Homologador (`scripts/p16c*`)

| Script | P1.6W reconhecido? | Observação |
|--------|-------------------|------------|
| `p16c-homolog-final.cjs` | ⚠️ Parcial | Auth preflight ✅ PASS; `openGates()` **não** habilita `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` |
| `p16c-final-precheck.mjs` | ❌ **Não** | Ainda espera HTTP **403** `asaas_webhook_blocked` (L96–101) |
| `p16c-prep-saque-tecnico.mjs` | ❌ | Default PIX: `validacao.goldeouro.e2e@gmail.com` |
| `p16c-retry-homolog.cjs` | ❌ | Idem |
| `p16c-create-withdraw-remote.cjs` | ❌ | Idem |

### Precheck desatualizado (evidência)

```96:101:scripts/p16c-final-precheck.mjs
  checks.asaas_webhook_production_gated =
    asaasWebhook.status === 403 &&
    (asaasWebhook.body?.error === 'asaas_webhook_blocked' ||
      String(asaasWebhook.body?.message || '').includes('ASAAS_PRODUCTION_ENABLED'));
  if (!checks.asaas_webhook_production_gated) {
    blockers.push('ASAAS_WEBHOOK_NOT_PRODUCTION_GATED');
```

Com P1.6W/código atual, este check **falharia** (probe retorna 401).

**Certificação homologador:** ❌ **FAIL** — requer atualização antes de P1.7

---

# 8. Conta Asaas

| Fonte | Achado |
|-------|--------|
| Logs Fly (buffer atual) | Apenas `asaas_webhook_invalid_payload` do probe P1.6Z — sem nova transferência |
| P1.6F / P1.6G (auditoria) | Último erro documentado: HTTP **400** **`invalid_action`** em `POST /v3/transfers` |
| P1.6F execução | `2026-06-30T15:34:24Z` — rollback ledger, `asaas_transfer_id: null` |
| Resolução suporte Asaas | **Não confirmada** |

**Último erro conhecido:** **`invalid_action`** (autorização crítica / conta Asaas — escalação P1.6G)

Nenhuma transferência executada nesta certificação.

**Certificação:** ❌ **FAIL** — conta não pronta para PIX OUT

---

# 9. Chave PIX

| Fonte | Chave | Tipo |
|-------|-------|------|
| **Esperado P1.7** | `indesconectavel@gmail.com` | `EMAIL` |
| Supabase saque `d0313dfe…` | `validacao.goldeouro.e2e@gmail.com` | `email` |
| Scripts homolog (defaults) | `validacao.goldeouro.e2e@gmail.com` | `email` |

**Certificação:** ❌ **BLOCKER**

---

# 10. Saque técnico

**Conta:** `85872488-9e4c-42df-8978-7f9ef9f5cb00`

| Campo | Valor |
|-------|-------|
| Saque `d0313dfe…` | status **`falhou`** (consumido P1.6F) |
| `technicalPending` | **`[]`** |
| Wallet | R$ 166.555,00 (saldo suficiente para novo saque) |

**Certificação:** ❌ **BLOCKER** — nenhum saque pendente elegível

---

# 11. Bloqueios restantes para P1.7

## Bloqueios técnicos

1. **`p16c-final-precheck.mjs`** espera HTTP 403 no webhook — incompatível com P1.6W
2. **`openGates()`** não habilita `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true` na janela de homologação
3. **Scripts de saque** defaultam chave PIX antiga (`validacao.goldeouro.e2e@gmail.com`)

## Bloqueios operacionais

4. **Sem saque técnico pendente** — criar novo antes de P1.7
5. **Chave PIX** não configurada como `indesconectavel@gmail.com`

## Bloqueios externos

6. **Conta Asaas** — `invalid_action` em `POST /v3/transfers` **sem resolução confirmada** (chamado P1.6H)
7. **Fila webhooks Asaas** — status incerto; penalização histórica por 403 pré-P1.6W (verificar painel)

---

# 12. Checklist

| Pergunta | Resposta |
|----------|----------|
| Backend saudável? | ✅ Sim |
| Webhook desacoplado? | ✅ Sim (401, não 403) |
| Deploy correto? | ✅ v532 / commit `6e24318` |
| Secrets corretos? | ✅ Sim (gates fechados) |
| Homologador atualizado? | ❌ Não |
| Precheck atualizado? | ❌ Não |
| Conta Asaas pronta? | ❌ Não (`invalid_action`) |
| Webhook auth pronto? | ⚠️ Token OK; gate OFF (homologador não abre) |
| Novo saque existe? | ❌ Não |
| Nova chave PIX configurada? | ❌ Não |
| **Pode executar P1.7?** | ❌ **Não** |

---

# Comparação P1.6X → P1.6Z

| Item | P1.6X | P1.6Z |
|------|-------|-------|
| Release | v531 | **v532** |
| P1.6W deployado | ❌ | ✅ |
| Webhook probe | 403 (esperado pré-deploy) | **401** |
| gitCommit meta | anterior | **6e24318** |
| Saque pendente | ❌ | ❌ |
| Chave PIX | antiga | antiga |
| Asaas invalid_action | aberto | aberto |
| Precheck P1.6W | desatualizado | desatualizado |

---

# Operações NÃO executadas

fly secrets set · fly deploy · git commit/push · approve-and-send · gates · saque · Supabase write · transferência Asaas · webhook reenvio financeiro

---

# Pré-requisitos P1.7

1. Resolução Asaas (`invalid_action` / PIX OUT API + auth webhook)
2. Atualizar `p16c-final-precheck.mjs` e `openGates()` para P1.6W
3. Criar saque técnico com `P16C_PIX_KEY=indesconectavel@gmail.com`
4. Verificar fila webhooks no painel Asaas pós-P1.6W
5. Reexecutar precheck atualizado → então P1.7

---

*Pipeline: P1.6W.DEPLOY (PASS) → **P1.6Z (FAIL)** → P1.7 (bloqueada)*
