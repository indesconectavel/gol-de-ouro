# P1.3G — Configuração Controlada dos Secrets Asaas Produção

**Projeto:** Gol de Ouro™ Backend  
**Data:** 2026-06-28  
**Modo:** READ-WRITE controlado — **sem PIX**, **sem cobrança**, **sem alteração de código**  
**App Fly:** `goldeouro-backend-v2`  
**Gate:** `ASAAS_PRODUCTION_ENABLED=false` — **mantido**

---

## Resumo executivo

| Etapa | Resultado |
|-------|-----------|
| Secrets flags configurados via `fly secrets set` | **PASS** |
| `ASAAS_PRODUCTION_ENABLED` permanece `false` | **PASS** |
| Runtime Fly — flags Asaas | **PASS** |
| Runtime Fly — `ASAAS_API_KEY` não vazia | **FAIL** |
| `verify-asaas-pix-in-production-readiness.mjs` | **PASS** |
| `p13r-pix-in-real-asaas-production.mjs` (sem cobrança) | **FAIL** |
| **Veredito P1.3G** | **FAIL** |

---

## Veredito

# FAIL

Todas as **flags** Asaas produção foram configuradas no Fly com sucesso. O **único bloqueador** restante é `ASAAS_API_KEY`: permanece **vazia no runtime** do processo Node. O operador deve executar `fly secrets set ASAAS_API_KEY=…` com a chave produção (`$aact_prod_…`) — **não fornecida nesta sessão**.

---

## 1. Ações executadas

### 1.1 Comando Fly (flags — sem chave API)

**Timestamp:** 2026-06-28T19:36Z

```bash
fly secrets set \
  ASAAS_ENABLED=true \
  ASAAS_ENV=production \
  ASAAS_PIX_IN_ENABLED=true \
  ASAAS_WEBHOOK_ENABLED=true \
  PAYMENT_WEBHOOK_ENGINE_ENABLED=true \
  ASAAS_PRODUCTION_ENABLED=false \
  -a goldeouro-backend-v2
```

**Resultado:** rolling deploy concluído — máquinas `080e207b071048` (app) e `784e047bd04e08` (payout_worker) atualizadas.

### 1.2 `ASAAS_API_KEY`

**Não configurada nesta sessão** — chave produção não disponível no ambiente do operador para o agente. Conforme política: **nenhum valor de chave registrado** em git, código, logs ou este relatório.

**Comando pendente (operador):**

```bash
fly secrets set ASAAS_API_KEY='<chave_producao_asaas>' -a goldeouro-backend-v2
```

Substituir `<chave_producao_asaas>` pelo valor `$aact_prod_…` do painel Asaas. Após o comando, o Fly reinicia as máquinas automaticamente.

---

## 2. Validação — secrets Fly (existência)

**Comando:** `fly secrets list -a goldeouro-backend-v2`  
**Timestamp:** 2026-06-28T19:40Z

| Secret | Na lista | Status Fly | Digest (parcial) |
|--------|:--------:|:----------:|------------------|
| `ASAAS_API_KEY` | ✅ | Deployed | `49bb8a67…` |
| `ASAAS_ENABLED` | ✅ | Deployed | `d8c5ac2e…` (`true`) |
| `ASAAS_ENV` | ✅ | Deployed | `a3311021…` (`production`) |
| `ASAAS_PIX_IN_ENABLED` | ✅ | Deployed | `d8c5ac2e…` (`true`) |
| `ASAAS_WEBHOOK_ENABLED` | ✅ | Deployed | `d8c5ac2e…` (`true`) |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | ✅ | Deployed | `d8c5ac2e…` (`true`) |
| `ASAAS_PRODUCTION_ENABLED` | ✅ | Deployed | `fa61a138…` (`false`) |
| `ASAAS_WEBHOOK_TOKEN` | ✅ | Deployed | `afb23cbc…` |

> Valores dos secrets **não exibidos** — apenas digest e metadados.

---

## 3. Validação — runtime Fly (máquina app)

**Fonte:** SSH read-only · `node /app/_p13g_fly_diag.cjs`  
**Timestamp:** 2026-06-28T19:42Z

| Variável | Observado | Esperado | OK |
|----------|-----------|----------|:--:|
| `NODE_ENV` | `production` | `production` | ✅ |
| `ASAAS_ENABLED` | `true` | `true` | ✅ |
| `ASAAS_ENV` | `production` | `production` | ✅ |
| `ASAAS_PIX_IN_ENABLED` | `true` | `true` | ✅ |
| `ASAAS_WEBHOOK_ENABLED` | `true` | `true` | ✅ |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | `true` | `true` | ✅ |
| `ASAAS_PRODUCTION_ENABLED` | `false` | `false` | ✅ |
| `ASAAS_WEBHOOK_TOKEN` | presente (length=49) | presente | ✅ |
| `ASAAS_API_KEY` **length** | **0** | **> 0** | ❌ |
| `ASAAS_API_KEY` **empty** | **true** | **false** | ❌ |
| `ASAAS_API_KEY` **is_prod** | **false** | `true` | ❌ |

### 3.1 Diagnóstico `ASAAS_API_KEY` (sem expor valor)

| Campo | Valor |
|-------|-------|
| `length` | **0** |
| `empty` | **true** |
| `sha256_prefix` | `null` |

**Interpretação:** divergência vault × runtime persiste — o nome do secret existe no Fly, mas o valor injetado no processo Node está vazio. **Re-set obrigatório** com chave produção válida.

---

## 4. Reexecução automática (READ ONLY — sem cobrança)

### 4.1 `verify-asaas-pix-in-production-readiness.mjs`

**Timestamp:** 2026-06-28T19:42Z  
**Resultado:** **PASS**

Código P1.3F íntegro — gates, fallback MP e validação de chave funcionam localmente.

### 4.2 `p13r-pix-in-real-asaas-production.mjs`

**Modo:** sem `P13R_ALLOW_REAL_CHARGE` (não cria cobrança)  
**Timestamp:** 2026-06-28T19:43Z

```
###P13R_VERDICT###FAIL
###P13R_BLOCKERS###["ASAAS_API_KEY vazia","ASAAS_API_KEY não é produção ($aact_prod_)"]
```

**Resultado:** **FAIL** — bloqueado antes de qualquer HTTP Asaas.

---

## 5. O que NÃO foi feito

| Item | Status |
|------|:------:|
| `ASAAS_PRODUCTION_ENABLED=true` | **Não** |
| Cobrança PIX real | **Não** |
| Pagamento PIX | **Não** |
| Alteração de código | **Não** |
| Alteração de banco | **Não** |
| Mercado Pago legado | **Não alterado** |
| Chave API em git/docs/logs | **Não** |

---

## 6. Bloqueador único para P1.3R

| ID | Bloqueador | Ação operador |
|----|------------|---------------|
| **B1** | `ASAAS_API_KEY` vazia no runtime Fly | `fly secrets set ASAAS_API_KEY='…' -a goldeouro-backend-v2` |

Após B1 resolvido, reexecutar:

```bash
# Validação runtime (length > 0, empty = false)
fly ssh console -a goldeouro-backend-v2 --machine 080e207b071048 -C "node -e \"const k=(process.env.ASAAS_API_KEY||'').trim(); console.log(JSON.stringify({len:k.length,empty:k.length===0,is_prod:k.startsWith('$aact_prod_')}))\""

# P1.3R dry-run (sem cobrança)
# (exportar ASAAS_API_KEY no shell local OU rodar p13r via Fly após B1)

# P1.3R com cobrança (quando autorizado)
# P13R_ALLOW_REAL_CHARGE=1 P13R_PROBE_WEBHOOK=1 node scripts/p13r-pix-in-real-asaas-production.mjs
```

---

## 7. Matriz final

| Configuração | Fly vault | Runtime Node | Impede PIX IN prod? |
|--------------|:---------:|:------------:|:-------------------:|
| `ASAAS_ENABLED=true` | ✅ | ✅ | — |
| `ASAAS_ENV=production` | ✅ | ✅ | — |
| `ASAAS_PIX_IN_ENABLED=true` | ✅ | ✅ | — |
| `ASAAS_WEBHOOK_ENABLED=true` | ✅ | ✅ | — |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED=true` | ✅ | ✅ | — |
| `ASAAS_PRODUCTION_ENABLED=false` | ✅ | ✅ | Gate fechado (intencional) |
| `ASAAS_API_KEY` produção | ⚠️ nome existe | ❌ **vazio** | **SIM** |

---

## 8. Conclusão

| Componente | Veredito |
|------------|----------|
| Configuração flags Fly | **PASS** |
| Gate financeiro fechado | **PASS** |
| `ASAAS_API_KEY` runtime | **FAIL** |
| Prontidão P1.3R | **FAIL** (bloqueada por B1) |
| **P1.3G global** | **FAIL** |

**Próximo passo:** operador define `ASAAS_API_KEY` produção via `flyctl secrets set` e reexecuta **P1.3R**.

---

**Fim do relatório P1.3G — Configuração Controlada dos Secrets Asaas Produção**
