# P1.6X — Certificação READ-ONLY Pré-Homologação Final

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-30  
**Modo:** READ-ONLY ABSOLUTO  
**Base:** P1.6W (PASS local) · P1.6F (FAIL) · P1.6G (ESCALAÇÃO ASAAS)

---

## Veredito

# FAIL

O ambiente **não está pronto** para P1.6Y. Infraestrutura, runtime e autenticação administrativa estão íntegros, mas existem **bloqueios técnicos conhecidos** que tornam uma nova homologação PIX OUT de alto risco de desperdício do saque técnico.

---

# 1. Infraestrutura

## Evidências coletadas (READ-ONLY)

| Item | Resultado | Timestamp UTC |
|------|---------|---------------|
| `/health` | **200** — `status: ok`, `database: connected`, `version: 1.2.1`, `mercadoPago: connected` | `2026-06-30T17:44:11Z` |
| `fly status` | App `080e207b071048` **started**, checks **1/1 passing**, região `gru` | `2026-06-30T17:41–17:45Z` |
| Release ativo | **v531** `complete` (~2h antes da auditoria) | deploy `2026-06-30T15:42:44Z` |
| `payout_worker` | `d894655b5114e8` **stopped** (esperado com gates OFF) | idem |
| DNS / hostname | `goldeouro-backend-v2.fly.dev` | — |
| Banco | `database: connected` via `/health` | — |

```json
{
  "status": "ok",
  "timestamp": "2026-06-30T17:44:11.317Z",
  "version": "1.2.1",
  "database": "connected",
  "mercadoPago": "connected"
}
```

**Conclusão:** Backend e runtime operacionais. Nenhuma anomalia de infra detectada.

---

# 2. Autenticação administrativa

**Comando executado (somente auth preflight):**

```powershell
P16C_ALLOW_REAL=1 P16C_SAQUE_ID=d0313dfe-… P16C_PHASE=auth-preflight node scripts/p16c-homolog-final.cjs
```

| Critério | Esperado | Resultado P1.6X |
|----------|----------|-----------------|
| JWT obtido automaticamente | Sim | ✅ `secretSource: fly_ssh` |
| fly ssh Windows-safe | Sim | ✅ Warning Windows ignorado; JWT extraído |
| Actor `tipo=admin` | Sim | ✅ `78fc62e7-334e-453f-ae8a-66047d765aec` |
| Probe HTTP | 200 | ✅ **200** |
| `probeOk` | true | ✅ **true** |
| Gates abertos | Não | ✅ Não |
| `approve-and-send` | Não | ✅ Não |

**Timestamp:** `2026-06-30T17:47:50Z`  
**Veredito auth:** **PASS**

---

# 3. Payment Engine

Auditoria estática de código (sem execução financeira):

| Componente | Estado | Evidência |
|------------|--------|-----------|
| Provider ativo (repouso) | Asaas configurado; `PAYOUT_PROVIDER` **unset** | `fly secrets list` — secret ausente |
| `FinanceProviderFactory` | Resolução via `getEffectiveProviderEnv('PAYOUT_PROVIDER')` | `FinanceProviderFactory.js` |
| `createPixWithdrawCompat` | Encadeia `resolvePayoutProvider()` → `AsaasPayoutProvider` | `createPixWithdrawCompat.js:15` |
| `AsaasProvider.createPixWithdraw` | POST via `createPixTransfer` + gate `pixOutProduction` | `AsaasProvider.js`, `asaas-http-client.js` |
| Compat layer webhook | `processPaymentWebhookCompat.js` propaga `isAsaasWebhookRouteAllowed` | código local P1.6W |
| Inconsistência estrutural local | **Nenhuma** detectada no código workspace | testes P1.6W PASS local |

**Observação:** Em janela de homologação, `openGates()` define `PAYOUT_PROVIDER=asaas` temporariamente (`p16c-homolog-final.cjs:111–121`). Em repouso, payout permanece bloqueado pelos gates.

**Conclusão:** Payment Engine **estruturalmente íntegra** no código; bloqueio operacional está nos gates (correto) e no provedor Asaas (P1.6G).

---

# 4. Webhook financeiro (P1.6W)

## Código local (P1.6W)

`isAsaasWebhookRouteAllowed()` em `processPaymentWebhook.js` depende **apenas** de `ASAAS_WEBHOOK_ENABLED` em produção:

```29:40:src/finance/webhooks/processPaymentWebhook.js
/**
 * P1.6W — Ingresso HTTP do webhook financeiro Asaas.
 * Depende apenas de ASAAS_WEBHOOK_ENABLED (+ ALLOW_ASAAS_SANDBOX_WEBHOOK em não-produção).
 * Não acopla ASAAS_PRODUCTION_ENABLED nem gates PIX OUT.
 */
function isAsaasWebhookRouteAllowed() {
  if (!isAsaasWebhookEnabled()) return false;
  if (isProductionRuntime()) {
    return true;
  }
  return isAsaasSandboxWebhookAllowed();
}
```

`blocked_prod` retorna HTTP **200 ignored** (não 403).

## Produção (runtime v531)

| Verificação | Resultado |
|-------------|-----------|
| Marker `P1.6W` no container | ❌ **`grep P1.6W` → exit 1** (string ausente) |
| Git workspace vs HEAD | `processPaymentWebhook.js` + `server-fly.js` com diff **não commitado** |
| Relatório P1.6W | Deploy Fly **não executado** |

## Comportamento esperado com gates atuais

| Secret | Valor (digest) | Efeito |
|--------|----------------|--------|
| `ASAAS_WEBHOOK_ENABLED` | **true** (`d8c5ac2e11c8e492`) | Gate ingresso ON |
| `ASAAS_PRODUCTION_ENABLED` | **false** (`fa61a13817d73a23`) | Crédito PIX IN OFF |

| Runtime | Comportamento webhook financeiro |
|---------|----------------------------------|
| **Produção atual (pré-P1.6W)** | HTTP **403** `asaas_webhook_blocked` — acoplamento legado a `ASAAS_PRODUCTION_ENABLED` |
| **Após deploy P1.6W** | HTTP **200** (processado ou `ignored`) — sem 403 por gate PIX OUT |

**Conclusão:** P1.6W **não implantado em produção**. O webhook financeiro **ainda pode retornar HTTP 403** com a configuração atual — **bloqueio ativo para P1.6Y**.

---

# 5. Webhook de autorização

Auditoria estática de `/webhooks/asaas/transfer-validation`:

| Item | Estado |
|------|--------|
| Rota | ✅ `server-fly.js:3481` |
| Gate | `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` → digest **`fa61a138…` = false** |
| Comportamento gate OFF | HTTP **404** `asaas_transfer_auth_webhook_disabled` |
| Token | `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` **deployed** (digest `4d518522…`) |
| Middleware / handler | `handleAsaasTransferAuthorization` + `asaas-transfer-auth-config.js` |
| Provider | Asaas dispara **após** criação de transferência `PENDING` |

**Chamadas HTTP:** **Não executadas** (restrição P1.6X).

**Conclusão:** Rota e token configurados; endpoint **inativo em repouso** (gate OFF — esperado). Na janela P1.6Y, `openGates()` **não habilita** este gate — homologador precisa incluir `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true` na abertura (gap conhecido desde P1.6F).

---

# 6. Gates (inventário Fly secrets)

Digest Fly: `d8c5ac2e11c8e492` = **`true`** · `fa61a13817d73a23` = **`false`**

| Secret | Digest | Valor inferido | Coerente? |
|--------|--------|----------------|-----------|
| `ASAAS_WEBHOOK_ENABLED` | `d8c5ac2e11c8e492` | **true** | ✅ Webhook ingresso habilitado |
| `ASAAS_PRODUCTION_ENABLED` | `fa61a13817d73a23` | **false** | ✅ Repouso seguro |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | `fa61a13817d73a23` | **false** | ✅ PIX OUT fechado |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | `fa61a13817d73a23` | **false** | ✅ Engine payout fechada |
| `PAYOUT_PIX_ENABLED` | `fa61a13817d73a23` | **false** | ✅ approve-and-send bloqueado |
| `PAYOUT_PROVIDER` | *(ausente)* | **unset** | ✅ Repouso |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | `fa61a13817d73a23` | **false** | ✅ Auth OFF em repouso |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | `d8c5ac2e11c8e492` | **true** | ✅ Engine webhook global ON |

**Janela aberta:** **Nenhuma** — todos os gates de payout e produção financeira estão fechados.

**Conclusão:** Gates em repouso **coerentes e seguros**. Nenhuma janela aberta detectada.

---

# 7. Conta Asaas (READ-ONLY)

| Item | Evidência |
|------|-----------|
| `ASAAS_API_KEY` | Digest `31a416b0a978b2d5` — **Deployed** (diferente do digest vazio histórico P1.3I `49bb8a67…`) |
| `ASAAS_ENABLED` | **true** |
| `ASAAS_ENV` | digest `a331102148f18977` (= `production`, mesmo de `NODE_ENV`) |
| `ASAAS_BASE_URL` | digest `3c6f47e61fcc7b9e` — produção (`https://api.asaas.com/v3`) |
| Provider | Asaas (Payment Engine) |
| Requisições financeiras | **Não executadas** |

**Bloqueio conhecido (P1.6G / P1.6H):** conta Asaas rejeita `POST /v3/transfers` com padrão **`invalid_action`** / autorização crítica — **não resolvido** nesta certificação. Escalado para suporte Asaas.

---

# 8. Chave PIX

| Fonte | `pixAddressKey` / `chave_pix` | `pixAddressKeyType` |
|-------|------------------------------|---------------------|
| **Esperado P1.6X** | `indesconectavel@gmail.com` | `EMAIL` |
| Saque `d0313dfe…` (Supabase) | `validacao.goldeouro.e2e@gmail.com` | `email` |
| Scripts homolog (`p16c-prepatch` defaults) | `validacao.goldeouro.e2e@gmail.com` | `email` |

**Busca no repositório:** `indesconectavel@gmail.com` **não aparece** como chave PIX em scripts de homologação (`p16c-prep-saque-tecnico.mjs`, `p16c-retry-homolog.cjs`, `p16c-create-withdraw-remote.cjs`).

**Conclusão:** Chave PIX **não atualizada**. A chave antiga `validacao.goldeouro.e2e@gmail.com` ainda é a única registrada no banco e nos defaults dos scripts.

---

# 9. Webhooks Asaas (fila / penalização)

| Aspecto | Estado |
|---------|--------|
| Painel Asaas | **Não auditado** (sem acesso API/dashboard nesta etapa) |
| Penalização histórica | P1.6F operou com `ASAAS_PRODUCTION_ENABLED=false` + código **pré-P1.6W** → Asaas recebeu **HTTP 403** no webhook financeiro |
| P1.6W em produção | **Não** — risco de penalização **persiste** até deploy |
| `p16c-final-precheck.mjs` | Ainda espera 403 `asaas_webhook_blocked` — **desatualizado** pós-P1.6W |

**Explicação técnica:** A documentação Asaas trata respostas ≠ 200 como falha de entrega. Respostas **403** repetidas durante homologações anteriores podem reduzir taxa de entrega ou pausar fila. Sem acesso ao painel, o status da fila permanece **incerto** — risco residual **ALTO** até deploy P1.6W + verificação no painel Asaas.

---

# 10. Novo saque técnico

**Conta técnica:** `85872488-9e4c-42df-8978-7f9ef9f5cb00`

## Histórico de saques (Supabase READ-ONLY)

| ID | Status | Amount / Net | Chave PIX | `asaas_transfer_id` | Criado |
|----|--------|--------------|-----------|----------------------|--------|
| `d0313dfe-b08a-4334-a3ca-d9e7011af38a` | **`falhou`** | 10 / 8 | `validacao.goldeouro.e2e@gmail.com` | `null` | 2026-06-30 |
| `baab82a8-2cdd-456a-bf14-bd815544b3bf` | `cancelado` | 5 / 3 | `validacao.goldeouro.e2e@gmail.com` | `null` | 2026-04-01 |

## Elegibilidade P1.6Y

| Critério | Resultado |
|----------|-----------|
| Saque pendente elegível | ❌ **`technicalPending: []`** |
| Nunca utilizado | ❌ `d0313dfe` consumido em P1.6F |
| Sem processamento anterior | ❌ ledger contém `falha_payout` + `rollback` |
| Wallet técnica | R$ 166.555,00 (saldo suficiente para novo saque) |

**Conclusão:** **Não existe** novo saque técnico pendente. P1.6Y exige **criação prévia** de saque com chave `indesconectavel@gmail.com` (fora do escopo READ-ONLY desta etapa).

---

# 11. Comparação P1.6F vs P1.6X

| Item | P1.6F | P1.6X | Mudou? |
|------|-------|-------|--------|
| **JWT / Auth preflight** | PASS (`probeOk: true`, fly_ssh) | PASS (idem, `17:47:50Z`) | ❌ Estável |
| **Fly SSH Windows** | PASS | PASS | ❌ Estável |
| **Gates repouso** | OFF (pós-fechamento) | OFF (todos digest `fa61a138…`) | ❌ Estável |
| **Webhook financeiro (prod)** | 403 com prod OFF | **Ainda 403** — P1.6W **não deployado** | ⚠️ Código local corrigido; **runtime inalterado** |
| **Webhook auth gate** | OFF na janela (bug homologador) | OFF em repouso | ❌ Gap homologador persiste |
| **Chave PIX** | `validacao.goldeouro.e2e@gmail.com` | **Mesma** — esperado `indesconectavel@gmail.com` | ❌ **Não atualizada** |
| **Provider** | Asaas (`PAYOUT_PROVIDER=asaas` na janela) | Asaas configurado; unset em repouso | ❌ Estável |
| **Runtime / release** | v528–529 era | **v531** complete | ✅ Deploy posterior (sem P1.6W) |
| **Saque técnico** | `d0313dfe` **pendente** | `d0313dfe` **`falhou`** | ✅ **Consumido** |
| **Payment Engine execução** | FAIL 502 `MP_TERMINAL_FAIL` | Não reexecutado | — |
| **Asaas conta** | `invalid_action` (escalação) | **Não resolvido** | ❌ Bloqueio raiz persiste |
| **P1.6W desacoplamento** | Não existia | PASS local; **FAIL prod** | ✅ **Novo** (pendente deploy) |

---

# 12. Checklist final

| Item | Pronto? | Notas |
|------|:-------:|-------|
| Backend | ✅ | `/health` 200, DB connected |
| Runtime | ✅ | v531 started, checks passing |
| Auth | ✅ | Auth preflight PASS |
| Payment Engine (código) | ✅ | Estrutura íntegra |
| PIX OUT | ❌ | Gates OFF (correto) + bloqueio Asaas não resolvido |
| Webhook financeiro | ❌ | P1.6W **não deployado** — 403 provável em prod |
| Webhook autorização | ⚠️ | Rota OK; gate OFF; homologador não abre gate |
| Gates corretos | ✅ | Repouso seguro, nenhuma janela aberta |
| Chave PIX correta | ❌ | Ainda `validacao.goldeouro.e2e@gmail.com` |
| Novo saque disponível | ❌ | `technicalPending: []` |

## Bloqueios conhecidos restantes

1. **P1.6W não deployado** — webhook financeiro continua sujeito a HTTP 403 com `ASAAS_PRODUCTION_ENABLED=false`.
2. **Conta Asaas** — escalação P1.6G (`invalid_action` / autorização crítica) **sem resolução confirmada**.
3. **Sem saque técnico pendente** — `d0313dfe` em `falhou`; nenhum substituto.
4. **Chave PIX desatualizada** — `indesconectavel@gmail.com` não configurada em DB nem scripts.
5. **Fila webhooks Asaas** — penalização por 403 históricos; status incerto sem painel.
6. **Gap homologador** — `openGates()` não habilita `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` (desde P1.6F).
7. **Precheck desatualizado** — `p16c-final-precheck.mjs` espera 403 no webhook (pré-P1.6W).

---

# 13. Critério de PASS

| Critério | Atendido? |
|----------|:---------:|
| Sem bloqueio técnico conhecido | ❌ |
| Ambiente íntegro | ⚠️ Infra sim; payout/webhook não |
| Autenticação íntegra | ✅ |
| Webhook íntegro | ❌ P1.6W ausente em prod |
| Payment Engine íntegra | ⚠️ Código sim; execução bloqueada Asaas |
| Chave PIX atualizada | ❌ |
| Sem risco estrutural restante | ❌ |

**Veredito final: FAIL**

---

# Pré-requisitos obrigatórios antes de P1.6Y

1. **Deploy Fly** com código P1.6W + confirmar `grep P1.6W` no container.
2. **Resolução Asaas** — chamado suporte (P1.6H) confirmando PIX OUT API + autorização webhook.
3. **Novo saque técnico** — `P16C_PIX_KEY=indesconectavel@gmail.com`, status `pendente`, nunca processado.
4. **Atualizar homologador** — `openGates()` incluir `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true`.
5. **Atualizar precheck** — `p16c-final-precheck.mjs` esperar HTTP 200 (não 403) no webhook financeiro.
6. **Verificar fila webhooks** no painel Asaas pós-deploy P1.6W.

---

# Artefatos

| Artefato | Caminho |
|----------|---------|
| Relatório P1.6X | `docs/relatorios/P1.6X-CERTIFICACAO-READONLY-PRE-HOMOLOGACAO.md` |
| Audit JSON | `tmp/p16c-audit.json` (atualizado READ-ONLY) |
| Auth preflight log | execução `p16c-homolog-final.cjs` `2026-06-30T17:47:50Z` |

---

# Operações NÃO executadas (conformidade)

- approve-and-send · POST `/v3/transfers` · abertura/fechamento de gates · alteração Fly Secrets · deploy · wallet/ledger writes · PIX · webhook POST (financeiro e auth) · Supabase INSERT/UPDATE/DELETE

---

*Pipeline: P1.6W (PASS local) → **P1.6X (FAIL)** → P1.6Y (bloqueada)*
