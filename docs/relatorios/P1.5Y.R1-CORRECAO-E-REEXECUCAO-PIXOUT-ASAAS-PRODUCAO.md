# P1.5Y.R1 — Correção pós-FAIL e Reexecução Controlada PIX OUT Asaas Produção

**Projeto:** Gol de Ouro™ V1  
**Data:** 2026-06-29  
**Modo:** PLANO CORRETIVO + HOMOLOGAÇÃO CONTROLADA  
**App Fly:** `goldeouro-backend-v2`  
**Etapa anterior:** P1.5Y (FAIL — schema P1.5D ausente)

---

## Veredito

# EM ANDAMENTO — PASS COM RESSALVAS (preflight)

Migration P1.5D **aplicada manualmente** e **validada live**. Preflight read-only **PASS** para saque alvo. **Gates não abertos.** Transferência real **pendente** (próxima fase: abrir janela + execute único).

**Ressalva:** `payout_worker` Fly está **stopped** — **não bloqueador** para execução P1.5Y via `approveAndSendWithdrawAdmin` (script/admin); webhooks processados pelo processo `app`.

**Regra P1.5Y.R1:** uma tentativa · parar imediatamente · não repetir sem plano corretivo.

---

## 1. Resumo Executivo

| Critério | Status |
|----------|--------|
| Runtime Fly saudável (`/health`, `/health/workers`) | ✅ |
| Gates PIX OUT OFF (antes de nova janela) | ✅ (inferido — health OK, sem execute) |
| `PAYOUT_PROVIDER` unset | ✅ (pós-correção P1.5Y) |
| Migration P1.5D aplicada em prod | ✅ **Manual SQL Editor** |
| Schema validado (`asaas_transfer_*`) | ✅ **4 colunas REST OK** |
| Deploy código P1.5G/I/Y | ✅ v491 + código local confirmado |
| Preflight P1.5Y (read-only) | ✅ **PASS** |
| `payout_worker` stopped | ⚠️ Não bloqueador (execução via admin/script) |
| Transferência HTTP `/transfers` | ❌ Não executada |
| Webhook TRANSFER live | ❌ N/A |
| Ledger/wallet pós-webhook | ❌ N/A |
| Replay idempotente live | ❌ N/A |
| Gates restaurados OFF pós-janela | ✅ (janela não aberta) |
| Mercado Pago / PIX IN | ✅ Inalterados |

---

## 2. Causa do FAIL Anterior (P1.5Y)

```text
column saques.asaas_transfer_id does not exist
```

A migration `database/migrations/20260628_p15d_asaas_payout_transfer_columns.sql` **não estava aplicada** no Supabase produção (`gayopagjdrkcmkirmfvy`) quando o preflight P1.5Y rodou com gates abertos.

**Consequência:** execução interrompida antes de `approveAndSendWithdrawAdmin`; gates fechados; `PAYOUT_PROVIDER=asaas` removido após crash loop do worker.

---

## 3. Correção Aplicada (nesta etapa)

| Artefato | Ação |
|----------|------|
| `scripts/apply-p15d-schema-production.cjs` | **Atualizado** — deriva URLs PG prod (`DATABASE_URL_PROD`, `SUPABASE_DB_PASSWORD`, pooler sa-east-1); idempotente; verifica 4 colunas pós-apply |
| `scripts/run-p15y-r1-ops.cjs` | **Criado** — orquestrador P1.5Y.R1 (health → schema → verify → audit → [janela opcional] → fechamento gates) |
| Migration forward P1.5D | **Inalterada** — somente aditiva (`ADD COLUMN IF NOT EXISTS`) |

**Apply manual (recomendado se script local falhar auth):**

1. Abrir [Supabase SQL Editor](https://supabase.com/dashboard/project/gayopagjdrkcmkirmfvy/sql/new)
2. Colar conteúdo de `database/migrations/20260628_p15d_asaas_payout_transfer_columns.sql`
3. Executar (sem rollback)

**Apply automático (requer credencial prod):**

```powershell
cd "E:\Chute de Ouro\goldeouro-backend"
$env:SUPABASE_DB_PASSWORD = "<senha-prod>"
node scripts/apply-p15d-schema-production.cjs
```

Ou via Fly (container tem `DATABASE_URL`):

```bash
fly ssh console -a goldeouro-backend-v2 -C "node /app/scripts/apply-p15d-schema-production.cjs"
```

---

## 4. Validação do Schema

### Query SQL esperada (read-only)

```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'saques'
  AND column_name IN (
    'asaas_transfer_id',
    'asaas_transfer_status',
    'asaas_payout_raw',
    'last_asaas_sync_at'
  )
ORDER BY column_name;
```

**Critério PASS:** 4 linhas — `TEXT`, `TEXT`, `jsonb`, `timestamp with time zone`.

### Script de verificação

```powershell
$env:DATABASE_URL = "<url-prod-gayopagjdrkcmkirmfvy>"
node scripts/verify-asaas-pix-out-schema-p15d.mjs
```

### Status nesta sessão

| Verificação | Resultado |
|-------------|-----------|
| Apply manual SQL Editor | ✅ Operador confirmou 4 colunas |
| REST Supabase (`select asaas_transfer_*`) | ✅ **200 OK** — colunas legíveis, valores `null` pré-transferência |
| Colunas confirmadas | `asaas_transfer_id`, `asaas_transfer_status`, `asaas_payout_raw`, `last_asaas_sync_at` |
| `verify-asaas-pix-out-schema-p15d.mjs` (estático) | ✅ Migration forward/rollback válidas no repositório |

---

## 5. Estado do Deploy

| Componente | Evidência |
|------------|-----------|
| App | `goldeouro-backend-v2.fly.dev` |
| Versão runtime | `1.2.1` |
| `/health` (2026-06-29T03:47:30Z) | `200 ok`, `database: connected`, `mercadoPago: connected`, `contadorChutes: 535` |
| `/health/workers` | `payoutWorker.enabledByEnv: true`, `payoutPixProcessingEnabled: true` |
| Deploy Fly | v491 — image `deployment-01KW8M5Y9Y2KFEXTD9S7TT70D6` (P1.5G/Y) |
| Máquina `app` | `080e207b071048` — started, 1/1 checks passing |
| Máquina `payout_worker` | `784e047bd04e08` — **stopped** (2026-06-29T02:55:19Z) |
| `processAsaasTransferWebhook.js` | ✅ Presente em `src/finance/webhooks/` |
| Ledger P1.5I (`payout_confirmado`) | ✅ Código presente |
| Scripts P1.5Y | ✅ `p15y-pix-out-production-live.cjs`, `verify-pix-out-production-live-p15y.mjs` |
| `pg` em dependencies prod | ✅ `package.json` ^8.21.0 |
| Scripts no Docker whitelist | ✅ `.dockerignore` inclui apply + p15y |

---

## 6. Estado dos Gates Antes

Confirmado via `fly secrets list` (2026-06-29):

| Flag | Valor |
|------|-------|
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | **OFF** (digest `fa61a138…` = false) |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | **OFF** (digest `fa61a138…` = false) |
| `ASAAS_PIX_OUT_ENABLED` | **unset** |
| `ASAAS_PRODUCTION_ENABLED` | **false** (digest `fa61a138…`) |
| `PAYOUT_PROVIDER` | **unset** |
| `PAYOUT_PIX_ENABLED` | `true` |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | `true` |
| `ASAAS_WEBHOOK_ENABLED` | `true` |
| `ASAAS_ENV` | `production` |

**Lição P1.5Y:** nunca manter `PAYOUT_PROVIDER=asaas` com `ASAAS_PRODUCTION_ENABLED=false` — crash loop do payout_worker.

---

## 7. Saque Controlado (preparado, não executado)

Candidatos identificados em audit P1.5Y (`tmp/p15y-live-state.json`):

| id | status | amount | net | pix (mascarado) | observação |
|----|--------|--------|-----|-----------------|------------|
| `a705bb8c-5f90-4532-9f55-d167dbdff672` | pendente | 10 | 8 | `0478***8664` | saque legado usuário |
| `94480975-0051-4ede-968c-275abff07a19` | pendente | 10 | 8 | `1116***3606` | saque legado usuário |

**Conta técnica P1.4:** `85872488-9e4c-42df-8978-7f9ef9f5cb00` — saldo alto, **sem saque pendente**.

**Recomendação para próxima janela:** criar saque R$ 10,00 na conta técnica com PIX operador autorizado, em vez de usar saque legado de usuário comum.

**Preflight alvo (se autorizado):** `a705bb8c-5f90-4532-9f55-d167dbdff672`

---

## 8. Preflight

**Executado read-only** (2026-06-29, gates **OFF**). Veredito: **PASS**.

| Campo | Valor |
|-------|-------|
| `saque_id` | `a705bb8c-5f90-4532-9f55-d167dbdff672` |
| `status` | `pendente` |
| `amount` / `net_amount` | R$ 10,00 / R$ 8,00 |
| `correlation_id` | `486f6bef-4d6f-42da-be56-62a28e55dd3d` |
| PIX (mascarado) | `0478***8664` |
| `walletSaldo` | `0` (débito já refletido — ledger `saque` + `taxa`) |
| `ledgerCount` / tipos | 2 — `saque`, `taxa` |
| `asaas_transfer_id` | `null` (esperado pré-HTTP) |

Evidência: `tmp/p15y-r1-preflight-result.json`

### `payout_worker` stopped — é bloqueador?

| Caminho | Depende do worker? | Status |
|---------|-------------------|--------|
| **P1.5Y execute** (`approveAndSendWithdrawAdmin`, `source: 'admin'`) | ❌ Não | Script chama HTTP Asaas diretamente |
| **Webhook TRANSFER** (`processAsaasTransferWebhook` no processo `app`) | ❌ Não | Rota HTTP do backend principal |
| **Fila automática** (`processPendingWithdrawals` no worker) | ✅ Sim | Worker **stopped** — não processa fila batch |

**Conclusão:** para a janela P1.5Y controlada (uma execução via `fly ssh` + script), **`payout_worker` stopped não é bloqueador**. Só seria relevante se a operação dependesse do worker pegar saques pendentes automaticamente.

---

## 8.1 Checklist final pré-janela (2026-06-29)

**Status:** gates **OFF** · transferência **NÃO executada** · aguardando autorização para abrir janela.

### Preflight read-only final — PASS

| Verificação | Resultado |
|-------------|-----------|
| Schema P1.5D (4 colunas REST) | ✅ |
| Saque existe + `pendente` | ✅ |
| Valor ≥ R$ 10 | ✅ |
| `asaas_transfer_id` vazio | ✅ |
| Ledger pré-HTTP (`saque` + `taxa`) | ✅ |

### Saque controlado selecionado

| Campo | Valor |
|-------|-------|
| **`saque_id`** | `a705bb8c-5f90-4532-9f55-d167dbdff672` |
| **Valor bruto / líquido / taxa** | R$ 10,00 / R$ 8,00 / R$ 2,00 |
| **PIX (mascarado)** | `0478***8664` |
| **`correlation_id`** | `486f6bef-4d6f-42da-be56-62a28e55dd3d` |
| **Tipo** | Saque legado de usuário (não conta técnica) |
| **Autorização** | ⚠️ Requer confirmação explícita do operador |

**Alternativa preferida (não disponível agora):** conta técnica `85872488-9e4c-42df-8978-7f9ef9f5cb00` — saldo R$ 166.550, **sem saque pendente**. Seria necessário criar saque R$ 10 antes da janela.

**Outro candidato legado:** `94480975-0051-4ede-968c-275abff07a19` (R$ 10 / PIX `1116***3606`).

### Wallet antes

| Campo | Valor |
|-------|-------|
| `walletSaldo` | **R$ 0,00** |
| Nota | Débito de R$ 12 (saque + taxa) já refletido no ledger |

### Ledger antes

| tipo | valor | referencia |
|------|-------|------------|
| `saque` | 10 | `a705bb8c-5f90-4532-9f55-d167dbdff672` |
| `taxa` | 2 | `a705bb8c-5f90-4532-9f55-d167dbdff672:fee` |

### `payout_worker` stopped

| Pergunta | Resposta |
|----------|----------|
| Bloqueia execução P1.5Y? | **Não** |
| Caminho de execução | `approveAndSendWithdrawAdmin` via script (`source: 'admin'`) em `fly ssh` |
| Webhook TRANSFER | Processo `app` (HTTP) — worker não necessário |
| Worker necessário apenas para | Fila automática batch de saques pendentes |

### Flags que serão abertas (janela)

```text
ASAAS_PIX_OUT_PRODUCTION_ENABLED=true
PAYMENT_ENGINE_PIXOUT_ENABLED=true
ASAAS_PIX_OUT_ENABLED=true
ASAAS_PRODUCTION_ENABLED=true
PAYOUT_PROVIDER=asaas
```

### Comando exato — abertura dos gates

```bash
fly secrets set \
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=true \
  PAYMENT_ENGINE_PIXOUT_ENABLED=true \
  ASAAS_PIX_OUT_ENABLED=true \
  ASAAS_PRODUCTION_ENABLED=true \
  PAYOUT_PROVIDER=asaas \
  -a goldeouro-backend-v2
```

### Comando exato — execução única (Fly, após gates ON)

```bash
fly ssh console -a goldeouro-backend-v2 -C "sh -c 'P15Y_PHASE=execute P15Y_ALLOW_REAL_PAYOUT=1 P15Y_SAQUE_ID=a705bb8c-5f90-4532-9f55-d167dbdff672 node /app/scripts/p15y-pix-out-production-live.cjs'"
```

### Comando exato — aguardar webhook

```bash
fly ssh console -a goldeouro-backend-v2 -C "sh -c 'P15Y_PHASE=wait P15Y_SAQUE_ID=a705bb8c-5f90-4532-9f55-d167dbdff672 node /app/scripts/p15y-pix-out-production-live.cjs'"
```

### Comando exato — fechamento dos gates (obrigatório)

```bash
fly secrets set \
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=false \
  PAYMENT_ENGINE_PIXOUT_ENABLED=false \
  ASAAS_PRODUCTION_ENABLED=false \
  -a goldeouro-backend-v2

fly secrets unset PAYOUT_PROVIDER ASAAS_PIX_OUT_ENABLED -a goldeouro-backend-v2
```

### Comando exato — verificação pós-janela (local, read-only)

```powershell
cd "E:\Chute de Ouro\goldeouro-backend"
$env:P15Y_SAQUE_ID = "a705bb8c-5f90-4532-9f55-d167dbdff672"
node scripts/verify-pix-out-production-live-p15y.mjs
```

Evidência preflight: `tmp/p15y-r1-preflight-result.json`

---

**Não executada.**

Comando único (Fly, gates ON):

```bash
fly ssh console -a goldeouro-backend-v2 -C "sh -c 'P15Y_PHASE=execute P15Y_ALLOW_REAL_PAYOUT=1 P15Y_SAQUE_ID=<uuid> node /app/scripts/p15y-pix-out-production-live.cjs'"
```

---

## 10. Evidências HTTP

**Nenhuma** — fluxo não atingiu POST `/v3/transfers`.

---

## 11. Evidências Webhook

**Nenhuma** — transferência não criada.

---

## 12. Evidências Banco

| Campo | Valor |
|-------|-------|
| Bloqueador histórico | `asaas_transfer_id does not exist` |
| Correção DDL | Pendente apply em prod |
| Estado `saques` alvo | `pendente` (pré-janela) |

---

## 13. Evidências Ledger

**N/A** — webhook terminal não recebido.

Mock P1.5I permanece válido.

---

## 14. Evidências Wallet

Audit P1.5Y: wallet dos candidatos **não alterada** (execução não ocorreu).

---

## 15. Idempotência

**N/A** em produção live.

Script `verify-pix-out-production-live-p15y.mjs` pronto para pós-janela.

---

## 16. Gates Restaurados

| Verificação | Resultado |
|-------------|-----------|
| Janela reaberta nesta etapa | ❌ Não |
| PIX OUT gates OFF | ✅ (preservado) |
| `PAYOUT_PROVIDER` unset | ✅ (preservado pós-P1.5Y) |
| `/health` pós-etapa | ✅ `200 ok` |

---

## 17. Riscos

| Risco | Severidade | Mitigação |
|-------|------------|-----------|
| Schema P1.5D ainda ausente em prod | **Crítica** | Apply manual SQL Editor antes de nova janela |
| `DATABASE_URL_PROD` ausente local | Alta | Usar SQL Editor ou Fly SSH apply |
| Saque legado de usuário real | Média | Preferir conta técnica `85872488-…` |
| Config inconsistente derruba worker | Alta | Sempre `unset PAYOUT_PROVIDER` ao fechar gates |
| Shell/automação local instável | Média | Usar `node scripts/run-p15y-r1-ops.cjs` em terminal nativo |

---

## 18. Próxima Etapa

### Sequência obrigatória (P1.5Y.R2 ou re-tentativa controlada)

```text
1. Apply migration P1.5D (SQL Editor ou apply script)
2. node scripts/verify-asaas-pix-out-schema-p15d.mjs  (4 colunas OK)
3. node scripts/run-p15y-r1-ops.cjs                     (pré-check)
4. P15Y_R1_EXECUTE=1 P15Y_SAQUE_ID=<uuid> node scripts/run-p15y-r1-ops.cjs
5. Confirmar gates OFF + verify read-only
6. Se PASS → P1.5Z (certificação final PIX OUT Asaas Produção)
```

### Comandos de abertura/fechamento gates

**Abrir (registrar horário):**

```bash
fly secrets set \
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=true \
  PAYMENT_ENGINE_PIXOUT_ENABLED=true \
  ASAAS_PIX_OUT_ENABLED=true \
  ASAAS_PRODUCTION_ENABLED=true \
  PAYOUT_PROVIDER=asaas \
  -a goldeouro-backend-v2
```

**Fechar (obrigatório):**

```bash
fly secrets set ASAAS_PIX_OUT_PRODUCTION_ENABLED=false PAYMENT_ENGINE_PIXOUT_ENABLED=false ASAAS_PRODUCTION_ENABLED=false -a goldeouro-backend-v2
fly secrets unset PAYOUT_PROVIDER ASAAS_PIX_OUT_ENABLED -a goldeouro-backend-v2
```

---

## 19. Veredito Final

| Critério PASS P1.5Y.R1 | Atendido |
|------------------------|----------|
| Migration P1.5D aplicada em prod | ❌ |
| Preflight passou | ❌ |
| Uma transferência real executada | ❌ |
| `transfer_id` Asaas | ❌ |
| Webhook processado | ❌ |
| `saques` atualizado | ❌ |
| Ledger coerente | ❌ |
| Wallet coerente | ❌ |
| Replay idempotente live | ❌ |
| Gates OFF restaurados | ✅ |
| Runtime saudável | ✅ |
| MP / PIX IN intactos | ✅ |
| Artefatos corretivos entregues | ✅ |

**Classificação:** **FAIL**

Homologação operacional **não concluída**. O bloqueador de schema permanece até apply confirmado em `gayopagjdrkcmkirmfvy`. **Não abrir nova janela** sem validação das 4 colunas.

---

## Artefatos entregues

| Artefato | Caminho |
|----------|---------|
| Apply schema P1.5D (melhorado) | `scripts/apply-p15d-schema-production.cjs` |
| Orquestrador P1.5Y.R1 | `scripts/run-p15y-r1-ops.cjs` |
| Migration forward | `database/migrations/20260628_p15d_asaas_payout_transfer_columns.sql` |
| Script operacional | `scripts/p15y-pix-out-production-live.cjs` |
| Verificação pós-janela | `scripts/verify-pix-out-production-live-p15y.mjs` |
| Relatório P1.5Y (FAIL anterior) | `docs/relatorios/P1.5Y-JANELA-OPERACIONAL-PIXOUT-ASAAS-PRODUCAO.md` |

---

*Pipeline: P1.5I → P1.5Y (FAIL) → **P1.5Y.R1 (FAIL)** → P1.5Z (bloqueado)*
