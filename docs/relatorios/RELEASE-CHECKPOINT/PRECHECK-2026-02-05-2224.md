# PRECHECK — Checkpoint PRE-V1-STABLE

**Data/Hora:** 2026-02-05 22:27 (geração do checkpoint)  
**Timestamp:** 2026-02-05-2224

---

## 1. Git

| Item | Valor |
|------|--------|
| **Commit** | `d8ceb3bb65a888069ab8f9d84e6561476b822568` |
| **Tag** | `PRE_V1_STABLE_2026-02-05-2224` |
| **Branch** | `release/v1-stability-ui` |
| **Mensagem último commit** | `chore: checkpoint pre-v1 stable` |

---

## 2. Resultado dos comandos locais

| Comando | Resultado |
|---------|-----------|
| **npm ci** (root) | Timeout; não concluído na execução automatizada. |
| **Backend build** | N/A — backend não possui script `build` no `package.json`. |
| **goldeouro-player: npm run build** | Iniciado com sucesso (prebuild + vite build); execução interrompida por timeout antes do fim. |
| **Lint/Test** | Não executados nesta sessão. |

---

## 3. Scripts relevantes (package.json)

### Root (goldeouro-backend)

```json
"scripts": {
  "start": "node server-fly.js",
  "dev": "nodemon server-fly.js",
  "test": "node test-supabase.js",
  "report": "node scripts/generate-integrity-report-pdf.cjs"
}
```

### goldeouro-player

```json
"scripts": {
  "dev": "vite",
  "prebuild": "node scripts/inject-build-info.cjs || echo 'Build info script skipped'",
  "build": "vite build",
  "preview": "vite preview",
  "test": "jest",
  "test:watch": "jest --watch",
  "test:coverage": "jest --coverage",
  "test:all": "npm run test",
  "health:check": "node scripts/network-smoke.js",
  "validate": "node validate-player-system.cjs",
  "rollback": "node rollback-player-to-approved.cjs",
  "status": "node -e \"...\"",
  "audit:pre-deploy": "cd ../mcp-system && node audit-simple.js",
  "deploy:safe": "npm run audit:pre-deploy && npm run build && npx vercel --prod",
  "audit:full": "cd ../mcp-system && node audit-simple.js"
}
```

---

## 4. Backup

- **Pasta do backup:** `backups\PRE-V1-STABLE\2026-02-05-2224\`
- **MANIFEST:** `backups\PRE-V1-STABLE\2026-02-05-2224\MANIFEST.txt` (contém data, commit, tag e lista de arquivos).
- **ZIP:** Não gerado — `Compress-Archive` falhou por caminho de arquivo excessivamente longo no Windows. Rollback deve usar **Git** (tag `PRE_V1_STABLE_2026-02-05-2224` ou branch `release/v1-stability-ui`).

---

## 5. Rollback rápido

```powershell
git checkout release/v1-stability-ui
# ou
git checkout PRE_V1_STABLE_2026-02-05-2224
```

---

*Relatório gerado no protocolo MAX SAFETY — checkpoint pre-v1 stable.*
