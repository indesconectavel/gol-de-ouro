# P1.5H — Verificação End-to-End Controlada PIX OUT Asaas

**Projeto:** Gol de Ouro™ V1  
**Data:** 2026-06-29  
**Modo:** VALIDAÇÃO CONTROLADA  
**Base:** P1.5A–P1.5G (infraestrutura completa)

---

## Veredito

# PASS COM RESSALVAS

Cadeia técnica PIX OUT Asaas percorrida integralmente em ambiente mockado. Provider, HTTP, persistência, webhook e idempotência **certificados**. Reconciliação ledger (`payout_confirmado` / rollback FAILED) **pendente P1.5I** — gap documentado, sem alteração estrutural nesta etapa.

---

## 1. Resumo Executivo

| Critério | Status |
|----------|--------|
| Cadeia E2E percorrida (mock) | ✅ |
| Factory → Asaas | ✅ |
| HTTP POST `/transfers` | ✅ (mock) |
| Persistência `asaas_*` | ✅ |
| Webhook TRANSFER_* | ✅ |
| 7 estados validados | ✅ |
| Idempotência webhook | ✅ |
| Wallet inalterado no webhook | ✅ |
| Ledger reconciliação | ⏳ P1.5I |
| PIX real / gates prod permanentes | ❌ Não executado |
| Regressões P1.5E–P1.5G (8 scripts) | ✅ PASS |
| Alterações estruturais | ❌ Nenhuma |

---

## 2. Fluxo Completo

```text
[1] Worker/Admin (certificado P1.5F)
        ↓
[2] createPixWithdrawCompat
        ↓
[3] FinanceProviderFactory → AsaasPayoutProvider
        ↓
[4] AsaasProvider.createPixWithdraw → POST /v3/transfers (mock)
        ↓
[5] payoutProviderPersistence → asaas_* em saques
        ↓
[6] status: aguardando_confirmacao
        ↓
[7] Webhook TRANSFER_* → processAsaasTransferWebhook
        ↓
[8] asaas_transfer_status + status saque atualizados
        ↓
[9] Reconciliação ledger → PENDENTE (ledgerPending: true)
```

---

## 3. Componentes Validados

| Componente | Etapa origem | P1.5H |
|------------|--------------|-------|
| Gates P1.5B | ✅ | Simulados (mock key) |
| HTTP P1.5C | ✅ | Mock fetch |
| Schema P1.5D | ✅ | Patch validado |
| Provider-aware P1.5E | ✅ | Factory asaas |
| Factory unificada P1.5F | ✅ | Compat layer |
| Webhook TRANSFER P1.5G | ✅ | State machine |
| Ledger MP (referência) | server-fly | Padrão existe |
| Ledger Asaas | — | **Não wired** |

---

## 4. Cenários Executados

Script: `scripts/verify-pix-out-end-to-end-p15h.mjs`

| Step | Cenário | Resultado |
|------|---------|-----------|
| 1 | Factory resolve asaas | ✅ |
| 2 | HTTP mock POST /transfers | ✅ |
| 3 | Persistência asaas_* | ✅ |
| 4 | State machine (7 estados) | ✅ |
| 5 | Happy path PENDING→DONE | ✅ |
| 6 | Webhook replay idempotente | ✅ |
| 7 | HTTP replay audit | ✅ (documentado) |
| Audit | Ledger gap + mock key | ✅ |
| Regressões | 8 scripts | ✅ |

---

## 5. Estados Testados

| Estado / Evento | `asaas_transfer_status` | Status saque |
|-----------------|-------------------------|--------------|
| CREATED (`TRANSFER_CREATED`) | PENDING | aguardando_confirmacao |
| PENDING | PENDING | aguardando_confirmacao |
| BANK_PROCESSING | BANK_PROCESSING | aguardando_confirmacao |
| DONE | DONE | processado |
| FAILED | FAILED | falhou |
| CANCELLED | CANCELLED | falhou |
| BLOCKED | BLOCKED | falhou |

---

## 6. Idempotência

| Camada | Comportamento | P1.5H |
|--------|---------------|-------|
| Webhook replay (mesmo status) | `idempotent: true`, `replay: true` | ✅ |
| Persistência replay | Sem duplicidade de patch | ✅ |
| HTTP replay (compat) | 2 chamadas ao mock — idempotência **provider/webhook** | Documentado |
| Ledger | Nenhuma entrada duplicada (0 writes) | ✅ |
| Wallet | Saldo inalterado em webhooks | ✅ |

---

## 7. Regressões

Todos **PASS**:

```text
verify-payout-factory-unification-p15f.mjs
verify-payout-provider-aware-p15e.mjs
verify-asaas-transfer-webhook-p15g.mjs
verify-asaas-pix-out-production-gates.mjs
verify-asaas-pix-out-production-readiness.mjs
verify-asaas-pix-out-schema-p15d.mjs
verify-asaas-provider.mjs
verify-payment-webhook-engine.mjs
```

---

## 8. Evidências

```
Verification PASSED (P1.5H E2E controlled — ledger reconciliation pending P1.5I)
OK e2e step 1: factory resolves asaas
OK e2e step 2: HTTP mock POST /transfers via compat
OK e2e step 3: payout persistence patch (asaas_*)
OK e2e step 4: full webhook state machine
OK e2e step 5: happy path PENDING → BANK_PROCESSING → DONE
OK e2e step 6: webhook replay idempotent
OK e2e step 7: HTTP replay audit (provider-level)
OK regression: (8/8 scripts)
```

Log produção simulada:

```json
{"event":"asaas_production_pix_withdraw_http_sent","transferId":"trf_p15h_e2e_001","status":"PENDING"}
{"event":"asaas_transfer_webhook_applied","transferStatus":"DONE","saqueStatus":"processado"}
```

---

## 9. Pendências (P1.5I)

| Pendência | Referência MP | Ação futura |
|-----------|---------------|-------------|
| `ledger_financeiro` tipo `payout_confirmado` em DONE | server-fly L3651 | Wire em P1.5I |
| `ledger_financeiro` tipo `falha_payout` + rollback em FAILED | server-fly L3710 | Wire em P1.5I |
| Idempotência ledger (correlation_id + referencia) | MP webhook L3602 | Replicar padrão Asaas |
| Homologação transferência real | — | Gate operacional P1.5I |
| HTTP idempotência provider-side | externalReference unique | Monitorar em prod |

Flag código: `ledgerPending: true` em `processAsaasTransferWebhook.js`.

---

## 10. Riscos

| Risco | Severidade | Mitigação |
|-------|------------|-----------|
| Ledger Asaas ausente vs MP | Média | P1.5I antes de homologação real |
| HTTP replay duplica transfer no provider | Média | externalReference + monitoramento |
| Gates abertos acidentalmente em prod | Alta | Fly gates OFF mantidos |
| Wallet débito duplicado | Baixa | Débito no request, não no webhook |

---

## 11. Rollback

Etapa **somente validação** — nenhum código de produção alterado.

- Remover `scripts/verify-pix-out-end-to-end-p15h.mjs` se necessário
- Infraestrutura P1.5A–G permanece intacta

---

## 12. Próxima Etapa

### P1.5I — Homologação Operacional Controlada

- Wire ledger Asaas espelhando MP webhook
- **Primeira** transferência real autorizada por gates operacionais
- Evidências financeiras read-only pós-transferência

---

## 13. Veredito Final

| Critério de aceitação | Atendido |
|----------------------|----------|
| Cadeia percorrida | ✅ |
| Provider resolve | ✅ |
| Webhook atualiza | ✅ |
| Persistência consistente | ✅ |
| Replay idempotente (webhook) | ✅ |
| Nenhuma regressão | ✅ |
| Nenhum PIX real | ✅ |
| Reconciliação ledger completa | ⏳ P1.5I |

**Classificação:** **PASS COM RESSALVAS**

Infraestrutura PIX OUT Asaas **certificada tecnicamente** para autorizar planejamento da homologação operacional P1.5I.

---

*Pipeline: P1.5A → … → P1.5G → **P1.5H** → P1.5I*
