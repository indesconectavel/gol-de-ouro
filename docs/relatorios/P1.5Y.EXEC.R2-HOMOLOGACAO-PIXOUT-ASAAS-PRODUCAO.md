# P1.5Y.EXEC.R2 — Homologação PIX OUT Asaas Produção (Execução Manual)

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** Produção Controlada — manual fase a fase  
**Base:** P1.5Y.EXEC.R1 FAIL (exit code Windows após preflight PASS)

---

## Veredito

# FAIL

Integração **atingiu o Asaas produção** (`POST https://api.asaas.com/v3/transfers`), porém a API retornou **HTTP 400** com `errorCode: invalid_action`. **Nenhum `transfer_id`** foi criado. Saque marcado **`falhou`** com rollback de wallet. Gates **fechados**.

**Não classificado como saldo insuficiente** — resposta oficial foi `invalid_action`, não mensagem de saldo.

**P1.5Z:** bloqueada.

---

## 1. Resumo Executivo

| Critério | Status |
|----------|--------|
| Pré-check local | ✅ |
| Abertura gates (v504) | ✅ |
| Espera runtime 95s | ✅ app started, 1/1 passing, health 200 |
| Preflight remoto (stdout) | ✅ **PASS** |
| Execução HTTP Asaas | ✅ request enviado |
| Resposta Asaas | ❌ **400 invalid_action** |
| `transfer_id` | ❌ null |
| Webhook TRANSFER | ❌ N/A |
| Rollback wallet/saque | ✅ `falhou`, wallet restaurada |
| Gates fechados | ✅ |
| `/health` pós-janela | ✅ 200 |

---

## 2. Objetivo R2

Contornar bloqueio Windows/`fly ssh` validando fases pelo **stdout** (`###P15Y_VERDICT`), não exit code.

**Orquestrador:** não utilizado ✅

---

## 3. Timeline

| UTC | Fase | Evento |
|-----|------|--------|
| 05:17:55 | 1 | Pré-check OK — saque `pendente`, schema OK, gates OFF |
| 05:21:28 | 2 | Gates abertos (release v504) |
| 05:24:47 | 3 | Após 95s — app started, checks passing, health 200 |
| 05:26:xx | 4 | Preflight stdout **PASS** (exit Windows 1 ignorado) |
| 05:28:26 | 5 | Execute — POST `/transfers` → **400 invalid_action** |
| 05:28:27 | 5 | Rollback — saque `falhou`, wallet +R$10 |
| 05:29:xx | 8 | Gates fechados |
| 05:30:xx | 9 | verify-p15y — FAIL (sem transferência live) |

---

## 4. Fase 1 — Pré-check

| Item | Valor |
|------|-------|
| `/health` | 200, database connected |
| Schema P1.5D | 4 colunas OK |
| Gates | OFF (`fa61a138…` = false) |
| `PAYOUT_PROVIDER` | unset |
| `saque_id` | `a705bb8c-5f90-4532-9f55-d167dbdff672` |
| Status | `pendente` |
| `asaas_transfer_id` | null |
| Valor bruto / líquido | R$ 10 / R$ 8 |
| Wallet antes | R$ 0 |
| Ledger antes | `saque` 10 + `taxa` 2 |

---

## 5. Fase 2–3 — Gates + Espera

**Abertura:**

```bash
fly secrets set ASAAS_PIX_OUT_PRODUCTION_ENABLED=true PAYMENT_ENGINE_PIXOUT_ENABLED=true ASAAS_PIX_OUT_ENABLED=true ASAAS_PRODUCTION_ENABLED=true PAYOUT_PROVIDER=asaas -a goldeouro-backend-v2
```

**Runtime pós-95s:**

```text
app 080e207b071048 │ v504 │ started │ 1 total, 1 passing
/health → 200, database connected
```

---

## 6. Fase 4 — Preflight

**Stdout:**

```text
###P15Y_VERDICT###{"phase":"preflight","result":"PASS"}
```

Exit code Windows: 1 (`Identificador inválido`) — **ignorado** conforme R2.

Evidência: `tmp/p15y-exec-r2/04-preflight.out`

---

## 7. Fase 5 — Execução (Caso C)

**Fluxo confirmado:**

```text
approveAndSendWithdrawAdmin (source: admin)
  → createPixWithdrawCompat
  → FinanceProviderFactory → asaas
  → POST https://api.asaas.com/v3/transfers
```

**Request (mascarado):**

| Campo | Valor |
|-------|-------|
| `value` | 8 |
| `pixAddressKeyType` | CPF |
| `pixKeyPreview` | `0478...664` |
| `externalReference` | `wd_67786aba15ef420083118834` |
| `httpGate` | `pixOutProduction` |

**Response Asaas:**

| Campo | Valor |
|-------|-------|
| HTTP | **400** |
| `errorCode` | **`invalid_action`** |
| `transfer_id` | **null** |

**Stdout veredito:**

```text
###P15Y_VERDICT###{"phase":"execute","result":"FAIL"}
```

**Pós-execução:**

| Campo | Valor |
|-------|-------|
| `saque.status` | **`falhou`** (rollback) |
| `asaas_transfer_id` | null |
| `last_asaas_sync_at` | 2026-06-29T05:28:26.756Z |
| `payout_external_reference` | `wd_67786aba15ef420083118834` |

Evidência: `tmp/p15y-exec-r2/05-execute.out`

### Classificação

**Caso C — erro de integração/API** (não Caso B saldo insuficiente).

---

## 8. Fases 6–7 — Wait / Replay

**N/A** — transferência não criada.

---

## 9. Fase 8 — Fechamento

```bash
fly secrets set ASAAS_PIX_OUT_PRODUCTION_ENABLED=false PAYMENT_ENGINE_PIXOUT_ENABLED=false ASAAS_PRODUCTION_ENABLED=false -a goldeouro-backend-v2
fly secrets unset PAYOUT_PROVIDER ASAAS_PIX_OUT_ENABLED -a goldeouro-backend-v2
```

`/health` pós-fechamento: **200 OK**

Evidência: `tmp/p15y-exec-r2/08-close-gates.txt`

---

## 10. Fase 9 — Verificação

`verify-pix-out-production-live-p15y.mjs` — **FAIL/N/A** (sem evidências live de transferência).

Evidência: `tmp/p15y-exec-r2/09-verify-p15y.out`

---

## 11. Conclusões R2

| Objetivo R2 | Resultado |
|-------------|-----------|
| Contornar exit code Windows | ✅ Preflight/execute stdout capturado |
| Evidência real API Asaas | ✅ POST `/transfers` em produção |
| Transferência financeira | ❌ |
| Certificação P1.5Z | ❌ bloqueada |

**Achado positivo:** Payment Engine provider-aware **funcionou ponta a ponta até o Asaas** — factory, gates, HTTP client, persistência parcial (`last_asaas_sync_at`, `payout_external_reference`).

**Bloqueador:** resposta Asaas `invalid_action` — investigar configuração conta produção (PIX OUT habilitado, saldo, KYC, limites).

---

## 12. Riscos

| Risco | Nota |
|-------|------|
| Saque `a705bb8c` agora `falhou` | Não reutilizável para nova tentativa sem novo saque |
| Rollback wallet | Wallet restaurada (+R$10) |
| `invalid_action` opaco | Requer painel Asaas / suporte |

---

## 13. Próxima Etapa

1. Investigar `invalid_action` no dashboard Asaas produção.
2. Creditar conta Asaas se saldo for causa subjacente.
3. Criar **novo saque** (preferir conta técnica) para eventual P1.5Y.EXEC.R3.
4. **P1.5Z** somente após transferência + webhook + ledger OK.

---

## 14. Veredito Final

**FAIL** — integração validada até provedor; resposta Asaas impediu transferência.

---

*Artefatos: `tmp/p15y-exec-r2/*`*

*Pipeline: P1.5Y.EXEC.R1 → **P1.5Y.EXEC.R2 (FAIL)** → P1.5Z (bloqueado)*
