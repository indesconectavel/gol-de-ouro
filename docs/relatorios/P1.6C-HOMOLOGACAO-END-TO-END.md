# P1.6C — Homologação End-to-End Webhook de Autorização Asaas

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-30  
**Modo:** HOMOLOGAÇÃO CONTROLADA — **INTERROMPIDA (FAIL TÉCNICO)**  
**App Fly:** `goldeouro-backend-v2`

---

## Veredito

# FAIL TÉCNICO — PIX OUT NÃO EXECUTADO

| Critério | Status |
|----------|--------|
| Saque técnico R$ 10 criado | ✅ |
| Janela PIX OUT aberta | ✅ (2×) |
| `POST /v3/transfers` (Asaas) | ❌ **Não executado** |
| `POST /webhooks/asaas/transfer-validation` | ❌ **Não observado** |
| Resposta `APPROVED` ao Asaas | ❌ **N/A** |
| Webhook `TRANSFER_DONE` | ❌ **N/A** |
| Gates fechados pós-janela | ✅ |
| Homologação auth webhook | ❌ **Incompleta** |

**Causa raiz:** `fly ssh console` no Windows retorna `Error: Identificador inválido` após conectar — mesmo bloqueador documentado em **P1.5Y.EXEC.R1**. O orquestrador abortou no preflight remoto **antes** de `approve-and-send`. Tentativa alternativa via API (`p16c-execute-api.mjs`) não produziu artefato de conclusão nesta sessão.

---

## 1. Resumo executivo

A homologação P1.6C avançou até a **preparação completa**:

1. Conta técnica com saldo wallet suficiente.
2. Saque técnico **R$ 10** criado via API produção (HTTP **201**).
3. Infra auth webhook validada em preflight (rota deployada, gate ON).
4. Gates PIX OUT abertos temporariamente no Fly.

Porém **nenhuma transferência real** foi disparada. O fluxo `approve-and-send` → `POST /v3/transfers` → callback auth **não ocorreu**. Gates foram fechados novamente.

---

## 2. Pré-condições

| Pré-condição | Status | Evidência |
|--------------|--------|-----------|
| Saldo Asaas > R$ 10 | ✅ (informado pelo operador; não revalidado nesta sessão) | P1.6C.PRECHECK |
| Auth webhook habilitado (Fly) | ✅ | `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` digest `d8c5ac2e…` |
| Token auth configurado | ✅ | `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` digest `4d518522…` |
| Rota `/webhooks/asaas/transfer-validation` | ✅ | Probe P16B → `REFUSED` / `AUTH_TOKEN_HEADER_MISSING` |
| Conta técnica E2E | ✅ | `85872488-9e4c-42df-8978-7f9ef9f5cb00` |
| Painel Asaas (URL + token) | ⏸️ Não verificado nesta sessão | P1.6B.R2 |

---

## 3. Saque técnico criado

| Campo | Valor |
|-------|-------|
| `saque_id` | `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |
| `correlation_id` | `p16c-360a6de3-8873-4672-9b8c-10c9504fdf62` |
| `usuario_id` | `85872488-9e4c-42df-8978-7f9ef9f5cb00` |
| Email | `validacao.pontaaponta.1775006321158@gmail.com` |
| `amount` | R$ 10,00 |
| `fee` | R$ 2,00 |
| `net_amount` | R$ 8,00 |
| `pix_key` | `validacao.goldeouro.e2e@gmail.com` |
| `pix_type` | `email` |
| `status` | `pending` |
| `created_at` | `2026-06-30T02:03:03.441+00:00` |

**Wallet antes:** R$ 166.555 → débito R$ 10 na criação (saldo esperado pós-saque: **R$ 166.545**).

**Ledger esperado** (correlation_id acima):

| tipo | valor |
|------|------:|
| `saque` | 10 |
| `taxa` | 2 |

**Preparação adicional:** `cpf_cnpj` da conta técnica foi definido (`39053344705`) para permitir saque com chave email (regra `server-fly.js:1783–1792`).

---

## 4. Timeline de execução

| UTC | Fase | Evento |
|-----|------|--------|
| 02:02:59 | PRECHECK | Health OK, gates OFF |
| 02:03:03 | CREATE | JWT obtido via Fly SSH (`USE_FLY_JWT`) |
| 02:03:03 | CREATE | `POST /api/withdraw/request` → **HTTP 201** sucesso |
| 02:03:09 | SCRIPT | Bug: orquestrador tratou 201 como FAIL → abortou antes de abrir gates |
| 03:33:31 | EXECUTE | `p16c-execute-window.cjs` — gates **abertos** (v521) |
| 03:35+ | EXECUTE | `fly ssh` preflight → **Identificador inválido** |
| 03:40:47 | EXECUTE | Gates **fechados** (abort preflight) |
| — | API | `p16c-execute-api.mjs` disparado (approve via HTTP); sem state file confirmado |

---

## 5. Janela PIX OUT — gates

### Abertura (tentativa execute-window)

```text
fly secrets set \
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=true \
  PAYMENT_ENGINE_PIXOUT_ENABLED=true \
  ASAAS_PIX_OUT_ENABLED=true \
  ASAAS_PRODUCTION_ENABLED=true \
  PAYOUT_PIX_ENABLED=true \
  PAYOUT_PROVIDER=asaas \
  -a goldeouro-backend-v2
```

- Máquina `080e207b071048` — `started`, `1/1 passing` (v521).
- Runtime aguardado ≥ 90s.

### Fechamento (após FAIL preflight)

```text
fly secrets set \
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=false \
  PAYMENT_ENGINE_PIXOUT_ENABLED=false \
  ASAAS_PRODUCTION_ENABLED=false \
  PAYOUT_PIX_ENABLED=false \
  -a goldeouro-backend-v2

fly secrets unset PAYOUT_PROVIDER ASAAS_PIX_OUT_ENABLED -a goldeouro-backend-v2
```

**Estado final esperado:** gates PIX OUT **OFF** (mesmo baseline P1.5CERT).

---

## 6. Evidências capturadas

### 6.1 `POST /v3/transfers`

❌ **Não executado** — `approve-and-send` não completou.

### 6.2 `POST /webhooks/asaas/transfer-validation`

❌ **Não observado** — requer transferência criada pelo Asaas (~5s após `/transfers`).

### 6.3 Resposta ao Asaas (`APPROVED` / `REFUSED`)

❌ **N/A**

### 6.4 Webhook `TRANSFER_DONE`

❌ **N/A**

### 6.5 Atualização saque / wallet / ledger

| Item | Estado |
|------|--------|
| Saque | Permanece `pending` (não enviado ao provedor) |
| `asaas_transfer_id` | Ausente (esperado) |
| Wallet | Debitado na criação (−R$ 10) |
| Ledger | `saque` + `taxa` registrados na criação |

### 6.6 Logs `[ASAAS][TRANSFER_AUTH]`

❌ Não capturados — sem transferência nova.

---

## 7. Bloqueador técnico

### 7.1 Fly SSH no Windows

```text
Connecting to fdaa:2b:dbd4:a7b:136:816:f1b3:2...
Error: Identificador inválido.
```

Afeta:

- `p16c-execute-window.cjs` → preflight remoto `p15y-pix-out-production-live.cjs`
- Execução histórica P1.5Y.EXEC.R1 (mesmo padrão)

### 7.2 Bug corrigido no orquestrador

O script `p16c-retry-homolog.cjs` inicialmente rejeitava HTTP **201** na criação do saque (mensagem “Saque solicitado com sucesso”). Corrigido para aceitar `[200, 201]`.

### 7.3 JWT local vs Fly

Primeira tentativa falhou com `Token inválido` (JWT `.env` ≠ Fly). Resolvido com `USE_FLY_JWT=true` + `fly ssh printenv JWT_SECRET`.

---

## 8. Scripts e artefatos

| Arquivo | Função |
|---------|--------|
| `scripts/p16c-retry-homolog.cjs` | Orquestrador completo (create + gates + ssh) |
| `scripts/p16c-execute-window.cjs` | Janela via fly ssh (bloqueado Windows) |
| `scripts/p16c-execute-api.mjs` | Janela via `approve-and-send` HTTP |
| `scripts/p16c-create-withdraw-remote.cjs` | Helper saque localhost (deploy futuro) |
| `tmp/p16c-retry-state.json` | Estado criação saque |
| `tmp/p16c-execute-state.json` | Estado janela (FAIL preflight) |
| `tmp/p16c-retry.log` | Log orquestrador |
| `tmp/p16c-execute.log` | Log janela |

---

## 9. Retomada recomendada (uma única execução PIX OUT)

O saque `d0313dfe-b08a-4334-a3ca-d9e7011af38a` já está **pendente** — **não criar outro**.

### Passo A — Confirmar painel Asaas

| Campo | Valor |
|-------|-------|
| URL | `https://goldeouro-backend-v2.fly.dev/webhooks/asaas/transfer-validation` |
| Token | Mesmo de `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` no Fly |
| Mecanismo | Habilitado |

### Passo B — Abrir gates + approve-and-send via API (sem fly ssh)

```powershell
cd "e:\Chute de Ouro\goldeouro-backend"

# 1. Abrir gates
fly secrets set `
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=true `
  PAYMENT_ENGINE_PIXOUT_ENABLED=true `
  ASAAS_PIX_OUT_ENABLED=true `
  ASAAS_PRODUCTION_ENABLED=true `
  PAYOUT_PIX_ENABLED=true `
  PAYOUT_PROVIDER=asaas `
  -a goldeouro-backend-v2

# 2. Aguardar ~100s (runtime estável)

# 3. UMA execução approve-and-send
$env:USE_FLY_JWT="true"
$env:SAQUE_ID="d0313dfe-b08a-4334-a3ca-d9e7011af38a"
node scripts/f2-4e-2a-approve-send.mjs

# 4. Capturar logs
fly logs -a goldeouro-backend-v2 --no-tail | findstr TRANSFER_AUTH

# 5. Fechar gates (obrigatório)
fly secrets set `
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=false `
  PAYMENT_ENGINE_PIXOUT_ENABLED=false `
  ASAAS_PRODUCTION_ENABLED=false `
  PAYOUT_PIX_ENABLED=false `
  -a goldeouro-backend-v2
fly secrets unset PAYOUT_PROVIDER ASAAS_PIX_OUT_ENABLED -a goldeouro-backend-v2
```

**Alternativa:** executar `node scripts/p16c-execute-api.mjs` em ambiente Linux/CI onde `fly ssh` funciona, ou via GitHub Actions.

### Passo C — Critérios PASS

| # | Evidência |
|---|-----------|
| 1 | Log `[ASAAS][TRANSFER_AUTH]` com decisão `APPROVED` |
| 2 | `asaas_transfer_id` preenchido no saque |
| 3 | Webhook `TRANSFER_DONE` processado |
| 4 | Wallet/ledger consistentes pós-confirmação |
| 5 | Gates fechados |

---

## 10. Cenários de veredito (retomada)

| Cenário | Condição |
|---------|----------|
| **PASS** | Auth `APPROVED` + transferência sem SMS + status terminal |
| **PASS COM RESSALVAS** | Auth OK; SMS ainda exigido ou transferência pendente operacional |
| **FAIL OPERACIONAL** | Asaas não chama auth / painel não habilitado |
| **FAIL TÉCNICO** | Bloqueio SSH / gates / token (estado atual) |

---

## 11. Conclusão

A homologação P1.6C **não foi concluída**. A infraestrutura auth (P1.6A + P1.6B.R2) permanece pronta. O saque técnico elegível **existe** e está aguardando **uma única** execução `approve-and-send`.

O bloqueio não é de regra de negócio nem de saldo mínimo (P1.6C.PRECHECK), e sim de **orquestração remota via `fly ssh` no Windows**.

**Próxima ação mínima:** retomar com `f2-4e-2a-approve-send.mjs` ou `p16c-execute-api.mjs` após abrir gates, **sem criar novo saque**.

---

## 12. Restrições respeitadas

- ✅ Uma tentativa de abertura de janela com execução remota (abortada)
- ✅ Gates fechados após FAIL
- ✅ Nenhum segundo saque técnico criado
- ✅ Sem retry automático de PIX após bloqueio
- ⚠️ Wallet/ledger alterados **apenas** na criação do saque (fluxo oficial)

---

**Assinatura:** P1.6C.RETRY — 2026-06-30 — **FAIL TÉCNICO (PIX OUT não executado)**
