# P1.5Y.EXEC — Homologação Operacional PIX OUT Asaas Produção

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** PRODUÇÃO CONTROLADA  
**App Fly:** `goldeouro-backend-v2` (v497 pós-rollback)

---

## Veredito

# FAIL

A janela operacional foi **aberta e fechada**, porém **nenhuma transferência PIX OUT real foi executada**. O `fly ssh console` falhou com **`app has no started VMs`** durante restart pós-abertura de secrets. Gates **restaurados OFF** · runtime **estável** · suíte mock P1.5E–I **PASS**.

**Regra P1.5Y.EXEC:** parar imediatamente · não repetir sem plano corretivo.

---

## 1. Resumo Executivo

| Critério | Status |
|----------|--------|
| Auditoria pré-janela (health, schema, gates OFF) | ✅ |
| Migration P1.5D (4 colunas) | ✅ |
| Abertura controlada dos gates | ✅ |
| Execução HTTP POST `/v3/transfers` | ❌ **Não executada** |
| `transfer_id` Asaas | ❌ |
| Webhook TRANSFER live | ❌ N/A |
| Ledger/wallet pós-webhook | ❌ N/A |
| Replay idempotente live | ❌ N/A |
| Gates restaurados OFF | ✅ |
| `/health` pós-janela | ✅ 200 |
| Suíte verify P1.5E–I (mock) | ✅ PASS |
| verify-p15y live | ❌ N/A (sem transferência) |
| MP / PIX IN | ✅ Inalterados |

---

## 2. Timeline Completa

| Horário (UTC) | Fase | Evento |
|---------------|------|--------|
| ~04:22 | 1-audit | `/health` 200, database connected |
| ~04:22 | 1-audit | Schema P1.5D REST OK (4 colunas) |
| ~04:22 | 1-audit | Saque selecionado: `a705bb8c-…` (R$ 10 / legado, menor valor disponível) |
| ~04:36 | 2-open | `fly secrets set` — gates PIX OUT ON + `PAYOUT_PROVIDER=asaas` |
| ~04:38 | 3-exec | `fly ssh console` → **FAIL: no started VMs** (app em restart) |
| ~04:44 | 6-close | `fly secrets set` gates OFF + `unset PAYOUT_PROVIDER` |
| ~04:46 | 6-close | App v497 started, health 200 |
| ~04:51 | 7-verify | Suíte P1.5E–I mock — PASS |

---

## 3. Health

### Pré-janela

```json
{
  "status": "ok",
  "database": "connected",
  "mercadoPago": "connected",
  "version": "1.2.1"
}
```

### Pós-janela (2026-06-29T04:46:38Z)

```json
{
  "status": "ok",
  "database": "connected",
  "mercadoPago": "connected",
  "contadorChutes": 535
}
```

### Fly status pós-rollback

| Processo | Estado | Checks |
|----------|--------|--------|
| `app` | started | 1/1 passing |
| `payout_worker` | stopped | — |

---

## 4. Secrets / Gates

### Antes

| Flag | Valor |
|------|-------|
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | false |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | false |
| `ASAAS_PRODUCTION_ENABLED` | false |
| `PAYOUT_PROVIDER` | unset |
| `ASAAS_PIX_OUT_ENABLED` | unset |

### Durante (breve)

```bash
fly secrets set \
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=true \
  PAYMENT_ENGINE_PIXOUT_ENABLED=true \
  ASAAS_PIX_OUT_ENABLED=true \
  ASAAS_PRODUCTION_ENABLED=true \
  PAYOUT_PROVIDER=asaas \
  -a goldeouro-backend-v2
```

Máquinas atualizadas: `080e207b071048` (app), `784e047bd04e08` (payout_worker).

### Depois (confirmado digest `fa61a138…` = false)

```bash
fly secrets set \
  ASAAS_PIX_OUT_PRODUCTION_ENABLED=false \
  PAYMENT_ENGINE_PIXOUT_ENABLED=false \
  ASAAS_PRODUCTION_ENABLED=false \
  -a goldeouro-backend-v2

fly secrets unset PAYOUT_PROVIDER ASAAS_PIX_OUT_ENABLED -a goldeouro-backend-v2
```

---

## 5. Saque de Homologação Selecionado

| Campo | Valor |
|-------|-------|
| **`saque_id`** | `a705bb8c-5f90-4532-9f55-d167dbdff672` |
| Valor bruto / líquido / taxa | R$ 10,00 / R$ 8,00 / R$ 2,00 |
| Status pré-janela | `pendente` |
| PIX | `0478***8664` |
| `correlation_id` | `486f6bef-4d6f-42da-be56-62a28e55dd3d` |
| Tipo | Legado usuário (conta técnica sem saque pendente) |

**Wallet antes:** R$ 0,00  
**Ledger antes:** `saque` R$ 10 + `taxa` R$ 2

---

## 6. Transferência

**Não executada.**

Erro capturado:

```text
Error: app goldeouro-backend-v2 has no started VMs.
It may be unhealthy or not have been deployed yet.
```

**Causa provável:** `fly secrets set` dispara restart imediato das máquinas; `fly ssh` foi invocado antes do `app` estar `started` + checks passing.

**Estado pós-tentativa (banco):**

| Campo | Valor |
|-------|-------|
| `status` | `pendente` (inalterado) |
| `asaas_transfer_id` | `null` |
| `asaas_transfer_status` | `null` |
| `asaas_payout_raw` | `null` |
| `payout_external_reference` | `null` |

---

## 7. Webhook

**N/A** — transferência não criada.

---

## 8. Banco / Wallet / Ledger

Sem mutação pós-janela. Colunas P1.5D permanecem válidas. Campos `mp_*` preservados.

---

## 9. Idempotência / Replay

**N/A** em produção live.

---

## 10. Suíte de Verificação (P1.5E–I)

Executada localmente (mock/regressão) — **todas PASS**:

| Script | Resultado |
|--------|-----------|
| `verify-payout-provider-aware-p15e.mjs` | ✅ PASS |
| `verify-payout-factory-unification-p15f.mjs` | ✅ PASS |
| `verify-asaas-transfer-webhook-p15g.mjs` | ✅ PASS |
| `verify-pix-out-end-to-end-p15h.mjs` | ✅ PASS |
| `verify-pix-out-production-homologation-p15i.mjs` | ✅ PASS |
| `verify-pix-out-production-live-p15y.mjs` | ❌ N/A |

---

## 11. Riscos

| Risco | Severidade | Evidência |
|-------|------------|-----------|
| Janela aberta sem transferência | Média | Gates fechados rapidamente |
| Restart Fly bloqueia SSH | Alta | Causa direta do FAIL |
| Saque legado vs conta técnica | Média | Sem saque técnico pendente |
| Config inconsistente derruba worker | Alta | Lição P1.5Y — gates fechados corretamente |

---

## 12. Comandos Executados

```bash
# Abertura
fly secrets set ASAAS_PIX_OUT_PRODUCTION_ENABLED=true PAYMENT_ENGINE_PIXOUT_ENABLED=true ASAAS_PIX_OUT_ENABLED=true ASAAS_PRODUCTION_ENABLED=true PAYOUT_PROVIDER=asaas -a goldeouro-backend-v2

# Execução (FALHOU)
fly ssh console -a goldeouro-backend-v2 -C "sh -c 'P15Y_PHASE=execute P15Y_ALLOW_REAL_PAYOUT=1 P15Y_SAQUE_ID=a705bb8c-5f90-4532-9f55-d167dbdff672 node /app/scripts/p15y-pix-out-production-live.cjs'"

# Fechamento
fly secrets set ASAAS_PIX_OUT_PRODUCTION_ENABLED=false PAYMENT_ENGINE_PIXOUT_ENABLED=false ASAAS_PRODUCTION_ENABLED=false -a goldeouro-backend-v2
fly secrets unset PAYOUT_PROVIDER ASAAS_PIX_OUT_ENABLED -a goldeouro-backend-v2
```

---

## 13. Plano Corretivo (próxima tentativa)

1. Abrir gates (comando acima).
2. **Aguardar** `fly status` → `app` **started** + **1/1 checks passing** + `/health` 200 (≥ 60–90s).
3. Confirmar preflight via `fly ssh` **antes** de execute.
4. Executar **uma vez** via SSH.
5. `wait` → `replay` → fechar gates → `verify-p15y`.

**Alternativa preferida:** criar saque R$ 10 na conta técnica `85872488-…` antes da janela.

---

## 14. Próxima Etapa — P1.5Z

**BLOQUEADA** até PASS da homologação live.

Quando P1.5Y.EXEC receber PASS:

- Emitir **P1.5Z — Certificação Final PIX OUT Asaas Produção**
- Parecer técnico definitivo Indesconectável Payment Engine™
- Encerramento oficial V1 Gol de Ouro™

---

## 15. Veredito Final

| Critério PASS | Atendido |
|---------------|----------|
| Transferência real executada | ❌ |
| `transfer_id` Asaas | ❌ |
| Webhook processado | ❌ |
| Banco/ledger/wallet coerentes pós-webhook | ❌ |
| Replay idempotente live | ❌ |
| Gates OFF restaurados | ✅ |
| Runtime saudável | ✅ |
| Código certificado (P1.5E–I) | ✅ |

**Classificação:** **FAIL**

---

*Pipeline: P1.5I → P1.5Y (FAIL) → P1.5Y.R1 (preflight PASS) → **P1.5Y.EXEC (FAIL)** → P1.5Z (bloqueado)*

*Artefatos: `tmp/p15y-exec-state.json`, `tmp/p15y-exec-execute.out`, `scripts/run-p15y-exec-homolog.cjs`*
