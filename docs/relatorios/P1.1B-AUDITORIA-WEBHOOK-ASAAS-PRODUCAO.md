# P1.1B — Auditoria Final do Webhook Asaas Produção

**Projeto:** Gol de Ouro™ Backend  
**Data:** 2026-06-28  
**Modo:** READ-ONLY — nenhum código, banco, Fly, Asaas ou flag alterado  
**Gate:** `ASAAS_PRODUCTION_ENABLED=false` — **mantido**  
**App Fly:** `goldeouro-backend-v2` · máquina app `080e207b071048`

---

## Resumo executivo

| Etapa | Resultado |
|-------|-----------|
| 1 — Ambiente Fly | **FAIL** (parcial) — `ASAAS_API_KEY` vazia no runtime Node |
| 2 — Rota `POST /webhooks/asaas` | **PASS** |
| 3 — Configuração webhook (painel) | **PENDENTE OPERADOR** |
| 4 — Simulação lógica | **PASS** |
| **Conclusão geral** | **FAIL** |

A **proteção do webhook em produção está correta** (403 sem crédito, sem 500). A auditoria **falha** porque `ASAAS_API_KEY` está vazia no processo Node e a configuração no **painel Asaas produção** não pôde ser verificada nesta sessão.

---

## 1. Etapa 1 — Ambiente Fly

### 1.1 Secrets (`fly secrets list`)

**Timestamp:** 2026-06-28T15:52Z (aprox.)

| Secret | Existe na lista | Status Fly | Digest (parcial) |
|--------|:---------------:|:----------:|------------------|
| `ASAAS_API_KEY` | ✅ | Deployed | `49bb8a67…` |
| `ASAAS_WEBHOOK_TOKEN` | ✅ | Deployed | `7cb91c84…` |
| `ASAAS_WEBHOOK_ENABLED` | ✅ | Deployed | `d8c5ac2e…` |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | ✅ | Deployed | `d8c5ac2e…` |
| `ASAAS_PRODUCTION_ENABLED` | ✅ | Deployed | `fa61a138…` |

> Valores dos secrets **não exibidos** (apenas digest).

**Correlação digest × booleano (inferida por comprimento runtime):**

| Digest | Valor provável |
|--------|----------------|
| `d8c5ac2e11c8e492` | `true` (length=4) |
| `fa61a13817d73a23` | `false` (length=5) |

### 1.2 Runtime — processo Node (PID 662)

**Fonte:** `fly ssh console --machine 080e207b071048` + script read-only em `/proc/$PID/environ`  
**Timestamp:** 2026-06-28T15:54Z (aprox.)

| Variável | exists | length | empty | Valor inferido |
|----------|:------:|:------:|:-----:|----------------|
| `ASAAS_API_KEY` | true | **0** | **true** | ⚠️ **VAZIA** |
| `ASAAS_WEBHOOK_TOKEN` | true | **23** | false | presente |
| `ASAAS_WEBHOOK_ENABLED` | true | 4 | false | `true` |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | true | 4 | false | `true` |
| `ASAAS_PRODUCTION_ENABLED` | true | 5 | false | **`false`** |

### 1.3 Gates — confirmação

| Gate | Esperado (pré-go-live) | Observado | OK |
|------|------------------------|-----------|:--:|
| `ASAAS_PRODUCTION_ENABLED` | `false` | `false` | ✅ |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | `true` (fase intermediária runbook) | `true` | ✅ |
| `ASAAS_WEBHOOK_ENABLED` | `true` (fase intermediária) | `true` | ✅ |
| Crédito real bloqueado | sim | gate prod OFF | ✅ |

### 1.4 Veredito Etapa 1

**FAIL** — `ASAAS_API_KEY` com nome presente mas **valor vazio** no processo Node. Demais secrets/flags coerentes com pré-go-live (engine ON, gate prod OFF).

---

## 2. Etapa 2 — Auditoria da rota

### 2.1 Endpoint

| Item | Valor |
|------|-------|
| Método | `POST` |
| URL | `https://goldeouro-backend-v2.fly.dev/webhooks/asaas` |
| Implementação | `server-fly.js` L3551–3578 |
| Commit deploy | `fdfcd8aa1550a759fd916b253132c7379c017d17` (`/meta` 2026-06-28) |

### 2.2 Probes live

**Timestamp:** 2026-06-28T15:54–15:59Z

| Caso | Body | HTTP | Erro 5xx |
|------|------|:----:|:---------:|
| Mínimo | `{"event":"PAYMENT_RECEIVED"}` | **403** | ❌ |
| Shape válido sem token | `event` + `id` + `payment` | **403** | ❌ |
| Body vazio | `{}` | **403** | ❌ |
| Shape válido (PowerShell) | `PAYMENT_RECEIVED` + `payment` | **403** | ❌ |

**Nenhum probe retornou 500.** Rota **existe** e responde de forma determinística.

### 2.3 Resposta HTTP registrada

| Campo | Valor |
|-------|-------|
| **HTTP** | `403 Forbidden` |
| **Content-Type** | `application/json; charset=utf-8` |
| **Content-Length** | `119` |
| **Body** (inferido de `server-fly.js` L3556–3559; coerente com `Content-Length`) | `{"error":"asaas_webhook_blocked","message":"Rota Asaas bloqueada por flags ou produção sem ASAAS_PRODUCTION_ENABLED"}` |

> PowerShell `Invoke-WebRequest` não retornou o body no stream de erro; o JSON acima é **evidência estrutural** do handler + tamanho da resposta.

### 2.4 Headers relevantes (probe 2026-06-28T15:54:42Z)

| Header | Valor |
|--------|-------|
| `Content-Type` | `application/json; charset=utf-8` |
| `strict-transport-security` | `max-age=31536000; includeSubDomains; preload` |
| `x-content-type-options` | `nosniff` |
| `ratelimit-policy` | `100;w=900` |
| `ratelimit-remaining` | `97–99` (por probe) |
| `fly-request-id` | `01KW7F1B…` / `01KW7F1C…` / `01KW7F1CB…` |
| `server` | `Fly/866e52186c` |

### 2.5 Comportamento vs gates atuais

```text
POST /webhooks/asaas
  → isPaymentWebhookEngineEnabled()     → true  (passa)
  → isAsaasWebhookRouteAllowed()        → false (NODE_ENV=production + ASAAS_PRODUCTION_ENABLED=false)
  → HTTP 403 asaas_webhook_blocked
  → validator / normalizer / crédito    → NÃO EXECUTADOS
```

**Coerente:** engine habilitado, gate produção fechado → bloqueio na borda **antes** de qualquer efeito financeiro.

### 2.6 Logs

| Fonte | Resultado |
|-------|-----------|
| `fly logs` filtrado `WEBHOOK\|ASAAS` | **Não capturado** nesta sessão (comando lento / sem linhas filtradas no timeout) |
| Comportamento esperado | Sem log `asaas_webhook_valid_event` — request barrado em `server-fly.js` antes do engine |

### 2.7 Comparativo histórico

| Data | Engine | Gate prod | HTTP webhook |
|------|--------|-----------|:------------:|
| P1.1A | OFF | false | **404** `payment_webhook_engine_disabled` |
| P1.1B (esta auditoria) | ON | false | **403** `asaas_webhook_blocked` |

### 2.8 Veredito Etapa 2

**PASS** — rota existente, sem 500, comportamento coerente com gates, produção bloqueada.

---

## 3. Etapa 3 — Configuração webhook (painel Asaas)

> Configuração no painel **não auditada** (sem acesso API prod — `ASAAS_API_KEY` vazia). Checklist para preenchimento **manual pelo operador**.

### 3.1 Parâmetros obrigatórios produção

| Parâmetro | Valor correto para Gol de Ouro | Compatível com backend | Operador confirma |
|-----------|-------------------------------|:----------------------:|:-----------------:|
| **URL** | `https://goldeouro-backend-v2.fly.dev/webhooks/asaas` | ✅ `server-fly.js` | ☐ |
| **Token** | Idêntico a `ASAAS_WEBHOOK_TOKEN` (Fly, length=23) | ✅ header `asaas-access-token` | ☐ |
| **Versão API** | `v3` | ✅ Payment Engine F4.5+ | ☐ |
| **Tipo de envio** | `NON_SEQUENTIAL` ou `SEQUENTIAL` (Asaas) | ✅ backend stateless + idempotência Supabase | ☐ |
| **Webhook ativo** | Sim | — | ☐ |
| **SSL/HTTPS** | Obrigatório | ✅ Fly TLS | ☐ |

### 3.2 Eventos obrigatórios

| Evento Asaas | `validateAsaasWebhook` | `normalizeAsaasPaymentWebhook` | Crédito quando gate ON |
|--------------|:----------------------:|:------------------------------:|:----------------------:|
| `PAYMENT_RECEIVED` | ✅ KNOWN_PAYMENT_EVENTS | ✅ `shouldCreditWallet=true` se status approved | ✅ |
| `PAYMENT_CONFIRMED` | ✅ KNOWN_PAYMENT_EVENTS | ✅ idem | ✅ |
| `PAYMENT_CREATED` | ✅ conhecido | ❌ sem crédito (`CREDIT_EVENTS` exclui) | informativo |
| `PAYMENT_UPDATED` | ✅ conhecido | ❌ sem crédito | informativo |
| Eventos `TRANSFER_*` | ✅ conhecidos | ❌ sem crédito depósito | payout futuro |

**Mínimo operador no painel:** marcar **`PAYMENT_RECEIVED`** e **`PAYMENT_CONFIRMED`**.

### 3.3 Contrato validador (`validateAsaasWebhook`)

| Requisito | Implementação |
|-----------|---------------|
| Header auth | `asaas-access-token` (`ASAAS_WEBHOOK_AUTH_HEADER`) |
| Modo strict | default `ASAAS_WEBHOOK_STRICT_MODE=true` |
| Token runtime | `ASAAS_WEBHOOK_TOKEN` — **presente** (length 23) |
| Payload | campos `event` + `id` obrigatórios |
| Token inválido | `401` · `ASAAS_WEBHOOK_TOKEN_MISMATCH` |
| Payload inválido | `401` · `INVALID_PAYLOAD` |
| Evento desconhecido | ignorado · `IGNORED_UNKNOWN_EVENT` |

### 3.4 Contrato normalizer (`normalizeAsaasPaymentWebhook`)

| Campo interno | Origem |
|---------------|--------|
| `provider` | `'asaas'` |
| `eventType` | `body.event` |
| `paymentId` | `body.payment.id` |
| `status` | mapeado → `approved` se `RECEIVED`/`CONFIRMED` |
| `shouldCreditWallet` | `true` só para `PAYMENT_RECEIVED`/`PAYMENT_CONFIRMED` + approved |

### 3.5 Checklist final — painel Asaas produção

| # | Item | OK | Observação |
|---|------|:--:|------------|
| W1 | URL = `https://goldeouro-backend-v2.fly.dev/webhooks/asaas` | ☐ | |
| W2 | Token = valor de `ASAAS_WEBHOOK_TOKEN` no Fly | ☐ | length=23 no runtime |
| W3 | API v3 | ☐ | |
| W4 | `PAYMENT_RECEIVED` habilitado | ☐ | |
| W5 | `PAYMENT_CONFIRMED` habilitado | ☐ | |
| W6 | Webhook ativo / não interrompido | ☐ | verificar fila no painel |
| W7 | Teste de entrega (evento de teste Asaas) | ☐ | esperar 403 até gate abrir |
| W8 | Última entrega sem erro persistente | ☐ | |

### 3.6 Veredito Etapa 3

**PENDENTE OPERADOR** — parâmetros documentados e compatíveis com código; **confirmação no painel Asaas produção não realizada**.

---

## 4. Etapa 4 — Simulação lógica (sem abrir produção)

### 4.1 Scripts locais executados

| Script | Resultado | Timestamp |
|--------|-----------|-----------|
| `verify-asaas-production-webhook-wire.mjs` | **PASS** | 2026-06-28T15:55Z |
| `verify-payment-webhook-engine.mjs` | **PASS** | 2026-06-28T15:55Z |

### 4.2 Casos simulados relevantes

| Caso | Resultado |
|------|-----------|
| Gate default OFF | ✅ `isAsaasProductionEnabled()` = false |
| Prod sem gate → bloqueado | ✅ HTTP 403 / `blocked_prod` |
| Prod com gate + claim mock | ✅ crédito + idempotência |
| Token inválido | ✅ `INVALID_SIGNATURE_OR_TOKEN` |
| Payload inválido | ✅ `INVALID_PAYLOAD` |
| Evento desconhecido | ✅ ignorado |
| `PAYMENT_RECEIVED` normalizado | ✅ `shouldCreditWallet` quando approved |

### 4.3 Fluxo esperado com gate atual (produção Fly real)

```text
Asaas (painel prod — não verificado)
        ↓ POST + asaas-access-token
POST /webhooks/asaas
        ↓ PAYMENT_WEBHOOK_ENGINE_ENABLED=true  → PASSA
        ↓ isAsaasWebhookRouteAllowed()         → BLOQUEIA (prod + gate OFF)
HTTP 403 asaas_webhook_blocked
        ↓
validator          → NÃO ALCANÇADO em produção hoje
normalizer         → NÃO ALCANÇADO
Payment Engine     → NÃO ALCANÇADO
gate               → ASAAS_PRODUCTION_ENABLED=false
produção bloqueada → SEM CRÉDITO REAL ✅
```

### 4.4 Fluxo quando gate for aberto (referência — NÃO executado)

```text
POST /webhooks/asaas
  → validator (asaas-access-token)
  → normalizer (PAYMENT_RECEIVED → shouldCreditWallet)
  → isAsaasProductionWebhookCreditEnabled() = true
  → checkSupabaseDepositIdempotency
  → claimAndCreditApprovedPixDeposit
  → wallet + ledger
```

### 4.5 Veredito Etapa 4

**PASS** — simulação lógica e scripts de verificação confirmam wire, validação e bloqueio sem crédito real.

---

## 5. Estado consolidado

### 5.1 Flags

| Flag | Fly runtime | Pré-go-live OK |
|------|-------------|:--------------:|
| `ASAAS_PRODUCTION_ENABLED` | `false` | ✅ |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | `true` | ✅ (fase intermediária) |
| `ASAAS_WEBHOOK_ENABLED` | `true` | ✅ (fase intermediária) |

### 5.2 Secrets

| Secret | Fly list | Runtime Node | OK |
|--------|:--------:|:------------:|:--:|
| `ASAAS_API_KEY` | ✅ | **vazio** | ❌ |
| `ASAAS_WEBHOOK_TOKEN` | ✅ | length=23 | ✅ |

### 5.3 Webhook HTTP

| Item | Estado |
|------|--------|
| Rota implantada | ✅ |
| Protegida (403) | ✅ |
| Crédito real | ❌ bloqueado |
| Painel Asaas configurado | ☐ pendente operador |

---

## 6. Conclusão

# FAIL

| Componente | Veredito |
|------------|----------|
| Proteção rota webhook | **PASS** |
| Wire + simulação | **PASS** |
| Secrets runtime | **FAIL** (`ASAAS_API_KEY` vazia) |
| Painel Asaas | **PENDENTE** |

**Interpretação:** o webhook está **tecnicamente implantado e protegido** para pré-go-live. A auditoria **falha** na prontidão operacional completa por (1) chave API vazia no Node e (2) configuração do painel Asaas não confirmada.

---

## 7. Próximos passos

### 7.1 Fly (operador)

1. Re-setar `ASAAS_API_KEY` com chave produção válida (`$aact_prod_…`).
2. `fly apps restart goldeouro-backend-v2`.
3. Revalidar: `ASAAS_API_KEY` → `exists=true`, `length>0`, `empty=false`.
4. Manter `ASAAS_PRODUCTION_ENABLED=false` até checklist P1.0 §4.7.

### 7.2 Painel Asaas produção (operador)

1. Criar/confirmar webhook com parâmetros da §3.5.
2. Enviar evento de teste — **esperar HTTP 403** enquanto gate fechado (comportamento correto).
3. Após go-live autorizado: repetir teste → esperar `200` + crédito idempotente.

### 7.3 Engenharia (quando autorizado)

1. Abrir gate seguindo `ASAAS-PRODUCTION-GO-LIVE.md` §2 (último passo: `ASAAS_PRODUCTION_ENABLED=true`).
2. Executar depósito valor mínimo + replay webhook.

---

## 8. Dependências exclusivas do operador — painel Asaas

Itens que **somente o operador** pode concluir no **painel Asaas produção**:

1. **Registrar a URL** `https://goldeouro-backend-v2.fly.dev/webhooks/asaas`.
2. **Configurar o token de autenticação** igual ao `ASAAS_WEBHOOK_TOKEN` do Fly (23 caracteres no runtime atual).
3. **Selecionar API v3**.
4. **Habilitar eventos** `PAYMENT_RECEIVED` e `PAYMENT_CONFIRMED` (mínimo).
5. **Ativar o webhook** e confirmar que não está interrompido / sem fila penalizada.
6. **Executar teste de entrega** do painel e arquivar evidência (HTTP esperado: **403** enquanto gate fechado).
7. **Validar conta produção** (CNPJ, PIX IN, saldo) — fora do escopo webhook, mas bloqueia go-live.

**Adicional Fly (não é painel Asaas, mas operador):** corrigir `ASAAS_API_KEY` vazia no runtime.

---

## 9. Conformidade da auditoria

| Verificação | Status |
|-------------|:------:|
| Arquitetura alterada | **Não** |
| Regras de negócio alteradas | **Não** |
| Frontend alterado | **Não** |
| Go-live aberto | **Não** |
| `ASAAS_PRODUCTION_ENABLED` alterado | **Não** |
| Valores de secrets exibidos | **Não** |
| Arquivo criado | apenas este relatório |

---

**Fim do relatório P1.1B — Auditoria Final Webhook Asaas Produção**
