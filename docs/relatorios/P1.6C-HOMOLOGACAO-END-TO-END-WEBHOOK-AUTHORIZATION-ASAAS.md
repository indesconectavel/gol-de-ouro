# P1.6C — Homologação Final End-to-End — Webhook de Autorização Asaas

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-30  
**Modo:** HOMOLOGAÇÃO CONTROLADA — **UMA TENTATIVA**  
**App Fly:** `goldeouro-backend-v2`  
**Saque alvo:** `d0313dfe-b08a-4334-a3ca-d9e7011af38a`

---

## Veredito

# FAIL

A homologação end-to-end **não foi concluída**. Uma janela PIX OUT foi aberta e fechada, mas **`approve-and-send` / `POST /v3/transfers` nunca executou** — o preflight remoto falhou com `fly ssh` no Windows (`Error: Identificador inválido.`). Não há evidência de chamada ao webhook `/webhooks/asaas/transfer-validation`, resposta `APPROVED`, transferência `DONE` ou logs `[ASAAS][TRANSFER_AUTH]` em produção para este saque.

---

## 1. Resumo executivo

| Critério | Status |
|----------|--------|
| Saque técnico `d0313dfe-…` preparado (P1.6C.PREP) | ✅ |
| Precheck final (P1.6C.FINAL-PRECHECK) | ✅ `READY_TO_EXECUTE` |
| Gates PIX OUT abertos na janela | ⚠️ Sim (~7 min) — depois fechados |
| `P15Y_PHASE=execute` / `approve-and-send` | ❌ **Não executado** |
| Webhook auth chamado pelo Asaas | ❌ N/A (sem transferência nova) |
| Resposta `APPROVED` | ❌ N/A |
| SMS exigido | ❌ N/A |
| Transferência `DONE` | ❌ N/A |
| Wallet / ledger / Asaas reconciliados | ❌ N/A |
| Gates fechados pós-janela (financeiros) | ✅ |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=false` | ⏸️ Não alterado (homolog incompleta) |

---

## 2. Saque alvo (pré-execução)

Fonte: `P1.6C.PREP-SAQUE-TECNICO-PENDENTE.md`, `tmp/p16c-retry-state.json`.

| Campo | Valor |
|-------|-------|
| `saque_id` | `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |
| `user_id` | `85872488-9e4c-42df-8978-7f9ef9f5cb00` |
| `correlation_id` | `p16c-360a6de3-8873-4672-9b8c-10c9504fdf62` |
| `amount` / `fee` / `net_amount` | R$ 10 / R$ 2 / **R$ 8** |
| `status` esperado pré-janela | `pendente` |
| `asaas_transfer_id` pré-janela | **null** |
| Chave PIX | `validacao.goldeouro.e2e@gmail.com` (email) |

---

## 3. Janela de execução (tentativa única)

**Script:** `node scripts/p16c-execute-window.cjs`  
**Variáveis:** `P16C_ALLOW_REAL=1`, `P16C_SAQUE_ID=d0313dfe-b08a-4334-a3ca-d9e7011af38a`  
**Evidência:** `tmp/p16c-execute-state.json`, `tmp/p16c-execute.log`

### 3.1 Timeline

| Horário (UTC) | Fase | Resultado |
|---------------|------|-----------|
| `2026-06-30T03:33:32Z` | Abertura gates | ✅ `fly secrets set` — deploy rolling OK |
| `2026-06-30T03:33:39Z` | Runtime | ✅ app `started`, 1/1 health checks |
| `2026-06-30T03:33:xx` | Preflight remoto (`fly ssh`) | ❌ **FAIL** — `Identificador inválido` |
| `2026-06-30T03:40:47Z` | Fechamento gates | ✅ financeiros OFF + `PAYOUT_PROVIDER` unset |

### 3.2 Gates abertos (conforme especificação)

```
ASAAS_PRODUCTION_ENABLED=true
ASAAS_PIX_OUT_PRODUCTION_ENABLED=true
PAYMENT_ENGINE_PIXOUT_ENABLED=true
PAYOUT_PIX_ENABLED=true
PAYOUT_PROVIDER=asaas
```

`ENABLE_PIX_PAYOUT_WORKER` **não** foi ligado (fluxo admin/direto).

### 3.3 Bloqueador — preflight remoto

```
$ fly ssh console -a goldeouro-backend-v2 -C sh -c "P15Y_PHASE=preflight P15Y_SAQUE_ID=d0313dfe-..."
Connecting to fdaa:2b:dbd4:a7b:136:816:f1b3:2...
Error: Identificador inválido.
```

**Impacto:** `P15Y_PHASE=execute` nunca foi invocado. Nenhum `POST /v3/transfers`. O Asaas **não** dispara o webhook de autorização sem transferência nova.

**Causa raiz:** `fly ssh console` instável no ambiente Windows/PowerShell desta sessão (mesmo padrão observado em `tmp/p15y-exec-r2/05-execute.out` e `tmp/p15y-exec-r1-state.json`).

---

## 4. Evidências de webhook auth — N/A

| Verificação | Esperado | Observado |
|-------------|----------|-----------|
| Asaas chama `POST /webhooks/asaas/transfer-validation` | ✅ | ❌ Sem transferência |
| Log `[ASAAS][TRANSFER_AUTH]` event `approved` | ✅ | ❌ Não capturado |
| Resposta HTTP `APPROVED` | ✅ | ❌ N/A |
| SMS / autorização crítica | ❌ não exigido | ❌ N/A |
| `TRANSFER_DONE` / status terminal | ✅ | ❌ N/A |

Formato esperado (código `src/finance/webhooks/asaasTransferAuthorization.js`):

```json
[ASAAS][TRANSFER_AUTH] {"event":"approved","authorized":true,"saque_id":"d0313dfe-...","transfer_id":"..."}
```

---

## 5. Gates — estado pós-tentativa

| Gate | Pós-janela (esperado) | Observado |
|------|----------------------|-----------|
| `ASAAS_PRODUCTION_ENABLED` | false | ✅ fechado (`p16c-execute.log`) |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | false | ✅ |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | false | ✅ |
| `PAYOUT_PIX_ENABLED` | false | ✅ |
| `PAYOUT_PROVIDER` | unset | ✅ unset |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | false *após homolog PASS* | ⏸️ **permanece true** (homolog incompleta — correto não desligar auth sem evidência) |
| `ENABLE_PIX_PAYOUT_WORKER` | false | ✅ inalterado |

---

## 6. Auditoria DB read-only

**Script:** `node scripts/p16c-audit-only.cjs`  
**Nota:** coluna inexistente `payout_provider` removida do SELECT (corrigido nesta sessão).

Auditoria durante a sessão encontrou erro de schema na query antiga; **estado do saque pós-falha inferido** pelo fluxo: preflight falhou **antes** de `approve-and-send` → `asaas_transfer_id` deve permanecer **null**, status **pendente**, wallet **166545** (débito R$ 10 na criação).

| Entidade | Pré-janela | Pós-tentativa (inferido) |
|----------|------------|--------------------------|
| `saques.status` | `pendente` | `pendente` |
| `asaas_transfer_id` | null | null |
| `usuarios.saldo` | 166545 | 166545 |
| `ledger_financeiro` | `saque` + `taxa` | inalterado |

**Revalidação obrigatória:**

```powershell
cd "e:\Chute de Ouro\goldeouro-backend"
node scripts/p16c-audit-only.cjs
# → tmp/p16c-audit.json
```

---

## 7. Health produção (read-only)

```json
{
  "status": "ok",
  "database": "connected",
  "version": "1.2.1",
  "timestamp": "2026-06-30T04:14:27.641Z"
}
```

Fonte: `GET https://goldeouro-backend-v2.fly.dev/health`

---

## 8. Correção preparada — execução Windows-safe

Foi adicionado script que **evita `fly ssh`** e usa `POST /api/admin/withdraw/approve-and-send` via HTTP:

| Artefato | Função |
|----------|--------|
| `scripts/p16c-homolog-final.cjs` | Janela única: gates → wait 100s → approve-and-send → poll → logs → fecha gates incl. auth |
| `tmp/run-p16c-homolog.bat` | Runner batch |

```powershell
cd "e:\Chute de Ouro\goldeouro-backend"
$env:P16C_ALLOW_REAL = "1"
$env:P16C_SAQUE_ID = "d0313dfe-b08a-4334-a3ca-d9e7011af38a"
node scripts/p16c-homolog-final.cjs
```

**Saídas esperadas:** `tmp/p16c-homolog-state.json`, `tmp/p16c-homolog.log`

---

## 9. Matriz de veredito (critérios P1.6C)

| Critério | Resultado |
|----------|-----------|
| Auth webhook `APPROVED` | ❌ FAIL |
| Transferência concluída sem SMS | ❌ FAIL |
| Webhook não chamado / token falhar | ✅ **FAIL aplicável** — webhook não chamado (sem transfer) |
| Inconsistência wallet/ledger/Asaas | ⏸️ N/A |

---

## 10. Conclusão

| Item | Resultado |
|------|-----------|
| Veredito P1.6C | **FAIL** |
| Motivo | Preflight remoto bloqueado por `fly ssh` Windows; PIX OUT não executado |
| Auth webhook homologado | ❌ Não |
| Gates financeiros | ✅ Fechados após falha |
| Saque reutilizável | ✅ Provável — pendente sem `asaas_transfer_id` |
| Próximo passo | Executar `scripts/p16c-homolog-final.cjs` (Windows-safe) **ou** homolog via máquina Linux / CI com `fly ssh` funcional |

---

## 11. Artefatos desta sessão

| Arquivo | Conteúdo |
|---------|----------|
| `tmp/p16c-execute-state.json` | Estado janela — verdict FAIL, stopReason preflight |
| `tmp/p16c-execute.log` | Log fly secrets + ssh error |
| `tmp/p16c-agent-capture.txt` | Agregação evidências |
| `scripts/p16c-homolog-final.cjs` | Script retry Windows-safe |
| `docs/relatorios/P1.6C.FINAL-PRECHECK.md` | READY_TO_EXECUTE (pré-requisito atendido) |

**FAIL** — infraestrutura auth pronta (P1.6A–P1.6B), saque técnico preparado, mas homologação real end-to-end **não completou** por bloqueio operacional de execução remota no ambiente Windows.
