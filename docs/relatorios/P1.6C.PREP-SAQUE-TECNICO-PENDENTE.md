# P1.6C.PREP — Saque Técnico Pendente para Homologação Final

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-30  
**Modo:** PREPARAÇÃO CONTROLADA — **SEM PIX OUT**  
**App Fly:** `goldeouro-backend-v2`

---

## Veredito

# READY

Um único saque técnico novo de **R$ 10,00** bruto foi criado via **fluxo oficial** (`POST /api/withdraw/request`). Status **`pendente`**. Nenhum gate PIX OUT aberto. Nenhuma chamada a `/v3/transfers`. Nenhum `approve-and-send`.

---

## 1. Resumo executivo

| Critério | Status |
|----------|--------|
| Saque técnico `pendente` elegível | ✅ **1 criado** |
| Conta técnica `85872488-…` | ✅ |
| Valor bruto R$ 10 | ✅ |
| Taxa R$ 2 / líquido R$ 8 | ✅ |
| `asaas_transfer_id` | ✅ **null** (PIX OUT não aberto) |
| Gates PIX OUT | ✅ OFF |
| Auth webhook gate | ✅ ON (P1.6B.R2) |
| Saques antigos alterados | ✅ Não |
| Fluxo legado usado | ✅ Não |

---

## 2. Identificadores do saque preparado

| Campo | Valor |
|-------|-------|
| **`saque_id`** | `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |
| **`user_id`** | `85872488-9e4c-42df-8978-7f9ef9f5cb00` |
| **Email conta** | `validacao.pontaaponta.1775006321158@gmail.com` |
| **`correlation_id`** | `p16c-360a6de3-8873-4672-9b8c-10c9504fdf62` |
| **Chave PIX** | `validacao.goldeouro.e2e@gmail.com` (tipo `email`) |
| **Criado em** | `2026-06-30T02:03:03.441+00:00` |

---

## 3. Wallet — antes e depois

| Momento | Saldo wallet (`usuarios.saldo`) |
|---------|-------------------------------|
| **Antes** (pré-débito) | **R$ 166.555,00** (`166555`) |
| **Depois** (pós-débito esperado) | **R$ 166.545,00** (`166545`) |
| Débito na solicitação | R$ 10,00 (bruto) |

**Nota:** o débito ocorre na criação do saque (`server-fly.js` — modelo financeiro V1-1E). O saldo Asaas (≈ R$ 10,19 informado) **não** é debitado nesta etapa.

---

## 4. Ledger — esperado (fluxo oficial)

Com HTTP **201** na criação, o backend insere **duas** linhas em `ledger_financeiro` antes de responder:

| `tipo` | `valor` | `referencia` |
|--------|---------|--------------|
| `saque` | R$ 10,00 | `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |
| `taxa` | R$ 2,00 | `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |

Ambas com `correlation_id` = `p16c-360a6de3-8873-4672-9b8c-10c9504fdf62`.

**Revalidação read-only local:**

```bash
node scripts/p16c-audit-only.cjs
# grava tmp/p16c-audit.json
```

---

## 5. Registro `saques` (resposta API produção)

Evidência: `tmp/p16c-retry-state.json` → `createWithdraw.parsed` (HTTP **201**).

| Campo | Valor |
|-------|-------|
| `id` | `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |
| `amount` | `10` |
| `fee` | `2` |
| `net_amount` | `8` |
| `status` | `pending` (DB: `pendente`) |
| `pix_key` | `validacao.goldeouro.e2e@gmail.com` |
| `pix_type` | `email` |
| `asaas_transfer_id` | **ausente** |
| `mp_transaction_intent_id` | **ausente** |

---

## 6. Como o saque foi criado (fluxo oficial)

| Etapa | Detalhe |
|-------|---------|
| Método | `POST https://goldeouro-backend-v2.fly.dev/api/withdraw/request` |
| Auth | JWT assinado com `userId` da conta técnica (mesmo caminho do app) |
| Idempotência | Header `x-idempotency-key` = `correlation_id` |
| Body | `{ valor: 10, chave_pix: "validacao.goldeouro.e2e@gmail.com", tipo_chave: "email" }` |
| Script auxiliar | `scripts/p16c-create-withdraw-remote.cjs` (Fly localhost) / `scripts/p16c-retry-homolog.cjs` fase `create-withdraw` |

**Restrições respeitadas:**

- ❌ PIX OUT não aberto  
- ❌ `approve-and-send` não executado  
- ❌ `/v3/transfers` não chamado  
- ❌ Saque P1.5CERT (`94480975-…`) não tocado  
- ❌ Rota legada `paymentController` não usada  

---

## 7. Provider Asaas — compatibilidade

| Verificação | Resultado |
|-------------|-----------|
| Coluna `asaas_transfer_id` | **null** — transferência ainda não criada |
| `PAYOUT_PROVIDER` no Fly | **unset** (gates OFF) |
| Próximo execute homolog | `PAYOUT_PROVIDER=asaas` na janela única |
| `net_amount` enviável ao Asaas | **R$ 8,00** (`POST /v3/transfers` `value`) |
| Saldo Asaas informado | ≈ **R$ 10,19** — cobre R$ 8 líquido + margem |

---

## 8. Gates e infra (snapshot preparação)

| Gate | Estado |
|------|--------|
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | **true** |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` | configurado |
| `ASAAS_PRODUCTION_ENABLED` | **false** |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | **false** |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | **false** |
| `PAYOUT_PIX_ENABLED` | **false** |
| `ENABLE_PIX_PAYOUT_WORKER` | **false** |
| `PAYOUT_PROVIDER` | unset |

---

## 9. Saques não elegíveis (inalterados)

| `saque_id` | Motivo |
|------------|--------|
| `94480975-0051-4ede-968c-275abff07a19` | `aguardando_confirmacao` + `asaas_transfer_id` já existe (P1.5CERT) |
| `a705bb8c-5f90-4532-9f55-d167dbdff672` | `falhou` / rollback |
| Saques cancelados conta técnica | Histórico — não reutilizados |

---

## 10. Auditoria de elegibilidade

**Script:** `node scripts/p16b-r3-audit-saques.mjs`  
**Esperado após esta preparação:**

```
###P16B_R3_VERDICT###{"result":"READY","recommendedSaqueId":"d0313dfe-b08a-4334-a3ca-d9e7011af38a"}
```

**Variável para homologação:**

```bash
export P16B_SAQUE_ID=d0313dfe-b08a-4334-a3ca-d9e7011af38a
# ou
export P15Y_SAQUE_ID=d0313dfe-b08a-4334-a3ca-d9e7011af38a
```

---

## 11. Próximo passo — P1.6C homologação

1. Confirmar painel Asaas: URL `https://goldeouro-backend-v2.fly.dev/webhooks/asaas/transfer-validation` + token Fly  
2. `node scripts/p16b-r3-audit-saques.mjs` → `READY`  
3. Abrir janela gates (uma tentativa)  
4. `P15Y_PHASE=execute P15Y_ALLOW_REAL_PAYOUT=1 P15Y_SAQUE_ID=d0313dfe-b08a-4334-a3ca-d9e7011af38a`  
5. Capturar logs `[ASAAS][TRANSFER_AUTH]` → `APPROVED`  
6. Fechar gates imediatamente  

---

## 12. Artefatos

| Arquivo | Função |
|---------|--------|
| `scripts/p16c-prep-saque-tecnico.mjs` | Snapshot / create / audit preparação |
| `scripts/p16c-audit-only.cjs` | Auditoria read-only pós-criação |
| `scripts/p16c-create-withdraw-remote.cjs` | Criação via localhost Fly (oficial) |
| `scripts/p16b-r3-audit-saques.mjs` | Elegibilidade homologação |
| `tmp/p16c-retry-state.json` | Evidência HTTP 201 criação |

---

## 13. Conclusão

| Item | Resultado |
|------|-----------|
| Veredito P1.6C.PREP | **READY** |
| Saque técnico pendente | ✅ `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |
| Homologação auth + PIX OUT | ⏸️ Pronta para janela P1.6C |
| Gates financeiros | ✅ Não abertos nesta preparação |

**READY** — saque técnico único pendente preparado para homologação final do Webhook de Autorização Asaas.
