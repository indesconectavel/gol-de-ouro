# P1.3K — Verificação pós `fly secrets set ASAAS_API_KEY` (produção)

**Projeto:** Gol de Ouro™ Backend  
**Data:** 2026-06-28  
**App Fly:** `goldeouro-backend-v2`  
**Ação:** `fly secrets set ASAAS_API_KEY='…'` com aspas simples (chave `$aact_prod_…`)

---

## Veredito

# PASS — `ASAAS_API_KEY` injetada corretamente no runtime

---

## Resumo executivo

| Verificação | P1.3J (falha) | P1.3K (agora) |
|-------------|:-------------:|:-------------:|
| Digest Fly `ASAAS_API_KEY` | `49bb8a67b557e235` | **`31a416b0a978b2d5`** ✅ |
| Runtime `length` | 0 | **166** ✅ |
| `empty` | true | **false** ✅ |
| `isProdPrefix` (`$aact_prod_`) | false | **true** ✅ |
| `sha256Prefix` | null | **`915bffe48d753013`** ✅ |
| App PID vs SSH | match (vazio) | **match (166)** ✅ |
| Máquinas rolling update | v476 | **sucesso** (2/2 healthy) |

---

## 1. Operação executada

```bash
fly secrets set ASAAS_API_KEY='<chave $aact_prod_…>' -a goldeouro-backend-v2
```

- Nome correto no Fly: **`ASAAS_API_KEY`** (não `ASAAS_API_KEY_PRODUCTION` — este nome não é lido pelo código)
- Aspas **simples** no PowerShell evitaram expansão do `$` no prefixo da chave

**Resultado Fly:**

```
✔ Machine 784e047bd04e08 [payout_worker] update succeeded
✔ Machine 080e207b071048 [app] update succeeded
```

---

## 2. Probe runtime (sem expor segredo)

Máquina app `080e207b071048` — 2026-06-28T21:09:25Z:

```json
{
  "probe": "P1.3K-post-secret-set",
  "sshNodeProcess": {
    "exists": true,
    "length": 166,
    "empty": false,
    "sha256Prefix": "915bffe48d753013",
    "isProdPrefix": true,
    "isSandboxPrefix": false
  },
  "appProcesses": [{
    "pid": 663,
    "cmdline": "node server-fly.js",
    "diag": {
      "length": 166,
      "empty": false,
      "sha256Prefix": "915bffe48d753013",
      "isProdPrefix": true
    }
  }],
  "processesMatch": true,
  "verdict": "PASS"
}
```

Processo HTTP real (`node server-fly.js`) e ambiente SSH compartilham a **mesma** chave (length e SHA-256 prefix idênticos).

---

## 3. Estado Asaas no Fly (flags — inalteradas)

| Variável | Estado |
|----------|--------|
| `ASAAS_ENABLED` | `true` |
| `ASAAS_ENV` | `production` |
| `ASAAS_PIX_IN_ENABLED` | `true` |
| `ASAAS_WEBHOOK_ENABLED` | `true` |
| `ASAAS_PRODUCTION_ENABLED` | **`false`** (gate financeiro fechado) |
| `ASAAS_API_KEY` | **presente, produção** ✅ |

PIX IN real e cobranças produção permanecem bloqueados por **`ASAAS_PRODUCTION_ENABLED=false`** até abertura controlada do gate.

---

## 4. Próximos passos sugeridos (operador)

1. **Segurança:** a chave foi compartilhada em canal de chat — considerar rotação no painel Asaas se política exigir.
2. Reexecutar **P1.3R** (PIX IN real gate controlado) ou probe API `api.asaas.com` read-only.
3. Validar webhook produção no painel Asaas (token = `ASAAS_WEBHOOK_TOKEN`).
4. Abrir `ASAAS_PRODUCTION_ENABLED=true` somente quando autorizado pelo runbook de homologação.

---

## 5. Conformidade

| Item | Status |
|------|:------:|
| Valor da chave no relatório | **Não** |
| `ASAAS_PRODUCTION_ENABLED` alterado | **Não** |
| Gate financeiro aberto | **Não** |
| Código alterado | **Não** |

---

**Fim do relatório P1.3K**
