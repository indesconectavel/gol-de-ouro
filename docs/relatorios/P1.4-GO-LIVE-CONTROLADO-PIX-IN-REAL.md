# P1.4 — GO-LIVE Controlado PIX IN Real (Asaas Produção)

**Projeto:** Gol de Ouro™ Backend  
**Data:** 2026-06-28  
**App Fly:** `goldeouro-backend-v2`  
**Máquina app:** `080e207b071048`  
**Script operacional:** `scripts/p14-go-live-pix-in-real.cjs`

---

## Resultado atual

# PASS COM RESSALVAS — ETAPAS 1–4 concluídas · ETAPAS 5–7 aguardando pagamento manual

| Etapa | Status |
|-------|--------|
| 1 — Auditoria flags/secrets | **PASS** |
| 2 — Abertura de gates | **PASS** |
| 3 — Cobrança PIX real (R$ 5,00) | **PASS** (recuperada após ajuste URL) |
| 4 — Pausa para pagamento manual | **AGUARDANDO OPERADOR** |
| 5 — Validação cadeia financeira | **PENDENTE** |
| 6 — Replay webhook / idempotência | **PENDENTE** |
| 7 — Encerramento | **PENDENTE** |

---

## ETAPA 1 — Auditoria (pré-gates)

Probe runtime Fly `080e207b071048` — 2026-06-28T21:16Z:

| Variável | Estado |
|----------|--------|
| `ASAAS_API_KEY` | `length=166`, `$aact_prod_`, sha256 `915bffe48d753013` |
| `ASAAS_WEBHOOK_TOKEN` | `length=49`, sha256 `0b616779f93e7474` |
| `ASAAS_ENABLED` | `true` |
| `ASAAS_ENV` | `production` |
| `ASAAS_PIX_IN_ENABLED` | `true` |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | `true` |
| `ASAAS_WEBHOOK_ENABLED` | `true` |
| `ASAAS_PRODUCTION_ENABLED` | **`false`** (antes da ETAPA 2) |
| `NODE_ENV` | `production` |

**Veredito ETAPA 1:** PASS — único bloqueio era gate financeiro fechado.

---

## ETAPA 2 — Gates abertos

### Flags alteradas

| Flag | Antes | Depois | Motivo |
|------|-------|--------|--------|
| **`ASAAS_PRODUCTION_ENABLED`** | `false` | **`true`** | Gate financeiro PIX IN + crédito webhook produção |
| **`ASAAS_BASE_URL`** | *(ausente)* | **`https://api.asaas.com/v3`** | URL produção correta (default código era `/api/v3` → HTTP 404) |

### Flags já corretas (não alteradas)

`ASAAS_ENABLED`, `ASAAS_ENV`, `ASAAS_PIX_IN_ENABLED`, `ASAAS_WEBHOOK_ENABLED`, `PAYMENT_WEBHOOK_ENGINE_ENABLED`, `ASAAS_API_KEY`, `ASAAS_WEBHOOK_TOKEN`

### Deploy associado

- **v477+** — `fly deploy` com wire P1.3F (PIX IN HTTP produção) — imagem `deployment-01KW81ZZCJM4GDBPBF34S664G0`
- Rolling update 2/2 máquinas healthy

### Correção código (pós-auditoria, repo local)

`DEFAULT_PRODUCTION_BASE_URL` em `asaas-config.js`: `https://api.asaas.com/v3` (alinha com docs Asaas; `ASAAS_BASE_URL` no Fly cobre até próximo deploy).

---

## ETAPA 3 — Cobrança PIX real (UMA)

| Campo | Valor |
|-------|-------|
| **provider** | `asaas` |
| **ambiente** | `production` |
| **valor** | R$ **5,00** |
| **payment_id** | `pay_1zr8bxbd5abypkws` |
| **customer_id** | `cus_000184160552` |
| **status** | `PENDING` |
| **external_reference** | `goldeouro_p14_85872488-9e4c-42df-8978-7f9ef9f5cb00_1782682434603` |
| **pagamentos_pix_id** | `06bcc936-ce25-4474-a810-14d873448e75` |
| **usuario_id** | `85872488-9e4c-42df-8978-7f9ef9f5cb00` (conta técnica E2E) |
| **saldo_antes** | `166545` (unidade interna do banco) |
| **expiration (QR)** | `2027-06-28 23:59:59` |
| **invoice_url** | https://www.asaas.com/i/1zr8bxbd5abypkws |

### Ressalvas na criação

1. Primeira tentativa falhou: URL default `https://api.asaas.com/api/v3` → **404** em `POST /customers`.
2. Corrigido via secret `ASAAS_BASE_URL=https://api.asaas.com/v3`.
3. Segunda tentativa criou cobrança mas QR retornou **400** na primeira chamada imediata (`invalid_action`/timing); QR disponível após ~1s (comportamento observado).
4. **Uma única cobrança** no Asaas — recuperada via `P14_PHASE=recover` (sem segunda cobrança).

---

## ETAPA 4 — PAUSA — Pagamento manual

**Ação do operador:** pagar **R$ 5,00** via PIX usando uma das opções abaixo.

### Opção A — Fatura Asaas (recomendado)

https://www.asaas.com/i/1zr8bxbd5abypkws

### Opção B — Pix Copia e Cola

```
00020101021226800014br.gov.bcb.pix2558pix.asaas.com/qr/cobv/b24328a1-9e00-4bdf-b9b6-4b676de74d7f5204000053039865802BR592348487173 FREDERICO SANT6013Montes Claros61083940054662070503***63049C55
```

### Após pagar

Avisar para executar **ETAPAS 5–7**:

```bash
# Na máquina Fly (ou via fly ssh):
cd /app && P14_PHASE=validate node p14-temp.cjs
cd /app && P14_PHASE=replay node p14-temp.cjs
```

Estado salvo em `/tmp/p14-go-live-state.json` na máquina app.

---

## ETAPAS 5–7 — Pendentes

### ETAPA 5 — Validar cadeia (após pagamento)

```
Asaas (PAYMENT_RECEIVED)
  → POST /webhooks/asaas
  → validateAsaasWebhook
  → normalizeAsaasPaymentWebhook
  → processPaymentWebhook
  → checkSupabaseDepositIdempotency
  → claimAndCreditApprovedPixDeposit (RPC)
  → Wallet (usuarios.saldo)
  → Ledger (ledger_financeiro)
```

### ETAPA 6 — Replay webhook

Reenviar mesmo evento → esperado: `idempotent=true`, saldo estável, sem ledger duplicado.

### ETAPA 7 — Encerramento

- **Não** executar PIX OUT.
- Documentar flags finais.

---

## Flags — estado atual e rollback sugerido

### Permanecem abertas (operacional)

| Flag | Valor atual |
|------|-------------|
| `ASAAS_PRODUCTION_ENABLED` | **`true`** |
| `ASAAS_BASE_URL` | `https://api.asaas.com/v3` |
| Demais flags Asaas | conforme P1.3K |

### Devem voltar após homologação completa (ETAPAS 5–7 PASS)

| Flag | Rollback sugerido |
|------|-------------------|
| `ASAAS_PRODUCTION_ENABLED` | **`false`** |
| `ASAAS_BASE_URL` | manter `https://api.asaas.com/v3` (correção permanente) ou remover se código deployado com fix |

Comando rollback gate financeiro:

```bash
fly secrets set ASAAS_PRODUCTION_ENABLED=false -a goldeouro-backend-v2
```

---

## Prontidão P1.5 (PIX OUT Real)

| Critério | Status |
|----------|--------|
| PIX IN real criado | ✅ |
| Webhook + crédito + idempotência homologados | ❌ pendente ETAPA 5–6 |
| `ASAAS_PRODUCTION_ENABLED` estável | ⚠️ aberto temporariamente |
| PIX OUT | **NÃO iniciar** até P1.4 PASS final |

**P1.5:** **NÃO PRONTO** — aguardar conclusão P1.4 ETAPAS 5–7.

---

## Conformidade

| Item | Status |
|------|:------:|
| Arquitetura alterada | **Não** |
| Mercado Pago legado alterado | **Não** |
| PIX OUT executado | **Não** |
| Múltiplas cobranças | **Não** (1 cobrança Asaas) |
| Supabase | insert `pagamentos_pix` pendente (fluxo normal) |

---

## Ressalvas consolidadas

1. Deploy P1.3F necessário (imagem anterior usava stub).
2. URL produção corrigida via `ASAAS_BASE_URL` + fix em `asaas-config.js` local.
3. QR PIX pode exigir retry (~1s) após criação da cobrança.
4. Validação financeira completa **bloqueada** até pagamento manual.

---

**Fim do relatório P1.4 (parcial — ETAPA 4)**
