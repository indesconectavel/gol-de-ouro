# P1.0D — Deploy Controlado do Wire Asaas Produção

**Projeto:** Gol de Ouro™ Backend  
**Data:** 2026-06-28  
**Gate:** `ASAAS_PRODUCTION_ENABLED=false` — **NÃO alterado**  
**Resultado:** **PASS**

---

## 1. Objetivo

Promover para produção o wire Asaas (Payment Engine F4.5 / crédito gated P1.0) **sem abrir go-live financeiro**.

---

## 2. Resumo executivo

| Etapa | Resultado |
|-------|-----------|
| Revisão diff / escopo | **PASS** (commit isolado P1.0) |
| Testes locais (3 scripts) | **PASS** |
| Commit | **PASS** — `fdfcd8a` |
| Deploy Fly | **PASS** (2ª tentativa, após correção boot) |
| Validação produção | **PASS** |
| Gate Asaas produção | **OFF** (inalterado) |

---

## 3. Escopo do commit

**Commit:** `fdfcd8aa1550a759fd916b253132c7379c017d17`  
**Branch:** `chore/f2-4e-2-mp-log`  
**Mensagem:** `feat(finance): deploy P1.0 wire Asaas produção gated`

### 3.1 Arquivos incluídos (34)

| Área | Arquivos |
|------|----------|
| Wire HTTP | `server-fly.js` — `POST /webhooks/asaas` + deps Supabase |
| Engine | `src/finance/webhooks/processPaymentWebhook.js`, `processPaymentWebhookCompat.js`, `payment-webhook-config.js` |
| Asaas | `src/finance/providers/asaas/*` (validator, normalizer, config) |
| MP webhook | `mercadopago-webhook-validator.js`, `mercadopago-webhook-normalizer.js` |
| Factory / deps boot | `FinanceProviderFactory.js`, contracts, MercadoPago/Asaas/Celcoin providers |
| Verificação | `verify-asaas-production-webhook-wire.mjs`, `verify-payment-webhook-engine.mjs`, `verify-payment-webhook-controlled-credit.mjs` |
| Docs | `.env.example`, runbook, checklist P1.0, relatório P1.0 |

### 3.2 Excluídos do commit (fora de escopo)

- `controllers/adminWithdrawController.js` (alteração local F2 — path `finance/` incorreto)
- `goldeouro-player/**`, patches SQL, docs operacionais não-P1.0
- `.cursor/mcp.json`, backups, artefatos temporários

---

## 4. Testes pré-deploy

| Script | Resultado |
|--------|-----------|
| `verify-asaas-production-webhook-wire.mjs` | **PASS** |
| `verify-payment-webhook-engine.mjs` | **PASS** |
| `verify-payment-webhook-controlled-credit.mjs` | **PASS** |

Casos críticos verificados localmente:

- `ASAAS_PRODUCTION_ENABLED` default OFF
- Produção sem gate → rota bloqueada (`blocked_prod` / `asaas_webhook_blocked`)
- Crédito real só com gate ON + claim handler
- F4.6 controlled credit inalterado em sandbox

---

## 5. Deploy Fly

| Item | Valor |
|------|-------|
| App | `goldeouro-backend-v2` |
| Comando | `flyctl deploy --remote-only --no-cache --build-arg GIT_COMMIT=fdfcd8a…` |
| Imagem final | `deployment-01KW6XVWT1NQMZ6FCZF37D7YR2` |
| Release | v467 (aprox.) |

### 5.1 Incidente — 1ª tentativa (resolvido)

A 1ª imagem incluiu **working tree não commitado** (`adminWithdrawController.js` referenciando `../finance/compat/createPixWithdrawCompat` inexistente). Resultado:

- App `080e207b071048` crash loop (`MODULE_NOT_FOUND`)
- `/health` → 502 (~3 min)

**Correção:** revert local de `adminWithdrawController.js` para HEAD + redeploy. **Sem alteração de gate/secrets.**

---

## 6. Validação produção (pós-redeploy)

**Timestamp probes:** 2026-06-28T10:59:15Z

### 6.1 `GET /health`

```json
{"status":"ok","database":"connected","mercadoPago":"connected","contadorChutes":535}
```

**HTTP 200 — PASS**

### 6.2 `GET /meta`

```json
{"gitCommit":"fdfcd8aa1550a759fd916b253132c7379c017d17","environment":"production"}
```

**Commit reflete deploy P1.0 — PASS**

### 6.3 `POST /webhooks/asaas`

```json
{"error":"payment_webhook_engine_disabled"}
```

**HTTP 404 — PASS (protegido)**

Interpretação: `PAYMENT_WEBHOOK_ENGINE_ENABLED=false` em produção → rota existe mas **recusa** antes de qualquer crédito. Equivalente seguro a `blocked_prod` / gate fechado.

### 6.4 Gate financeiro

| Flag | Estado esperado | Confirmado |
|------|-----------------|------------|
| `ASAAS_PRODUCTION_ENABLED` | `false` | ✅ Não alterado no deploy |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | `false` (prod) | ✅ Evidência: resposta acima |
| Crédito real executado | Nenhum | ✅ Engine OFF impede processamento |

---

## 7. Critérios PASS

| Critério | OK |
|----------|:--:|
| Wire Asaas commitado | ✅ |
| Deploy Fly concluído | ✅ |
| `/meta` reflete novo commit | ✅ `fdfcd8a` |
| `/health` PASS | ✅ |
| Produção protegida | ✅ `payment_webhook_engine_disabled` |
| `ASAAS_PRODUCTION_ENABLED=false` | ✅ |
| Nenhum crédito real executado | ✅ |

---

## 8. Veredito

## **PASS**

Wire Asaas em produção, gate financeiro **fechado**. Próximo passo operacional (quando autorizado): checklist `P1.0-PRODUCTION-GO-LIVE-CHECKLIST.md` §4 — habilitar flags **sem** abrir `ASAAS_PRODUCTION_ENABLED` até §4.7.

---

## 9. Ressalvas

1. **Docker `COPY . .`** inclui working tree local — garantir árvore limpa ou `.dockerignore` antes de deploys futuros.
2. Branch `chore/f2-4e-2-mp-log` **não mergeada em `main`** — deploy feito direto via `flyctl` local.
3. Para probe 403 `asaas_webhook_blocked` (em vez de 404 engine disabled), operador deve setar `PAYMENT_WEBHOOK_ENGINE_ENABLED=true` **mantendo** `ASAAS_PRODUCTION_ENABLED=false` (fase intermediária do runbook §4.3–4.6).
