# P1.3I — Auditoria de Injeção Runtime `ASAAS_API_KEY`

**Projeto:** Gol de Ouro™ Backend  
**Data:** 2026-06-28  
**Modo:** READ-ONLY ABSOLUTO — nenhum código, secret, deploy ou gate alterado  
**App Fly:** `goldeouro-backend-v2` · máquina app `080e207b071048`

---

## Veredito

# SECRET VAZIO / DIGEST INCONCLUSIVO

O digest Fly (`49bb8a67b557e235`) **não comprova** que o vault contenha chave não vazia. A plataforma Fly documenta o digest apenas como *change hint* (checksum truncado, keyed, colisionável — **não** é SHA-256 nem MD5 confiável do plaintext).

Evidência runtime em **três camadas independentes** (processo SSH, `/proc/self/environ`, processo principal `node server-fly.js` PID 664 via `/proc/$PID/environ`):

| Camada | `exists` | `length` | `empty` | `sha256Prefix` |
|--------|:--------:|:--------:|:-------:|:--------------:|
| Processo SSH (probe Node) | true | **0** | true | null |
| `/proc/self/environ` (SSH) | true | **0** | true | null |
| Processo app PID 664 (`server-fly.js`) | true | **0** | true | null |

**Conclusão:** a injeção Fly entrega `ASAAS_API_KEY=""` (string vazia presente, não `undefined`). Não há sobrescrita em código nem `.env` no container. O probe **não** está incorreto — o processo HTTP real compartilha o mesmo ambiente vazio.

---

## Resumo executivo

| Pergunta | Resposta |
|----------|----------|
| Por que digest Fly ≠ vazio aparente? | Digest Fly é **inconclusivo** para inferir conteúdo |
| Runtime recebe valor? | **Não** — `length=0` em todos os processos auditados |
| SSH lê ambiente diferente do app? | **Não** — PID 664 (app) = probe SSH |
| Código sobrescreve a chave? | **Não** — zero atribuições encontradas |
| `.env` / dotenv explicam? | **Não** — `/app/.env` ausente na imagem |
| Causa raiz provável | **Gravação histórica** `fly secrets set ASAAS_API_KEY=` (ou expansão shell vazia) |

---

## 1. Escopo e método

Auditoria read-only cobrindo:

1. Pontos de carga `dotenv` / config / env no boot de produção  
2. Busca por sobrescritas de `ASAAS_API_KEY`  
3. Confirmação do ambiente do processo Node principal vs `fly ssh console`  
4. Probe remoto sem expor segredo (`exists`, `length`, `sha256Prefix`, `empty`)  
5. Comparação digest Fly × SHA-256 runtime × string vazia  

Nenhum secret foi lido, logado ou alterado.

---

## 2. Cadeia de boot no Fly

```
fly.toml [processes] app = "npm start"
  → package.json "start": "node server-fly.js"
    → server-fly.js (PID observado: 664)
```

| Artefato | Conteúdo relevante |
|----------|-------------------|
| `fly.toml` `[env]` | Apenas `NODE_ENV=production` — **sem** `ASAAS_API_KEY` |
| `fly.toml` `[processes]` | `app = "npm start"` · `payout_worker = "node src/workers/payout-worker.js"` |
| `Dockerfile` `CMD` | `node server-fly.js` (sobrescrito pelo processo Fly `npm start`) |
| `.dockerignore` | Whitelist estrita — **`.env` não entra na imagem** |
| Secrets Fly | Injetados pela plataforma no ambiente da máquina |

O worker `payout_worker` herda os mesmos secrets da máquina; com `ENABLE_PIX_PAYOUT_WORKER=true` e provider Asaas, `payout-worker.js` L36–38 exige `ASAAS_API_KEY` truthy — comportamento consistente com chave vazia.

---

## 3. Pontos de carga dotenv / config

### 3.1 `server-fly.js` (boot principal)

```57:64:server-fly.js
require('dotenv').config();

// Validação das variáveis de ambiente obrigatórias
const { assertRequiredEnv, isProduction } = require('./config/required-env');
assertRequiredEnv(
  ['JWT_SECRET', 'SUPABASE_URL', 'SUPABASE_SERVICE_ROLE_KEY'],
  { onlyInProduction: ['MERCADOPAGO_DEPOSIT_ACCESS_TOKEN'] }
);
```

| Observação | Impacto |
|------------|---------|
| `dotenv.config()` na L57, **após** vários `require()` no topo | Irrelevante em Fly: não há `.env` no container |
| `dotenv@16.x` — `override` default `false` | Não sobrescreveria secret Fly mesmo se `.env` existisse com valor diferente |
| `assertRequiredEnv` | **Não** lista `ASAAS_API_KEY` |

**Probe runtime:** `dotenvFileExists: false`, `dotenvLocalExists: false` na máquina `080e207b071048`.

### 3.2 `src/finance/providers/asaas/asaas-config.js`

Único ponto de leitura operacional da chave no código de produção:

```18:24:src/finance/providers/asaas/asaas-config.js
function readEnv(name) {
  const value = process.env[name];
  if (value == null || String(value).trim() === '') {
    return null;
  }
  return String(value).trim();
}
```

```106:116:src/finance/providers/asaas/asaas-config.js
function getAsaasApiKeyDiagnostics() {
  const raw = process.env.ASAAS_API_KEY;
  const trimmed = raw == null ? '' : String(raw).trim();
  return {
    present: raw != null,
    length: trimmed.length,
    empty: trimmed.length === 0,
    sha256Prefix:
      trimmed.length > 0 ? crypto.createHash('sha256').update(trimmed).digest('hex').slice(0, 16) : null
  };
}
```

- `getAsaasConfig()` → `apiKey: readEnv('ASAAS_API_KEY')`  
- Sem alias (`ASAAS_TOKEN`, etc.) no repositório de produção  
- Imagem deployada atual **não** exporta `getAsaasApiKeyDiagnostics` como função standalone (deploy anterior a P1.3F); diagnóstico foi feito via probe direto em `process.env` e `/proc`

### 3.3 Outros `dotenv.config()` no repositório

| Local | Usado no boot Fly? |
|-------|:------------------:|
| `server-fly.js` L57 | ✅ |
| `scripts/*.mjs` (vários) | ❌ não estão na imagem Docker |
| `scripts/certification/*.js` | ❌ |
| `config/env.js` (envalid) | ❌ não importado por `server-fly.js` |

**Veredito seção 3:** nenhum loader de `.env` ativo no container; leitura unificada em `process.env.ASAAS_API_KEY`.

---

## 4. Busca por sobrescritas

### 4.1 Atribuições diretas

```
grep process.env.ASAAS_API_KEY =  → 0 ocorrências
grep ASAAS_API_KEY=               → 0 em fly.toml / Dockerfile / workflows
grep Object.assign(process.env    → 0 no código de produção
grep delete process.env.ASAAS     → 0
grep dotenv override:true         → 0 em server-fly / boot
```

### 4.2 Vetores indiretos descartados

| Vetor | Evidência |
|-------|-----------|
| `fly.toml [env]` | Só `NODE_ENV` |
| `.env` local (dev) | Existe no workspace dev com chave **sandbox** — **não** copiado para imagem |
| `config/env.js` (envalid) | Não referencia `ASAAS_API_KEY`; não carregado no boot |
| `config/required-env.js` | Não valida `ASAAS_API_KEY` |
| Endpoints `/health`, `/meta` | Não expõem nem alteram `ASAAS_API_KEY` |

**Veredito seção 4:** **nenhuma sobrescrita em runtime** identificada no código ou configuração de deploy.

---

## 5. Ambiente do processo real vs `fly ssh console`

### 5.1 Hipótese testada

`fly ssh console` poderia executar Node em ambiente diferente do processo HTTP long-running, explicando divergência digest × probe.

### 5.2 Probe P1.3I (2026-06-28T20:28:10Z)

Executado na máquina `080e207b071048` via `fly ssh console` — probe read-only comparando:

- `process.env` do processo SSH  
- `/proc/self/environ` (kernel, processo SSH)  
- `/proc/664/environ` do processo **`node server-fly.js`** (app HTTP real)

```json
{
  "probe": "P1.3I",
  "hostname": "080e207b071048",
  "flyMachine": "080e207b071048",
  "flyApp": "goldeouro-backend-v2",
  "dotenvFileExists": false,
  "emptyStringSha256Prefix": "e3b0c44298fc1c14",
  "sshNodeProcess": { "exists": true, "length": 0, "empty": true, "sha256Prefix": null },
  "procSelfEnviron": { "exists": true, "length": 0, "empty": true, "sha256Prefix": null },
  "appProcesses": [{
    "pid": 664,
    "cmdline": "node server-fly.js ",
    "diag": { "exists": true, "length": 0, "empty": true, "sha256Prefix": null }
  }],
  "processesMatch": true
}
```

| Comparação | Resultado |
|------------|-----------|
| SSH Node vs `/proc/self/environ` | **Idêntico** |
| SSH vs app PID 664 | **Idêntico** (`processesMatch: true`) |
| Probe incorreto? | **Descartado** |

A leitura em `/proc/$PID/environ` é evidência de **kernel** — anterior a qualquer lógica Node/dotenv do app.

---

## 6. Comparação digest Fly × SHA-256 runtime

### 6.1 `fly secrets list` (2026-06-28)

| Secret | Digest | Status |
|--------|--------|--------|
| `ASAAS_API_KEY` | `49bb8a67b557e235` | Deployed |
| `ASAAS_WEBHOOK_TOKEN` | `afb23cbcee1dcd5b` | Deployed (length=49 no runtime — secret **não** vazio) |
| `ASAAS_ENABLED` / flags `true` | `d8c5ac2e11c8e492` | Deployed |
| `ASAAS_PRODUCTION_ENABLED` (`false`) | `fa61a13817d73a23` | Deployed |

Digest estável desde P1.1B — **não mudou** em P1.3G (que não incluiu `ASAAS_API_KEY`).

### 6.2 SHA-256 prefix (referência local — não é algoritmo Fly)

| Material | SHA-256 prefix (16 hex) |
|----------|-------------------------|
| String vazia `""` | `e3b0c44298fc1c14` |
| Runtime `ASAAS_API_KEY` | **null** (length=0) |
| `"true"` | `b5bea41b6c623f7c` (≠ digest Fly `d8c5ac2e…`) |
| `"false"` | `fcbcf165908dd18a` (≠ digest Fly `fa61a138…`) |
| `"production"` | `ab8e18ef4ebebedd` (≠ digest Fly `a3311021…`) |

**Conclusão:** o digest Fly **não** é SHA-256 truncado do valor. Documentação/comunidade Fly: digest é *change hint* keyed e colisionável — **não usar para verificar conteúdo**.

### 6.3 Tabela comparativa final

| Fonte | Indica valor não vazio? | Confiável para conteúdo? |
|-------|:-----------------------:|:------------------------:|
| Digest Fly `49bb8a67…` | Aparentemente sim (enganoso) | **Não** |
| `process.env` app PID 664 | **Não** (length=0) | **Sim** |
| `/proc/664/environ` | **Não** (length=0) | **Sim** |
| SHA-256 prefix runtime | null (= vazio) | **Sim** |
| SHA-256 prefix `""` | `e3b0c44298fc1c14` | Referência |

Contraste útil: `ASAAS_WEBHOOK_TOKEN` tem digest **e** `length=49` no runtime — prova que secrets **podem** injetar corretamente; o problema é **específico** de `ASAAS_API_KEY`.

---

## 7. Matriz de hipóteses

| Hipótese | Veredito P1.3I |
|----------|----------------|
| SECRET OK / PROBE INCORRETO | **Descartada** — app PID 664 confirma vazio em `/proc` |
| SECRET OK / SOBRESCRITA EM RUNTIME | **Descartada** — sem código/config que sobrescreva |
| SECRET VAZIO / DIGEST INCONCLUSIVO | **Confirmada** |
| FALHA NÃO IDENTIFICADA | **Descartada** |
| Gravação histórica vazia no Fly (`ASAAS_API_KEY=` / shell) | **Causa raiz provável** (herdada de P1.3H, reforçada aqui) |

---

## 8. Linha do tempo

| Data | Evento | `ASAAS_API_KEY` runtime |
|------|--------|-------------------------|
| P1.1B | Primeira detecção | length=0 |
| P1.2 / P1.3 / P1.3R | Probes PIX/webhook | length=0 |
| P1.3G | Flags Asaas (sem tocar chave) | length=0 |
| P1.3H | Diagnóstico `/proc/self/environ` | length=0 |
| **P1.3I** | Probe PID app 664 vs SSH | **length=0 (confirmado)** |

---

## 9. Remediação sugerida (operador — fora escopo P1.3I)

1. Re-set explícito com chave produção literal (evitar expansão shell vazia):
   ```bash
   fly secrets set ASAAS_API_KEY='COLE_CHAVE_$aact_prod_...' -a goldeouro-backend-v2
   ```
2. Validar pós-deploy com probe read-only (length > 0, `sha256Prefix` não null — **sem** imprimir valor).
3. Confirmar digest **mudou** após re-set (único uso legítimo do digest).
4. Reexecutar P1.3R / homologação PIX IN.

---

## 10. Conformidade da auditoria

| Verificação | Status |
|-------------|:------:|
| Código alterado | **Não** |
| Secrets alterados | **Não** |
| Deploy / gates alterados | **Não** |
| Valores de secrets exibidos | **Não** |
| Probe temporário remoto | Executado em `/tmp`, não persistido na imagem |

---

**Fim do relatório P1.3I**
