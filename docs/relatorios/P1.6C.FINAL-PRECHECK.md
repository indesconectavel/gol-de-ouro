# P1.6C.FINAL-PRECHECK — Última Auditoria Antes da Homologação Final

**Projeto:** Gol de Ouro™ V1  
**Data:** 2026-06-30  
**Modo:** READ-ONLY — sem código, banco, gates ou PIX alterados  
**App Fly:** `goldeouro-backend-v2`

---

## Veredito

# READY_TO_EXECUTE

---

## Matriz de verificação

| # | Verificação | Resultado | Evidência |
|---|-------------|-----------|-----------|
| 1 | `POST /webhooks/asaas/transfer-validation` ativa | ✅ | Deploy P1.6A.R1; probe P1.6B.R2 **HTTP 200** + `REFUSED` (não 404, não `asaas_transfer_auth_webhook_disabled`); token aceito (`WITHDRAWAL_NOT_FOUND`) |
| 2 | `POST /webhooks/asaas` protegido por `ASAAS_PRODUCTION_ENABLED` | ✅ | Código `server-fly.js` L3455–3459 → **403** `asaas_webhook_blocked`; `ASAAS_PRODUCTION_ENABLED` digest `fa61a138…` = **false** no Fly |
| 3 | Saque `d0313dfe-…` em `pending` | ✅ | Criação oficial **HTTP 201** `2026-06-30T02:03:03Z`; gates PIX OUT **OFF** desde então; sem `approve-and-send` |
| 4 | `net_amount` = **8.00** | ✅ | Resposta API criação: `net_amount: 8`, `fee: 2`, `amount: 10` |
| 5 | `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true` | ✅ | Fly secrets `Deployed` digest `d8c5ac2e…` (= true) — snapshot `2026-06-30T02:03Z` |
| 6 | `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` carregado | ✅ | Fly secrets `Deployed` digest `4d518522…`; 75 chars, prefixo `whsec_p16b_` (valor **não** registrado) |
| 7 | `PAYOUT_PROVIDER` só na janela | ✅ | **Ausente** em `fly secrets list` (snapshot 02:03Z); script homolog define apenas na abertura da janela |
| — | `/health` produção | ✅ | Live `2026-06-30T02:56:49Z` — `database: connected` |

---

## 1. Rota de autorização

**Endpoint:** `POST /webhooks/asaas/transfer-validation`

| Critério | Esperado | Observado |
|----------|----------|-----------|
| Não 404 genérico | ✅ | Rota registrada `server-fly.js` L3481 |
| Não `asaas_transfer_auth_webhook_disabled` | ✅ | Gate ON desde P1.6B.R2 |
| Aceita token | ✅ | Probe P1.6B.R2 com header `asaas-access-token` → **200** `REFUSED` / `WITHDRAWAL_NOT_FOUND` (token válido, saque probe inexistente) |

Sem token (strict mode default): esperado **200** `REFUSED` / `AUTH_TOKEN_HEADER_MISSING` — rota ainda **ativa**.

---

## 2. Webhook financeiro Asaas

**Endpoint:** `POST /webhooks/asaas`

Com `ASAAS_PRODUCTION_ENABLED=false` e runtime produção:

```http
HTTP 403
{ "error": "asaas_webhook_blocked", "message": "…ASAAS_PRODUCTION_ENABLED" }
```

**Esperado e correto** — webhook financeiro separado do auth; PIX IN/out financeiro permanece gated.

---

## 3–4. Saque técnico alvo

| Campo | Valor |
|-------|-------|
| `saque_id` | `d0313dfe-b08a-4334-a3ca-d9e7011af38a` |
| `user_id` | `85872488-9e4c-42df-8978-7f9ef9f5cb00` |
| `correlation_id` | `p16c-360a6de3-8873-4672-9b8c-10c9504fdf62` |
| `status` | `pending` / `pendente` |
| `amount` | 10 |
| `fee` | 2 |
| `net_amount` | **8** |
| `asaas_transfer_id` | **null** (PIX OUT não aberto) |

Fonte: `tmp/p16c-retry-state.json` → `createWithdraw.parsed` (fluxo oficial, sem alteração posterior).

---

## 5–7. Secrets Fly (snapshot operacional)

| Secret | Estado | Digest (evidência) |
|--------|--------|-------------------|
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | Deployed | `d8c5ac2e…` → **true** |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` | Deployed | `4d518522…` (valor omitido) |
| `ASAAS_PRODUCTION_ENABLED` | Deployed | `fa61a138…` → **false** |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | Deployed | `fa61a138…` → **false** |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | Deployed | `fa61a138…` → **false** |
| `PAYOUT_PIX_ENABLED` | Deployed | `fa61a138…` → **false** |
| `ENABLE_PIX_PAYOUT_WORKER` | Deployed | `fa61a138…` → **false** |
| `PAYOUT_PROVIDER` | — | **unset** |

---

## Gates — inalterados nesta auditoria

Nenhum secret financeiro foi aberto ou fechado durante este precheck.

---

## Revalidação local opcional

```powershell
Set-Location "e:\Chute de Ouro\goldeouro-backend"
node scripts/p16c-final-precheck.mjs
# ou
powershell -File tmp\run-p16c-precheck.ps1
```

---

## Conclusão

| Veredito | Motivo |
|----------|--------|
| **READY_TO_EXECUTE** | Rota auth ativa e token configurado; webhook financeiro corretamente gated; saque técnico único pendente R$ 8 líquido; gates PIX OUT OFF; `PAYOUT_PROVIDER` unset |

**Próximo passo:** janela única P1.6C homologação com `P15Y_SAQUE_ID=d0313dfe-b08a-4334-a3ca-d9e7011af38a`.
