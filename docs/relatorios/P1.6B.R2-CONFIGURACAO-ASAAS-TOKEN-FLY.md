# P1.6B.R2 — Configuração Asaas + Token Fly

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** CONFIGURAÇÃO CONTROLADA — SEM PIX OUT  
**App Fly:** `goldeouro-backend-v2`

---

## Veredito

# PASS

Token forte gerado e configurado no Fly. Gate `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true` ativo. Probe de produção com token válido retornou **HTTP 200** + `REFUSED` / `WITHDRAWAL_NOT_FOUND`. Gates financeiros PIX OUT permanecem **OFF**. Nenhum saque executado.

---

## 1. Resumo executivo

| Critério | Status |
|----------|--------|
| Token gerado | ✅ 75 chars, prefixo `whsec_p16b_` |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` no Fly | ✅ Deployed |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true` | ✅ Deployed |
| `/health` | ✅ HTTP 200 |
| `/health/workers` | ✅ payout worker OFF |
| Probe auth + token | ✅ `REFUSED` / `WITHDRAWAL_NOT_FOUND` |
| `ASAAS_PRODUCTION_ENABLED` | ✅ OFF (inalterado) |
| PIX OUT gates | ✅ OFF |
| Saque / PIX real | ❌ Não executado |
| Banco alterado | ❌ Não |

---

## 2. Token gerado

| Campo | Valor |
|-------|-------|
| Variável | `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` |
| Comprimento | 75 caracteres |
| Formato | `whsec_p16b_<64 hex>` |
| SHA256 prefix (evidência) | `d1f12afd77c303c5` |
| Fly digest | `4d518522950d2233` |
| Armazenamento local | `tmp/p16b-r2-auth-token.txt` (gitignored) |

**Segurança:** o token completo **não** está neste relatório. Use o arquivo local ou o secret Fly para configurar o painel Asaas.

---

## 3. Comandos Fly executados

```bash
fly secrets set \
  ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN="<TOKEN>" \
  ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED=true \
  -a goldeouro-backend-v2
```

**Resultado:** `exit_code: 0` — rolling update concluído, machine app healthy.

---

## 4. Health pós-configuração

### `/health`

```
HTTP 200
{"status":"ok","database":"connected","mercadoPago":"connected","version":"1.2.1"}
```

### `/health/workers`

```
HTTP 200
{"payoutWorker":{"enabledByEnv":false,"payoutPixProcessingEnabled":false}}
```

---

## 5. Probe seguro — rota ativa + gate ON

```http
POST https://goldeouro-backend-v2.fly.dev/webhooks/asaas/transfer-validation
Content-Type: application/json
asaas-access-token: <TOKEN_CONFIGURADO>

{
  "type": "TRANSFER",
  "transfer": {
    "id": "trf_p16b_probe_inexistente",
    "status": "PENDING",
    "value": 9,
    "externalReference": "goldeouro-p16b-probe-missing"
  }
}
```

### Resposta

```http
HTTP 200

{
  "status": "REFUSED",
  "refuseReason": "WITHDRAWAL_NOT_FOUND"
}
```

| Validação | Resultado |
|-----------|-----------|
| Rota existe (não é 404 genérico) | ✅ |
| Gate auth ON (não `asaas_transfer_auth_webhook_disabled`) | ✅ |
| Token aceito (não `AUTH_TOKEN_MISMATCH`) | ✅ |
| Negativa por saque inexistente | ✅ `WITHDRAWAL_NOT_FOUND` |
| Sem efeito financeiro | ✅ |

---

## 6. Estado dos gates (pós R2)

| Secret | Digest | Interpretação |
|--------|--------|---------------|
| `ASAAS_TRANSFER_AUTH_WEBHOOK_ENABLED` | `d8c5ac2e…` | **true** |
| `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` | `4d518522…` | configurado |
| `ASAAS_PRODUCTION_ENABLED` | `fa61a138…` | **false** |
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | `fa61a138…` | **false** |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | `fa61a138…` | **false** |
| `PAYOUT_PIX_ENABLED` | `fa61a138…` | **false** |
| `ENABLE_PIX_PAYOUT_WORKER` | `fa61a138…` | **false** |
| `PAYOUT_PROVIDER` | — | **unset** |

---

## 7. Configuração pendente — Painel Asaas

**Ação manual obrigatória** antes da homologação real:

**Caminho:** Integrações → Mecanismos de Segurança → Validação de saque via Webhook

| Campo | Valor |
|-------|-------|
| URL | `https://goldeouro-backend-v2.fly.dev/webhooks/asaas/transfer-validation` |
| Token | **Mesmo valor** de `ASAAS_TRANSFER_AUTH_WEBHOOK_TOKEN` no Fly |
| E-mail erros | E-mail operacional da conta |
| Habilitar | Sim |

Sem esta etapa, o Asaas **não chamará** o endpoint durante PIX OUT real.

Opcional: solicitar ao account manager desativação da **autorização crítica** (SMS).

---

## 8. O que NÃO foi feito (conforme escopo)

- ❌ `ASAAS_PRODUCTION_ENABLED` não aberto
- ❌ Gates PIX OUT não abertos
- ❌ Nenhum saque / PIX OUT
- ❌ Nenhuma migration ou alteração de banco
- ❌ Wallet / ledger intocados

---

## 9. Próximo passo — P1.6B.R3

1. Configurar URL + **mesmo token** no painel Asaas (evidência print)
2. Confirmar habilitação do mecanismo de validação
3. Abrir janela PIX OUT controlada (gates + 1 saque técnico)
4. Observar logs `[ASAAS][TRANSFER_AUTH]` + `TRANSFER_DONE`
5. Fechar todos os gates ao final

---

## 10. Conclusão

| Item | Status |
|------|--------|
| Veredito P1.6B.R2 | **PASS** |
| Backend pronto para receber auth do Asaas | ✅ |
| Painel Asaas configurado | ⏸️ Pendente manual |
| Homologação PIX OUT real | ⏸️ P1.6B.R3 |
