# P1.1B — Reauditoria Asaas Produção via API

**Projeto:** Gol de Ouro™ Backend  
**Data:** 2026-06-28  
**Modo:** READ-ONLY ABSOLUTO — nenhum código, banco, `.env`, deploy, secret ou flag alterado  
**Executor:** reauditoria via `flyctl` + probes live  
**Base:** P1.1A (`NOT READY`) · P1.0D (wire deployado)

---

## Resumo executivo

| Área | Resultado P1.1B |
|------|-----------------|
| Secrets Fly (existência) | **5/5 presentes** |
| `ASAAS_PRODUCTION_ENABLED` | **false** (confirmado runtime) |
| `ASAAS_API_KEY` runtime Fly | **VAZIA** (bloqueador crítico) |
| API Asaas produção | **NÃO VALIDADA** (sem chave utilizável) |
| Backend Fly | **SAUDÁVEL** — wire implantado, gate fechado |
| Webhook `POST /webhooks/asaas` | **403** (protegido, sem crédito) |
| **Veredito P1.1B** | **NOT READY** |

---

## 1. Etapa 1 — Secrets Fly (somente existência / flags)

**Comando:** `fly secrets list -a goldeouro-backend-v2`  
**Autenticação:** `fly auth whoami` → `indesconectavel@gmail.com`  
**Timestamp:** 2026-06-28T14:08Z (aprox.)

### 1.1 Secrets obrigatórios

| Secret | Na lista Fly | Status Fly | Valor exibido |
|--------|:------------:|:----------:|:-------------:|
| `ASAAS_API_KEY` | ✅ | Deployed | **Não** (apenas digest `49bb8a67…`) |
| `ASAAS_WEBHOOK_TOKEN` | ✅ | Deployed | **Não** (digest `7cb91c84…`) |
| `ASAAS_WEBHOOK_ENABLED` | ✅ | Deployed | **Não** |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | ✅ | Deployed | **Não** |
| `ASAAS_PRODUCTION_ENABLED` | ✅ | Deployed | **Não** |

### 1.2 Valores de flags no runtime (máquina app `080e207b071048`)

**Fonte:** `fly ssh console --machine 080e207b071048` + script diagnóstico read-only  
**Timestamp:** 2026-06-28T14:38Z (aprox.)

| Variável | Valor observado | Evidência |
|----------|-----------------|-----------|
| `ASAAS_PRODUCTION_ENABLED` | **`false`** | `printenv` + probe `###DIAG###` |
| `ASAAS_WEBHOOK_ENABLED` | **`true`** | idem |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | **`true`** | idem |
| `ASAAS_WEBHOOK_TOKEN` | **present** (não vazio) | probe `###DIAG###` |
| `ASAAS_ENABLED` | **ausente** (`null`) | probe Fly |
| `ASAAS_ENV` | **ausente** (`null`) | probe Fly |

✅ **`ASAAS_PRODUCTION_ENABLED` permanece `false`** — requisito de segurança atendido.

### 1.3 Achado crítico — `ASAAS_API_KEY`

| Verificação | Resultado |
|-------------|-----------|
| Nome no `fly secrets list` | ✅ Presente |
| Nome no `env` da máquina app | ✅ Presente (`sshEnvHasName=1`) |
| **Comprimento do valor runtime** | **`sshKeyLen=0`** — **VAZIO** |
| Prefixo `$aact_prod_` | **Não detectável** (valor vazio) |

**Interpretação:** o secret está **cadastrado** na plataforma Fly, mas o **valor injetado no runtime da máquina app é vazio**. Não foi possível autenticar na API Asaas produção usando o ambiente Fly.

> **Nota:** digest `49bb8a67b557e235` indica que Fly armazena *algum* valor no vault; a divergência vault × runtime sugere secret corrompido/vazio, deploy incompleto ou necessidade de re-set + restart das máquinas. **Esta auditoria não alterou secrets.**

### 1.4 Nomes ASAAS no shell da máquina app

```
ASAAS_API_KEY
ASAAS_PRODUCTION_ENABLED
ASAAS_WEBHOOK_ENABLED
ASAAS_WEBHOOK_TOKEN
```

---

## 2. Etapa 2 — Probes API Asaas Produção (`api.asaas.com`)

### 2.1 Execução

**Método planejado:** probes `GET` read-only a partir do runtime Fly (chave em `ASAAS_API_KEY`).

**Resultado:** **BLOQUEADO**

```json
{
  "blocker": "ASAAS_API_KEY_EMPTY_ON_FLY_RUNTIME",
  "note": "secret name exists in fly secrets list but runtime value empty"
}
```

### 2.2 Probes obrigatórios

| Endpoint | Executado | HTTP | Resultado |
|----------|:---------:|:----:|-----------|
| `GET /v3/myAccount` | ❌ | — | Bloqueado — sem chave |
| `GET /v3/webhooks` | ❌ | — | Bloqueado — sem chave |
| `GET /v3/pix/addressKeys` | ❌ | — | Bloqueado — sem chave |
| `GET /v3/finance/balance` | ❌ | — | Bloqueado — sem chave |

### 2.3 Controle P1.1A (chave sandbox local — não usada para veredito)

| Probe | HTTP | Mensagem |
|-------|:----:|----------|
| `GET api.asaas.com/v3/myAccount` com chave `$aact_hmlg_` local | **401** | *"A chave de API informada não pertence a este ambiente"* |

Confirma separação de ambientes; **não substitui** validação com chave produção.

---

## 3. Etapa 3 — Compatibilidade com backend produção

### 3.1 Deploy e saúde

| Item | Esperado | Observado | OK |
|------|----------|-----------|:--:|
| Commit ≥ `fdfcd8a` | sim | `fdfcd8aa1550a759fd916b253132c7379c017d17` | ✅ |
| `GET /health` | 200 | **200** — `database=connected`, `mercadoPago=connected` | ✅ |
| `GET /meta` | production | `environment=production` | ✅ |
| Wire Asaas P1.0 | implantado | commit confirma deploy P1.0D | ✅ |

**Timestamps probes HTTP públicos:** 2026-06-28T14:06Z

### 3.2 Webhook — proteção com gate fechado

| Probe | HTTP | Interpretação |
|-------|:----:|---------------|
| `POST /webhooks/asaas` corpo `{"event":"PAYMENT_RECEIVED","id":"p1-1b-probe"}` | **403** | Rota **existe** · engine **ON** (`PAYMENT_WEBHOOK_ENGINE_ENABLED=true`) · gate prod **OFF** → bloqueio esperado (`asaas_webhook_blocked`) |

**Mudança vs P1.1A:** em P1.1A o mesmo probe retornava **404** (`payment_webhook_engine_disabled`). Em P1.1B retorna **403**, coerente com `PAYMENT_WEBHOOK_ENGINE_ENABLED=true` e `ASAAS_WEBHOOK_ENABLED=true` no Fly.

**Crédito financeiro:** nenhum — gate `ASAAS_PRODUCTION_ENABLED=false` + probe sem token válido + evento mínimo.

### 3.3 Divergências backend ↔ conta (inalteradas desde P1.1A)

| Item | Status |
|------|--------|
| `ASAAS_ENABLED` / `ASAAS_ENV=production` no Fly | **Ausentes** |
| PIX IN API produção no código | **Não implementado** (sandbox/stub fora de guards) |
| Provider efetivo depósito | **Mercado Pago** (gate Asaas fechado) |

---

## 4. Etapa 4 — Classificação de prontidão

| Pergunta | Resposta | Evidência |
|----------|:--------:|-----------|
| API Key produção válida? | **NÃO** | `sshKeyLen=0` no runtime Fly |
| Conta produção acessível? | **NÃO** | Probes API não executados |
| Webhooks produção cadastrados? | **NÃO VERIFICADO** | `GET /webhooks` bloqueado |
| PIX IN disponível? | **NÃO VERIFICADO** | Sem API prod |
| PIX OUT disponível? | **NÃO VERIFICADO** | Sem API prod |
| Saldo consultável? | **NÃO** | `GET /finance/balance` bloqueado |
| Backend compatível? | **PARCIAL** | Wire + health OK; chave runtime vazia; flags parciais |
| Go-live pode avançar para teste controlado? | **NÃO** | Bloqueadores abaixo |

---

## 5. Bloqueadores para teste controlado

1. **`ASAAS_API_KEY` vazia no runtime Fly** — impossível validar API produção e conta.
2. **Probes Asaas produção não executados** — webhooks painel, PIX, saldo sem evidência.
3. **`ASAAS_ENV` / `ASAAS_ENABLED` ausentes** no runtime Fly.
4. **PIX IN backend** ainda sem HTTP produção (P1.1A D6).
5. **Nenhum ciclo real** depósito → webhook → saldo homologado.

---

## 6. Comparativo P1.1A → P1.1B

| Item | P1.1A | P1.1B |
|------|-------|-------|
| `flyctl` autenticado | ❌ | ✅ |
| Secrets Fly listados | ❌ | ✅ |
| `ASAAS_PRODUCTION_ENABLED` | assumido false | **confirmado `false`** |
| `PAYMENT_WEBHOOK_ENGINE_ENABLED` | false (404 webhook) | **`true` (403 webhook)** |
| `ASAAS_WEBHOOK_ENABLED` | não confirmado | **`true`** |
| `ASAAS_API_KEY` Fly | não verificado | **presente no vault, vazio no runtime** |
| API `api.asaas.com` | 401 (chave sandbox) | **não testada** (sem chave) |

---

## 7. Veredito P1.1B

# NOT READY

**Critérios GO-LIVE READY não atendidos:**

| Critério | OK |
|----------|:--:|
| `ASAAS_API_KEY` válida em produção | ❌ |
| Backend produção saudável | ✅ |
| Wire Asaas deployado | ✅ |
| Webhook produção configurado (painel) | ❌ não verificado |
| PIX IN disponível | ❌ não verificado |
| Saldo consultável | ❌ |
| `ASAAS_PRODUCTION_ENABLED` ainda false | ✅ |

**Não aplicável:** `GO-LIVE READY COM RESSALVAS` — falha em critérios obrigatórios (chave, conta, webhooks, saldo), não apenas PIX OUT.

---

## 8. Ações corretivas sugeridas (fora do escopo P1.1B — operador)

1. **Re-set `ASAAS_API_KEY`** no Fly com chave produção válida (`$aact_prod_…`) e **restart** das máquinas (`fly apps restart goldeouro-backend-v2`).
2. Confirmar `sshKeyLen > 0` e prefixo `$aact_prod_` antes de nova auditoria.
3. Configurar `ASAAS_ENABLED=true` e `ASAAS_ENV=production` quando autorizado (runbook §2).
4. Validar webhook no painel Asaas: URL `https://goldeouro-backend-v2.fly.dev/webhooks/asaas`, eventos `PAYMENT_RECEIVED` / `PAYMENT_CONFIRMED`, token = `ASAAS_WEBHOOK_TOKEN`.
5. Reexecutar probes P1.1B (ou P1.1C) após correção da chave.

---

## 9. Conformidade da auditoria

| Verificação | Status |
|-------------|:------:|
| Código alterado | **Não** |
| Banco alterado | **Não** |
| `.env` alterado | **Não** |
| Deploy executado | **Não** |
| `ASAAS_PRODUCTION_ENABLED` alterado | **Não** (permanece `false`) |
| Cobrança / PIX / webhook criados | **Não** |
| Secrets alterados | **Não** |
| Valores de secrets exibidos | **Não** |

---

**Fim do relatório P1.1B**
