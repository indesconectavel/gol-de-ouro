# P1.5FIN — Auditoria Geral Read-Only do Sistema Financeiro

**Projeto:** Gol de Ouro™ V1  
**Engine:** Indesconectável Payment Engine™  
**Data:** 2026-06-29  
**Modo:** READ-ONLY ABSOLUTO — nenhum código, banco, gate, deploy ou transferência alterados  
**Base:** P1.5Y.EXEC.R2 (FAIL `invalid_action`) + P1.5Y.DIAG (DIAG-CONTA)

---

## Veredito Final

# `FIN-PASS-COM-RESSALVAS`

O sistema financeiro interno está **tecnicamente íntegro** para operação controlada: rollback do valor principal executado, nenhuma transferência Asaas criada, gates de produção **fechados**, runtime **saudável**. O bloqueio remanescente do PIX OUT Asaas decorre **predominantemente de conta/permissão/provedor** (hipótese `invalid_action` = autorização crítica ou conta não habilitada para transferências API), **não** de payload incorreto nem de falha estrutural da Payment Engine.

**Ressalvas auditadas:**

| Ressalva | Severidade | Impacto |
|----------|------------|---------|
| Rollback da **taxa** (R$ 2,00) ausente no ledger | Média | Saldo usuário R$ 10,00 vs R$ 12,00 esperado após rollback integral |
| `asaas_transfer_status` = `DONE` com `asaas_transfer_id` null | Baixa | Higiene de persistência — não implica transferência real |
| Consulta `pagamentos_pix` recente indisponível nesta sessão | Baixa | Baseline V1.2A (2026-06-28) usado para PIX IN |
| GET saldo/conta Asaas produção não executado | Operacional | Pendência para confirmar saldo ≥ R$ 8,00 |

---

## 1. Resumo Executivo

| Pergunta central | Resposta |
|------------------|----------|
| Sistema financeiro íntegro? | **Sim**, com ressalvas menores de ledger/higiene |
| Bloqueio PIX OUT Asaas é interno ou externo? | **Externo** (conta/permissão/provedor) |
| Payload compatível? | **SIM** |
| Rollback financeiro correto? | **INCONCLUSIVO** — principal OK; taxa não revertida |
| Gates seguros pós-R2? | **SIM** |
| Próxima etapa | Chamado Asaas + crédito saldo + novo saque → P1.5Y.EXEC.R3 |

**Evidências primárias consultadas:**

- Relatórios `P1.5Y.EXEC.R2`, `P1.5Y.DIAG`, `P1.5Y.R1`
- Código Payment Engine (`src/finance/`, `src/domain/payout/`)
- Consultas read-only Supabase (2026-06-29 ~06:35 UTC)
- `fly ssh` fase `P15Y_PHASE=audit` em produção
- `GET /health` público (200, database connected)

---

## 2. Escopo

Auditoria cobrindo os 15 eixos solicitados:

1. PIX IN · 2. PIX OUT · 3. Wallet · 4. Ledger · 5. Saques · 6. Pagamentos PIX  
7. Payment Engine · 8. Asaas Provider · 9. Mercado Pago fallback · 10. Gates  
11. Webhooks · 12. Idempotência · 13. Rollback financeiro · 14. Evidências R2 · 15. Estado pós-`invalid_action`

**Proibido e respeitado:** INSERT/UPDATE/DELETE, abertura de gates, POST `/transfers`, deploy, commit.

---

## 3. Estado Atual

| Dimensão | Estado (2026-06-29) |
|----------|---------------------|
| Runtime Fly | `app` started, health 1/1 passing, v506 |
| `/health` | 200 — `database: connected`, `mercadoPago: connected` |
| `payout_worker` | **stopped** (não processa fila batch) |
| Gates PIX OUT produção | **OFF** (confirmado em runtime) |
| Saque R2 `a705bb8c-…` | `falhou` — não reutilizável |
| Transferência Asaas | **Não criada** (`asaas_transfer_id` null) |
| Candidatos pendentes | 1 (`94480975-…`) |
| P1.5Z | **Bloqueada** |

---

## 4. Evidências Asaas (Painel Operador)

Classificação: **evidência visual operacional** — orienta diagnóstico, **não substitui** confirmação oficial do suporte.

| Evidência (prints operador) | Implicação |
|-----------------------------|------------|
| Saldo conta **R$ 3,02** | **Abaixo** do valor da tentativa R2 (R$ 8,00 líquido) — possível causa secundária |
| Pix recebido com sucesso | PIX IN operacional no painel |
| Chave Pix aleatória cadastrada | Infraestrutura Pix presente |
| API Key "GolDeOuro Production API" ativa | Credencial operacional |
| Permissão "saque via API" ligada | Permissão **visual** — pode divergir do backend Asaas |
| Log `/api/v3/transfers` HTTP **400** | Corrobora R2 — request chegou, rejeitado |

**Correlação com R2:** request em `2026-06-29T05:28:26Z`, `errorCode: invalid_action`, sem `transfer_id`.

---

## 5. Estado dos Gates

### 5.1 Runtime Fly (audit SSH — `P15Y_PHASE=audit`)

```json
{
  "ASAAS_PIX_OUT_PRODUCTION_ENABLED": "false",
  "PAYMENT_ENGINE_PIXOUT_ENABLED": "false",
  "ASAAS_PRODUCTION_ENABLED": "false",
  "ASAAS_PIX_OUT_ENABLED": null,
  "PAYOUT_PROVIDER": null,
  "ASAAS_ENV": "production",
  "ASAAS_ENABLED": "true",
  "PAYMENT_WEBHOOK_ENGINE_ENABLED": "true",
  "ASAAS_WEBHOOK_ENABLED": "true",
  "PAYOUT_PIX_ENABLED": "true"
}
```

### 5.2 Fly secrets (digest — sem valores)

| Secret | Digest | Interpretação |
|--------|--------|---------------|
| `ASAAS_PIX_OUT_PRODUCTION_ENABLED` | `fa61a138…` | = **false** (mesmo digest de flags OFF) |
| `PAYMENT_ENGINE_PIXOUT_ENABLED` | `fa61a138…` | = **false** |
| `ASAAS_PRODUCTION_ENABLED` | `fa61a138…` | = **false** |
| `PAYOUT_PROVIDER` | *(ausente na lista)* | **unset** ✅ |
| `ASAAS_PIX_OUT_ENABLED` | *(ausente na lista)* | **unset** ✅ |

### 5.3 Verificação local de guards (`asaas-pix-out-config`)

Com `ASAAS_PIX_OUT_PRODUCTION_ENABLED=false` e `PAYMENT_ENGINE_PIXOUT_ENABLED=false`, `isAsaasPixOutProductionHttpEnabled()` retorna **false** — POST `/transfers` produção **bloqueado** no código.

### 5.4 Conclusão gates

```text
Produção segura para PIX OUT? SIM
/health 200? SIM
Crash loop? NÃO observado
```

---

## 6. Banco de Dados

### 6.1 Tabela `saques` — status agregado

| status | count |
|--------|------:|
| cancelado | 15 |
| rejeitado | 7 |
| falhou | **3** (+1 desde baseline V1.2A) |
| pendente | **1** |
| pago_manual | 1 |

### 6.2 Saque R2 — `a705bb8c-5f90-4532-9f55-d167dbdff672`

| Campo | Valor |
|-------|-------|
| `status` | **`falhou`** |
| `amount` / `net_amount` / `fee` | R$ 10 / R$ 8 / R$ 2 |
| `asaas_transfer_id` | **null** |
| `asaas_transfer_status` | `DONE` ⚠️ (inconsistente — ver §9) |
| `asaas_payout_raw` | `{ id: null, status: "DONE", value: 8, externalReference: "wd_67786aba15ef420083118834" }` |
| `last_asaas_sync_at` | `2026-06-29T06:21:55.977Z` |
| `payout_external_reference` | `wd_67786aba15ef420083118834` |
| `correlation_id` | `486f6bef-4d6f-42da-be56-62a28e55dd3d` |
| `motivo_rejeicao` | `payout_asaas_erro` |
| `processed_at` | `2026-06-29T05:28:27.005Z` |
| `mp_*` | sem uso nesta tentativa |

### 6.3 Schema P1.5D

Migration `20260628_p15d_asaas_payout_transfer_columns.sql` — colunas additive-only: `asaas_transfer_id`, `asaas_transfer_status`, `asaas_payout_raw`, `last_asaas_sync_at`. Verificação estática P1.5D: **OK**.

---

## 7. Wallet

| Fonte | Valor | Nota |
|-------|------:|------|
| `usuarios.saldo` (usuário afetado) | **R$ 10,00** | Campo canônico usado no rollback |
| `wallets.saldo` | **null** (sem linha) | Tabela `wallets` não populada para este usuário |
| Antes R2 (R1) | R$ 0,00 | Débito prévio já refletido |
| Após rollback R2 | R$ 10,00 | +R$ 10 principal |

**Consistência:** saldo reflete rollback do **valor bruto** (R$ 10). Taxa (R$ 2) **não** foi creditada de volta — ver §8.

---

## 8. Ledger

### 8.1 Por tipo (sistema)

| tipo | count |
|------|------:|
| saque | 17 |
| taxa | 17 |
| rollback | 17 |
| falha_payout | 16 |
| deposito | 12 |
| payout_manual_confirmado | 2 |
| rollback_manual | 1 |

### 8.2 Ledger do saque R2 (`correlation_id` `486f6bef-…`)

| # | tipo | valor | referencia | created_at (UTC) |
|---|------|------:|------------|------------------|
| 1 | `saque` | 10 | `a705bb8c-…` | 2026-06-01T18:27:07 |
| 2 | `taxa` | 2 | `a705bb8c-…:fee` | 2026-06-01T18:27:07 |
| 3 | `falha_payout` | 8 | `a705bb8c-…` | 2026-06-29T05:28:26 |
| 4 | `rollback` | 10 | `a705bb8c-…` | 2026-06-29T05:28:26 |

**Ausente:** `rollback` com referencia `a705bb8c-…:fee` (valor R$ 2,00).

### 8.3 Duplicidade

Nenhuma duplicidade de `falha_payout` ou `rollback` principal para este saque.

### 8.4 Resposta objetiva — rollback

```text
O rollback financeiro foi correto?
INCONCLUSIVO
```

| Critério | Resultado |
|----------|-----------|
| Débito original preservado no histórico | ✅ |
| `falha_payout` registrado (R$ 8 líquido) | ✅ |
| Rollback principal (R$ 10) | ✅ |
| Rollback taxa (R$ 2) | ❌ ausente |
| Saldo final vs pré-saque | ⚠️ -R$ 2,00 vs estado pré-criação do saque |
| Dinheiro enviado ao PIX externo | ❌ nenhum |

O código em `rollbackWithdraw()` **prevê** rollback da taxa (`referencia: ${saqueId}:fee`), mas o ledger pós-R2 não contém essa entrada. Não há evidência de perda para terceiros — é inconsistência interna de compensação da taxa em falha de payout.

---

## 9. Saques

| Aspecto | Achado |
|---------|--------|
| Fluxo admin | `approveAndSendWithdrawAdmin` → `createPixWithdrawCompat` → factory Asaas |
| Worker automático | Parado — não interfere pós-R2 |
| Saque R2 | Terminal `falhou` — bloqueado para reexecução |
| Próximo candidato | `94480975-…` (pendente, R$ 10/8) |
| Conta técnica P1.4 | `85872488-…` saldo alto, sem saque pendente — preferida para R3 |

---

## 10. PIX IN

| Componente | Status | Nota |
|------------|--------|------|
| Mercado Pago (legado) | Operacional | Baseline: 45 approved, 66 pending |
| Asaas PIX IN | Evidência painel | Pix recebido com sucesso (operador) |
| `pagamentos_pix` (query direta) | Indisponível nesta sessão | Retorno null — usar baseline V1.2A |
| Webhook engine | Habilitado | `PAYMENT_WEBHOOK_ENGINE_ENABLED=true` |

**Risco PIX IN:** baixo para operação MP existente; Asaas IN depende de gates separados (`ASAAS_PIX_IN_ENABLED`).

---

## 11. PIX OUT

| Aspecto | Status |
|---------|--------|
| Payment Engine até Asaas | ✅ Validado em R2 |
| POST `/v3/transfers` produção | ✅ Enviado |
| Resposta | ❌ HTTP 400 `invalid_action` |
| `transfer_id` | ❌ null |
| Webhook TRANSFER | N/A |
| MP fallback (gates OFF) | Preservado — factory resolve MP quando `PAYOUT_PROVIDER` unset e gates Asaas OFF |

---

## 12. Payment Engine

### 12.1 Inventário de componentes

| Componente | Função | Status | Risco |
|------------|--------|--------|-------|
| `FinanceProviderFactory` | Resolve PSP payout/payment | ✅ | Baixo |
| `createPixWithdrawCompat` | Ponte legado → `requestPixPayout` | ✅ | Baixo |
| `AsaasPayoutProvider` | Adapter PIX OUT Asaas | ✅ | Baixo |
| `AsaasProvider.createPixWithdraw` | HTTP produção com guards | ✅ | Baixo |
| `asaas-http-client` | POST/GET Asaas, sanitização | ✅ | Médio — não loga `description` completa de erros |
| `asaas-pix-out-config` | Gates produção P1.5B/C | ✅ | Baixo |
| `payoutProviderPersistence` | Patch `asaas_*` / `mp_*` | ⚠️ | Médio — persiste `status` mesmo em falha parcial |
| `processPendingWithdrawals` | Rollback, ledger, worker | ⚠️ | Médio — fee rollback não observado no R2 |
| `processAsaasTransferWebhook` | Reconciliação TRANSFER_* | ✅ | Baixo |
| `processPaymentWebhook` | Roteamento webhooks | ✅ | Baixo |
| `adminWithdrawController` | Aprovação manual admin | ✅ | Baixo |
| `pix-mercado-pago.js` | Legado MP | ✅ | Baixo |
| `mpWebhook.js` | Webhook MP | ✅ | Baixo |
| Scripts P1.5Y/P1.5D | Homologação/verificação | ✅ | Baixo |
| Migration P1.5D | Colunas `asaas_*` em saques | ✅ Aplicada | Baixo |

### 12.2 Confirmações da engine

| Verificação | Resultado |
|-------------|-----------|
| Provider = Asaas com gates ON (R2) | ✅ |
| MP fallback com gates OFF | ✅ |
| POST usa `https://api.asaas.com/v3/transfers` | ✅ |
| API Key não em logs (preview mascarado) | ✅ |
| Payload mascarado em logs | ✅ |
| Erro propagado (`ASAAS_API_REQUEST_FAILED`) | ✅ |
| Rollback acionado em falha | ✅ (parcial — ver §8) |

---

## 13. Payload Asaas

Reconstruído de R2 + `createPixTransfer()`:

| Campo | Valor enviado |
|-------|---------------|
| Endpoint | `POST https://api.asaas.com/v3/transfers` |
| `value` | **8** (líquido) |
| `operationType` | `PIX` |
| `pixAddressKey` | `0478***664` (11 dígitos CPF) |
| `pixAddressKeyType` | `CPF` |
| `externalReference` | `wd_67786aba15ef420083118834` |
| `description` | `Gol de Ouro saque a705bb8c-…` |
| `authToken` | **omitido** (produção — correto) |
| Headers | `Accept`, `Content-Type`, `User-Agent`, `access_token` |
| Ambiente | Produção (`httpGate: pixOutProduction`) |

```text
O payload parece compatível com a API Asaas?
SIM
```

Nenhum campo identificado como provável causa do `invalid_action`. Diferença sandbox→produção relevante: sandbox envia `authToken`; produção não — coerente com doc Asaas para autorização crítica.

---

## 14. Reconstituição do Saque R2

### Timeline financeira (UTC)

| Horário | Evento |
|---------|--------|
| 2026-06-01T18:27:07 | Criação saque — ledger `saque` R$10 + `taxa` R$2 |
| 2026-06-29T05:17:55 | Pré-check R2 — status `pendente`, wallet R$0 |
| 2026-06-29T05:21:28 | Gates abertos (v504) |
| 2026-06-29T05:28:26.079 | `compat_payout_start` |
| 2026-06-29T05:28:26.321 | `POST /transfers` |
| 2026-06-29T05:28:26.753 | HTTP **400** `invalid_action` |
| 2026-06-29T05:28:26.756 | `last_asaas_sync_at` persistido |
| 2026-06-29T05:28:26.803 | Ledger `falha_payout` R$8 |
| 2026-06-29T05:28:26.918 | Ledger `rollback` R$10 |
| 2026-06-29T05:28:27.005 | Saque → `falhou` (`payout_asaas_erro`) |
| 2026-06-29T05:29–05:30 | Gates fechados |
| 2026-06-29T06:21:55 | `last_asaas_sync_at` atualizado (sem nova transferência) |

### Estado final

| Item | Valor |
|------|-------|
| Saque | `falhou` |
| Wallet | R$ 10,00 |
| Transferência Asaas | inexistente |
| Ledger | 4 entradas (sem rollback taxa) |

---

## 15. Hipóteses do `invalid_action`

| # | Hipótese | Prob. | Evidência | Próxima ação |
|---|----------|-------|-----------|--------------|
| 1 | Conta sem liberação operacional para transferências API | **Alta** | Doc Asaas: `invalid_action` = autorização crítica; produção não envia `authToken` | Chamado suporte + desabilitar auth crítica ou IP/webhook |
| 2 | Compliance/KYC/risco bloqueando Pix Out | Média | Não confirmado em API read-only | Verificar painel + suporte |
| 3 | Saldo/limite insuficiente (R$ 3,02 < R$ 8,00) | Média-baixa | Painel operador; código oficial de saldo ≠ `invalid_action` | Creditar conta antes de R3 |
| 4 | Payload parcialmente incompatível | **Baixa** | OpenAPI + sandbox F4.2G mesmo shape | Descartada salvo contraprova do suporte |
| 5 | Tipo chave CPF rejeitado/formatado incor | Baixa | 11 dígitos, validação interna OK | Confirmar chave no DICT se suporte suspeitar |
| 6 | Permissão visual API ≠ backend habilitado | **Alta** | Print "saque via API" ligado mas 400 | Suporte confirmar flag real da conta |
| 7 | Outro (IP whitelist, conta divergente da API key) | Baixa-média | IP inseguro teria mensagem diferente | `GET /myAccount` + comparar walletId |

---

## 16. Riscos

| Risco | Nível | Mitigação |
|-------|-------|-----------|
| Reexecução sem habilitação Asaas | Alto | Manter gates OFF; P1.5Z bloqueada |
| Saldo Asaas < valor teste | Médio | Creditar antes de R3 |
| Saque R2 reutilizado | Alto | Criar **novo** saque para R3 |
| Rollback taxa incompleto | Baixo-médio | Corrigir ledger em etapa dedicada se política exigir devolução integral |
| `asaas_transfer_status` DONE órfão | Baixo | Limpar em manutenção; não afeta PIX real |
| Worker stopped + 1 pendente | Baixo | Não processa batch até ligado — monitorar |

---

## 17. Recomendações

### Imediatas (sem código)

1. Abrir chamado Asaas (§18).
2. Confirmar no painel: KYC, saldo ≥ R$ 8, autorização crítica, transferências API.
3. Creditar conta Asaas se saldo for bloqueador.
4. Executar `scripts/p15y-diag-asaas-readonly.cjs` com API Key produção (somente GET) para confirmar saldo e `walletId`.
5. Registrar IP egress Fly para whitelist se política exigir.

### Antes de P1.5Y.EXEC.R3

| Pré-requisito | Motivo |
|---------------|--------|
| Suporte confirma PIX OUT API automático | Eliminar `invalid_action` |
| Saldo Asaas ≥ valor do teste | Evitar falha secundária |
| Novo saque (conta técnica preferida) | R2 está `falhou` |
| Decisão auth crítica | IP fixo **ou** webhook auth **ou** desabilitação via gerente |
| Avaliar correção rollback taxa R$2 | Integridade ledger opcional pré-R3 |

### Melhorias futuras (fora escopo P1.5FIN)

| Melhoria | Prioridade |
|----------|------------|
| Não persistir `asaas_transfer_status` em falha HTTP sem `transfer_id` | Média |
| Logar `description` sanitizada de erros Asaas | Média |
| Gate read-only produção (`GET /finance/balance`) | Baixa |
| Webhook autenticação Asaas (F4.2G.2) | Alta se manter Asaas PSP OUT |

---

## 18. Mensagem para Suporte Asaas

**Assunto:** HTTP 400 `invalid_action` em `POST /v3/transfers` — habilitação PIX OUT via API

---

Olá, equipe Asaas.

Estamos integrando transferências PIX OUT via API em **produção** e recebemos erro ao criar transferência:

| Campo | Valor |
|-------|-------|
| Endpoint | `POST https://api.asaas.com/v3/transfers` |
| Horário (UTC) | **2026-06-29 ~05:28:26** |
| HTTP | **400** |
| `errorCode` | **`invalid_action`** |
| `operationType` | `PIX` |
| Valor | **R$ 8,00** |
| `pixAddressKeyType` | **CPF** |
| `externalReference` | `wd_67786aba15ef420083118834` |

A transferência **não foi criada** (sem `id` retornado).

**Pergunta:** a conta vinculada à nossa API Key de produção está habilitada para **transferências PIX via API** sem confirmação manual? É necessário desabilitar **autorização crítica**, configurar **IP fixo** ou **webhook de autenticação** para automação de saques?

Podem confirmar também se há pendência de **KYC**, **saldo mínimo** ou **limite** que explique o bloqueio?

*(Não incluímos API Key, CPF completo ou dados sensíveis neste chamado.)*

---

## 19. Próxima Etapa

Dado veredito **`FIN-PASS-COM-RESSALVAS`**:

```
┌─────────────────────────────────────────────────────────────┐
│  P1.5FIN → FIN-PASS-COM-RESSALVAS                           │
└───────────────────────────┬─────────────────────────────────┘
                            │
         ┌──────────────────┼──────────────────┐
         ▼                  ▼                  ▼
   Chamado Asaas      Creditar saldo      GET balance (readonly)
   (§18)             (se < R$ 8)         (script diagnóstico)
         │                  │                  │
         └──────────────────┴──────────────────┘
                            │
              Conta habilitada + saldo OK?
                            │
                            ▼
              Novo saque técnico + P1.5Y.EXEC.R3
                            │
                            ▼
                     P1.5Z (após transfer + webhook)
```

| Cenário | Ação |
|---------|------|
| Suporte confirma auth crítica | IP whitelist **ou** webhook **ou** desabilitar via gerente |
| Saldo < R$ 8 | Creditar conta Asaas |
| KYC pendente | Completar no painel |
| Payload rejeitado (improvável) | Revisar com body completo do suporte |
| Rollback taxa exigido | Etapa ledger dedicada antes de R3 |

---

## 20. Veredito Final

| Código | Significado | Aplicável? |
|--------|-------------|------------|
| **`FIN-PASS-COM-RESSALVAS`** | Sistema íntegro; bloqueio externo Asaas | **✅ SIM** |
| `FIN-FAIL-PAYLOAD` | Payload incorreto | ❌ |
| `FIN-FAIL-LEDGER` | Inconsistência grave wallet/ledger | ❌ (ressalva menor taxa) |
| `FIN-FAIL-GATES` | Gates inseguros | ❌ |
| `FIN-INCONCLUSIVO` | Evidência insuficiente | ❌ |

**Síntese:** A Indesconectável Payment Engine™ cumpriu o fluxo até o provedor; o Asaas rejeitou com `invalid_action`. Patrimônio interno protegido — nenhum PIX OUT realizado. O caminho seguro é **suporte Asaas → habilitação → saldo → R3 controlado**.

---

## Anexos

### A. Consultas read-only Asaas (Fase 9)

| Teste | Status |
|-------|--------|
| `GET /myAccount` produção | ⏳ Pendente — script `p15y-diag-asaas-readonly.cjs` não executado nesta sessão |
| `GET /finance/balance` produção | ⏳ Pendente |
| `fly ssh` com script diagnóstico | ⏳ Script ausente na imagem (DIAG) |

### B. Artefatos R2

`tmp/p15y-exec-r2/*` — não presentes no workspace local (gitignored); conteúdo citado via `P1.5Y.EXEC.R2-HOMOLOGACAO-PIXOUT-ASAAS-PRODUCAO.md`.

### C. Referências

| Documento | Uso |
|-----------|-----|
| `P1.5Y.EXEC.R2-HOMOLOGACAO-PIXOUT-ASAAS-PRODUCAO.md` | Evidência primária execução |
| `P1.5Y.DIAG-ASAAS-INVALID-ACTION.md` | Análise payload e hipóteses |
| `P1.5Y.R1-CORRECAO-E-REEXECUCAO-PIXOUT-ASAAS-PRODUCAO.md` | Estado pré-janela |
| `F4.8-AUDITORIA-FINAL-PAYMENT-ENGINE-READONLY.md` | Baseline engine |
| `V1-2A-RUNTIME-FINANCIAL-HEALTH-DATA-2026-05-18.json` | Baseline PIX IN / ledger |

---

*Pipeline: P1.5Y.EXEC.R2 (FAIL) → P1.5Y.DIAG → **P1.5FIN (FIN-PASS-COM-RESSALVAS)** → Suporte Asaas / P1.5Y.EXEC.R3 / P1.5Z*
