name: 🔒 Segurança e Qualidade

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 3 * * 1,3,5'  # Executar 3x por semana (segunda, quarta, sexta) - OTIMIZADO PARA REDUZIR CUSTOS

env:
  NODE_VERSION: '20'

jobs:
  # 🔒 Análise de Segurança
  security-analysis:
    name: 🔒 Análise de Segurança
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v6
        
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: 📦 Instalar dependências
        continue-on-error: true
        run: |
          npm ci || echo "⚠️ Falha ao instalar dependências do backend"
          cd goldeouro-player && npm ci || echo "⚠️ Falha ao instalar dependências do frontend"
          echo "✅ Dependências instaladas"
          
      - name: 🔍 CodeQL Analysis
        continue-on-error: true
        uses: github/codeql-action/init@v3
        with:
          languages: javascript
          
      - name: 🔍 CodeQL Autobuild
        continue-on-error: true
        uses: github/codeql-action/autobuild@v3
        
      - name: 🔍 CodeQL Analysis
        continue-on-error: true
        uses: github/codeql-action/analyze@v3
        
      - name: 🔍 Análise de vulnerabilidades
        continue-on-error: true
        run: |
          echo "🔍 Analisando vulnerabilidades..."
          npm audit --audit-level=moderate || echo "⚠️ Vulnerabilidades encontradas no backend"
          cd goldeouro-player
          npm audit --audit-level=moderate || echo "⚠️ Vulnerabilidades encontradas no frontend"
          
      - name: 🔍 Verificação de secrets
        continue-on-error: true
        run: |
          echo "🔍 Verificando secrets expostos..."
          if command -v truffleHog &> /dev/null; then
            truffleHog --no-verification --regex --entropy=False . || echo "⚠️ Secrets podem ter sido encontrados"
          else
            echo "⚠️ TruffleHog não instalado. Pulando verificação de secrets."
          fi
          
      - name: 🔍 Análise de dependências
        continue-on-error: true
        run: |
          echo "🔍 Analisando dependências..."
          if [ -f "audit-ci.json" ]; then
            npx audit-ci --config audit-ci.json || echo "⚠️ Vulnerabilidades encontradas"
          else
            echo "⚠️ audit-ci.json não encontrado. Pulando análise de dependências."
          fi

  # 📊 Análise de Qualidade
  quality-analysis:
    name: 📊 Análise de Qualidade
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v6
        
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: 📦 Instalar dependências
        continue-on-error: true
        run: |
          npm ci || echo "⚠️ Falha ao instalar dependências do backend"
          cd goldeouro-player && npm ci || echo "⚠️ Falha ao instalar dependências do frontend"
          echo "✅ Dependências instaladas"
          
      - name: 🔍 ESLint Backend
        continue-on-error: true
        run: |
          echo "🔍 Executando ESLint no backend..."
          if [ -f ".eslintrc.js" ]; then
            npx eslint . --ext .js --max-warnings 0 || echo "⚠️ Problemas de lint encontrados"
          else
            echo "⚠️ ESLint não configurado"
          fi
          
      - name: 🔍 ESLint Frontend
        continue-on-error: true
        run: |
          cd goldeouro-player
          echo "🔍 Executando ESLint no frontend..."
          if [ -f ".eslintrc.js" ]; then
            npx eslint src/ --ext .js,.jsx,.ts,.tsx --max-warnings 0 || echo "⚠️ Problemas de lint encontrados"
          else
            echo "⚠️ ESLint não configurado"
          fi
          
      - name: 🔍 Prettier
        continue-on-error: true
        run: |
          echo "🔍 Verificando formatação..."
          if [ -f ".prettierrc" ]; then
            npx prettier --check . || echo "⚠️ Problemas de formatação encontrados"
            cd goldeouro-player
            npx prettier --check src/ || echo "⚠️ Problemas de formatação no frontend"
          else
            echo "⚠️ Prettier não configurado"
          fi
          
      - name: 🔍 TypeScript Check
        continue-on-error: true
        run: |
          cd goldeouro-player
          echo "🔍 Verificando TypeScript..."
          if [ -f "tsconfig.json" ]; then
            npx tsc --noEmit
          else
            echo "⚠️ TypeScript não configurado"
          fi

  # 🧪 Testes de Segurança
  security-tests:
    name: 🧪 Testes de Segurança
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v6
        
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: 📦 Instalar dependências
        continue-on-error: true
        run: |
          npm ci || echo "⚠️ Falha ao instalar dependências"
          echo "✅ Dependências instaladas"
          
      - name: 🧪 Testes de autenticação
        continue-on-error: true
        run: |
          echo "🧪 Testando autenticação..."
          if [ -f "tests/security/auth.test.js" ]; then
            npm run test:security:auth || echo "⚠️ Testes de autenticação falharam"
          else
            echo "⚠️ Testes de autenticação não encontrados"
          fi
          
      - name: 🧪 Testes de autorização
        continue-on-error: true
        run: |
          echo "🧪 Testando autorização..."
          if [ -f "tests/security/authorization.test.js" ]; then
            npm run test:security:authz || echo "⚠️ Testes de autorização falharam"
          else
            echo "⚠️ Testes de autorização não encontrados"
          fi
          
      - name: 🧪 Testes de validação
        continue-on-error: true
        run: |
          echo "🧪 Testando validação de dados..."
          if [ -f "tests/security/validation.test.js" ]; then
            npm run test:security:validation || echo "⚠️ Testes de validação falharam"
          else
            echo "⚠️ Testes de validação não encontrados"
          fi

  # 📊 Relatório de Segurança
  security-report:
    name: 📊 Relatório de Segurança
    runs-on: ubuntu-latest
    needs: [security-analysis, quality-analysis, security-tests]
    if: always()
    timeout-minutes: 10
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v6
        
      - name: 📊 Gerar relatório
        run: |
          echo "📊 Gerando relatório de segurança..."
          echo "## 🔒 Relatório de Segurança - $(date)" >> security-report.md
          echo "" >> security-report.md
          echo "### 🔍 Status das Análises:" >> security-report.md
          echo "- Análise de Segurança: ${{ needs.security-analysis.result }}" >> security-report.md
          echo "- Análise de Qualidade: ${{ needs.quality-analysis.result }}" >> security-report.md
          echo "- Testes de Segurança: ${{ needs.security-tests.result }}" >> security-report.md
          echo "" >> security-report.md
          echo "### 📈 Métricas:" >> security-report.md
          echo "- Data: $(date)" >> security-report.md
          echo "- Branch: ${{ github.ref_name }}" >> security-report.md
          echo "- Commit: ${{ github.sha }}" >> security-report.md
          echo "- Vulnerabilidades: Verificar logs acima" >> security-report.md
          
      - name: 📤 Upload relatório
        uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: security-report.md
          retention-days: 30
