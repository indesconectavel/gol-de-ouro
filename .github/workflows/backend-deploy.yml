name: 🚀 Backend Deploy (Fly.io)

on:
  push:
    branches: [ main, dev ]
    paths:
      - 'server-fly.js'
      - 'package.json'
      - 'package-lock.json'
      - 'Dockerfile'
      - 'fly.toml'
      - 'config/**'
      - 'controllers/**'
      - 'middlewares/**'
      - 'services/**'
      - 'scripts/**'
      - '.github/workflows/backend-deploy.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'server-fly.js'
      - 'package.json'
      - 'package-lock.json'
      - 'Dockerfile'
      - 'fly.toml'
      - 'config/**'
      - 'controllers/**'
      - 'middlewares/**'
      - 'services/**'
      - 'scripts/**'

env:
  NODE_VERSION: '20'
  FLY_APP_NAME: goldeouro-backend-v2  # ✅ CORRIGIDO

jobs:
  # 🔍 Análise de Código e Testes
  test-and-analyze:
    name: 🧪 Testes e Análise
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: 📦 Instalar dependências
        run: |
          npm ci
          echo "✅ Dependências instaladas"
          
      - name: 🔍 Análise de segurança
        continue-on-error: true
        run: |
          echo "🔍 Analisando vulnerabilidades..."
          npm audit --audit-level=moderate || {
            echo "⚠️ Vulnerabilidades encontradas (não bloqueia deploy)"
            echo "💡 Para corrigir: npm audit fix ou atualizar dependências manualmente"
          }
          echo "✅ Análise de segurança concluída"
          
      - name: 🧪 Executar testes
        run: |
          if [ -f "tests/backend-tests.js" ]; then
            npm test
          else
            echo "⚠️ Arquivo de testes não encontrado, pulando testes"
          fi
          echo "✅ Testes executados"
          
      - name: 📊 Análise de qualidade
        run: |
          echo "🔍 Verificando sintaxe..."
          node -c server-fly.js
          echo "✅ Sintaxe válida"
          
          echo "🔍 Verificando estrutura..."
          if [ -f "package.json" ] && [ -f "server-fly.js" ] && [ -f "fly.toml" ]; then
            echo "✅ Estrutura válida"
          else
            echo "❌ Estrutura inválida"
            exit 1
          fi

  # 🚀 Deploy para Fly.io
  deploy-backend:
    name: 🚀 Deploy Backend
    runs-on: ubuntu-latest
    needs: test-and-analyze
    if: github.ref == 'refs/heads/main'
    timeout-minutes: 30
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: 📦 Instalar dependências
        run: |
          npm ci
          echo "✅ Dependências instaladas"
          
      - name: 🔐 Configurar Fly.io
        uses: superfly/flyctl-actions/setup-flyctl@master
        
      - name: 🚀 Deploy para Fly.io
        run: |
          echo "🚀 Iniciando deploy para Fly.io..."
          if [ -z "${{ secrets.FLY_API_TOKEN }}" ]; then
            echo "⚠️ FLY_API_TOKEN não configurado. Pulando deploy."
            exit 0
          fi
          flyctl deploy --remote-only --no-cache --app ${{ env.FLY_APP_NAME }} --build-arg GIT_COMMIT="${{ github.sha }}"
          echo "✅ Deploy concluído"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          
      - name: 🔍 Verificar deploy
        run: |
          echo "🔍 Verificando status do deploy..."
          flyctl status --app ${{ env.FLY_APP_NAME }}
          echo "✅ Verificação concluída"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          
      - name: 📊 Logs do deploy
        run: |
          echo "📊 Coletando logs..."
          flyctl logs --app ${{ env.FLY_APP_NAME }} --lines 50
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          
      - name: 🧪 Teste de saúde
        continue-on-error: true
        run: |
          echo "🧪 Testando endpoint de saúde..."
          # Retry logic: 6 tentativas com intervalo de 10s (total ~60s)
          SUCCESS=0
          for i in {1..6}; do
            echo "Tentativa $i de 6..."
            if curl -f --max-time 30 https://${{ env.FLY_APP_NAME }}.fly.dev/health; then
              echo "✅ Endpoint de saúde funcionando na tentativa $i"
              SUCCESS=1
              break
            fi
            if [ $i -lt 6 ]; then
              echo "⏳ Aguardando 10s antes da próxima tentativa..."
              sleep 10
            fi
          done
          if [ "$SUCCESS" = "0" ]; then
            echo "⚠️ Health check falhou após 6 tentativas"
          fi
          
      - name: 📢 Notificar sucesso
        run: |
          echo "🎉 Deploy do backend concluído com sucesso!"
          echo "🌐 URL: https://${{ env.FLY_APP_NAME }}.fly.dev"
          echo "📊 Status: Online"

  # 🔄 Deploy para desenvolvimento
  deploy-dev:
    name: 🔄 Deploy Dev
    runs-on: ubuntu-latest
    needs: test-and-analyze
    if: github.ref == 'refs/heads/dev'
    timeout-minutes: 30
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: 📦 Instalar dependências
        run: |
          npm ci
          echo "✅ Dependências instaladas"
          
      - name: 🔐 Configurar Fly.io
        uses: superfly/flyctl-actions/setup-flyctl@master
        
      - name: 🚀 Deploy para desenvolvimento
        run: |
          echo "🔄 Deploy para ambiente de desenvolvimento..."
          if [ -z "${{ secrets.FLY_API_TOKEN }}" ]; then
            echo "⚠️ FLY_API_TOKEN não configurado. Pulando deploy."
            exit 0
          fi
          flyctl deploy --remote-only --no-cache --app ${{ env.FLY_APP_NAME }} --build-arg GIT_COMMIT="${{ github.sha }}"
          echo "✅ Deploy de desenvolvimento concluído"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          
      - name: 📢 Notificar deploy dev
        run: |
          echo "🔄 Deploy de desenvolvimento concluído!"
          echo "🌐 URL: https://${{ env.FLY_APP_NAME }}.fly.dev"
          echo "📊 Status: Ambiente de desenvolvimento"
